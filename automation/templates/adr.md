<!--
SKELETON for docs/DECISIONS/<NNNN>-<slug>.md.

Auto-generated by the review session (Phase 2B, B-tier action) when:
  - HANDOFF.md Section 2's per-milestone "design decisions (deviations
    from PLAN)" subsections collectively have >=2 divergences AND >=1
    is architectural, OR
  - A single divergence touches >2 source files (clear cross-cutting
    decision).

NNNN = (highest existing ADR number in docs/DECISIONS/) + 1, zero-padded
to 4 digits. If docs/DECISIONS/ does not yet exist, the review agent
also creates docs/DECISIONS/README.md (the index) and starts at 0001.

slug = kebab-case summary of the decision (<= 6 words), derived from the
HANDOFF divergence titles.

Comment blocks (HTML comments) are guidance to the review agent and
should NOT appear in the generated ADR.
-->

# ADR-{{NUMBER}}: {{TITLE}}

**Status**: accepted (auto-generated by `{{INITIATIVE_SLUG}}` review on {{TODAY}})
**Author**: `{{INITIATIVE_SLUG}}` review session
**Related initiative**: [`initiatives/_archive/{{ARCHIVE_SLUG}}/`](../../initiatives/_archive/{{ARCHIVE_SLUG}}/)

## Context

<!--
One paragraph: what was being done that forced this decision.
Pull verbatim from the per-milestone HANDOFF Section 2 "design decisions
(deviations from PLAN)" entries listed in DIVERGENCE_REFS, and from
PLAN.md's Goal / Background sections.
-->
{{CONTEXT_PARAGRAPH}}

## Decision

<!--
The actual decision — what was chosen, what alternatives were rejected,
and the immediate reason. Pull from the WHY field of the per-milestone
HANDOFF Section 2 "design decisions" items.
-->
{{DECISION_PARAGRAPHS}}

## Consequences

<!--
What follows. Both positive and negative — things that become easier,
things that become harder, follow-up work required. Bullet list.
-->
- {{CONSEQUENCE_1}}
- {{CONSEQUENCE_2}}

## Alternatives considered

<!--
If the divergence record names alternatives, list them with a one-line
reason for rejection. If none recorded, write "(not recorded)".
-->
{{ALTERNATIVES_OR_NONE}}

## Source

This ADR was auto-generated because **{{TRIGGER_REASON}}**. The full
divergence record is in `initiatives/_archive/{{ARCHIVE_SLUG}}/HANDOFF.md`
Section 3, items: {{DIVERGENCE_REFS}}.

Code reference (changes that this ADR explains):
```
git log --follow {{COMMIT_RANGE}} -- {{AFFECTED_PATHS}}
```

## Future updates

If a later initiative supersedes this decision, **do not edit this file**.
Write a new ADR `ADR-{{NEXT_NUMBER}}-<reversal-slug>.md` with
`**Status**: supersedes ADR-{{NUMBER}}` and link back to this one. Then
change this file's Status line to `superseded by ADR-{{NEXT_NUMBER}}`.
