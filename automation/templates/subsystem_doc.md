<!--
SKELETON for docs/<subsystem-slug>.md.

Auto-generated by the review session (Phase 2B, B-tier action) when:
  - src/ gains a new subdirectory (clear "new subsystem" signal), OR
  - src/ gains a new module >150 LOC AND with >=3 public symbols
    (function/class/dataclass exports, not counting underscored helpers).

slug = the new directory name OR the new module's basename without .py,
kebab-cased. Resulting file: docs/<slug>.md.

If a file already exists at docs/<slug>.md, the review agent appends a new
"## Recent changes" entry instead of overwriting (per safety guardrails).

Comment blocks (HTML comments) are guidance to the review agent and
should NOT appear in the generated file.
-->

# {{SUBSYSTEM_NAME}}

> Auto-generated by review session of `{{INITIATIVE_SLUG}}` on {{TODAY}}.
> Future updates: extend this file by hand. Review sessions of
> subsequent initiatives will only append to "## Recent changes" — they
> never rewrite the sections above.

## Overview

<!--
One paragraph: what this subsystem does, why it exists, what it
explicitly does not do. Pull from PLAN.md Goal + Out-of-scope.
-->
{{OVERVIEW_PARAGRAPH}}

## Modules

<!--
List every file under the new subdir (or, for a single-module subsystem,
the single file). One row each. Pull docstring summary from the source.
-->
| Module | Purpose |
|---|---|
| `{{MODULE_PATH_1}}` | {{MODULE_PURPOSE_1}} |

## Public API

<!--
Classes, functions, CLI flags, slash commands, env vars introduced by
this subsystem. Refer to CLAUDE.md per-file summary for full per-module
detail — this section is a TOC, not a duplication.
-->
{{PUBLIC_API_LIST}}

## Boundaries

<!--
- Reads from: <upstream modules / external resources>
- Writes to: <downstream modules / disk paths / network>
- Depends on: <internal packages / external libs>
- Does NOT do: <explicit exclusions from the initiative>
-->
{{BOUNDARIES_PARAGRAPH}}

## Related

- Plan: [`initiatives/_archive/{{ARCHIVE_SLUG}}/PLAN.md`](../initiatives/_archive/{{ARCHIVE_SLUG}}/PLAN.md)
- Review: [`initiatives/_archive/{{ARCHIVE_SLUG}}/REVIEW.md`](../initiatives/_archive/{{ARCHIVE_SLUG}}/REVIEW.md)
- Architecture overview: [`CLAUDE.md`](../CLAUDE.md)

## Recent changes

<!--
Append-only log. Each entry is one bullet:
  - YYYY-MM-DD — initiative slug — what changed (one line)
The review agent of FUTURE initiatives appends to this list ONLY;
they do not edit the sections above.
-->
- {{TODAY}} — `{{INITIATIVE_SLUG}}` — initial creation. {{INITIAL_CHANGE_SUMMARY}}
