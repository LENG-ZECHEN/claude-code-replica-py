# session-memory-dream — M3: Wire session-memory into loop + LLM updater + cross-process persistence

You are an autonomous agent executing milestone **M3** of the
**session-memory-dream** initiative. This session was launched by
`automation/scripts/run_all_milestones.sh`. There is no user available to
ask — every decision must be made and documented now.

When you are done, the shell loop will start the next milestone if (and
only if) you have honored the exit ritual in §5 below.

## §1 Baseline (verify before any code change)

- Last commit: `094cf90d09fef37b3f8357b4e2b8de0434834dfd`
- pytest: 912 passing (+1 xpassed)
- mypy: clean (`mypy src` → no issues in 30 source files)
- ruff: clean (`ruff check .` → All checks passed!)

Verify with:

```
git -C python-replica log --oneline -3
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

This is the **initiative baseline** (`094cf90`). By the time M3 runs, M1
and M2 have already committed, so HEAD will be *ahead* of `094cf90` and
the pytest total will be *higher* than 912 (M1 added ≥6, M2 added ≥12).
That is expected — re-derive the true "before" count from
`pytest --tb=no -q` at HEAD, not from this line. What you MUST confirm is
that `094cf90` is still the merge-base of your work (it appears in
`git -C python-replica log --oneline`) and that pytest/mypy/ruff are all
green at HEAD before you touch anything. If pytest is RED at HEAD or
`094cf90` is absent from history, **stop and report** — do not start work
against a broken or unexpected baseline.

## §2 Scope (what THIS milestone implements)

- **Phase IDs**: [S2, S3]
- **Exit gate (must be objectively verifiable)**:

  Behind a `--session-memory` flag (default OFF, mirrors
  extract_memories_enabled): `maybe_update_session_memory(...)` runs in
  loop.py::_run_stop_hooks and keeps SessionMemoryState warm across REPL
  turns. At compaction, loop.py::_force_compact uses
  SessionMemorySummarizer so a WARM reuse makes ZERO summarization
  provider calls — assert via MockProvider call-count delta across a
  budget-driven compaction (full path would add ≥1 call; SM reuse adds
  0). The LLM-mode updater uses ForkedAgentRunner (from M1) with an
  Edit-summary.md-only can_use_tool gate. session_store.py round-trips
  `session_memory_state` (save_session/load_session; absent key → empty
  state, backward compatible with old session files). Extend
  tests/test_end_to_end_long_session.py: cross-process resume preserves
  the warm SM so session B's first compaction reuses it. Two-tier
  fallback holds: cold/empty SM → full Rule/LLM compaction (null-vs-throw
  contract, no crash). pytest grows by ≥ 10.

- **Expected files to touch**:

  - MODIFIED: `src/simple_coding_agent/loop.py` — in `_run_stop_hooks`, add a
    `maybe_update_session_memory` call beside the existing
    `maybe_extract_memories`; in `_force_compact`, inject a
    `SessionMemorySummarizer` (wrapping the warm `SessionMemoryState`) into
    the `ContextCompactor` when `--session-memory` is on, falling back to the
    default Rule/LLM summarizer when OFF or cold.
  - MODIFIED: `src/simple_coding_agent/extraction_hooks.py` — add a
    `maybe_update_session_memory(...)` helper mirroring
    `maybe_extract_memories` (gated, returns a frozen outcome dataclass, never
    raises; cursor/state advance only on success).
  - MODIFIED: `src/simple_coding_agent/session_memory_state.py` — add the
    LLM-mode updater path that drives `ForkedAgentRunner` (from M1) with an
    `Edit-summary.md-only` `can_use_tool` gate; keep the M2 deterministic
    `update_session_memory` fold intact as the no-API fallback.
  - MODIFIED: `src/simple_coding_agent/session_store.py` — round-trip
    `session_memory_state` through `save_session`/`load_session`
    (backward-compatible OPTIONAL key: absent → empty state).
  - MODIFIED: `src/simple_coding_agent/cli.py` — add the `--session-memory`
    flag (default OFF); thread it into the REPL-built `AgentLoop`. If
    `openai_cli.py` shares the flag surface via `_drive_repl_session` /
    `_build_repl_loop`, thread it there too (only if required to honor the
    gate — do not expand scope otherwise).
  - MODIFIED TESTS: `tests/test_end_to_end_long_session.py` — extend so a
    cross-process resume keeps SM warm (session B's first compaction reuses
    the prewarmed summary, ZERO summarization provider calls).
  - NEW TESTS: a stop-hook test (the synchronous incremental fold fires in
    `_run_stop_hooks` and keeps the state warm across turns) and a
    persistence round-trip test (`session_memory_state` survives
    `save_session`→`load_session`; absent key → empty state, old session
    files still load).

  Touch ONLY these files plus their tests. `forked_agent.py`,
  `session_memory_state.py`'s M2 deterministic fold, and
  `SessionMemorySummarizer` already exist (M1 + M2); this milestone WIRES
  them — it does not re-introduce them.

Source mapping (read BEFORE implementing — every claim ties to a specific
TS line range):

- `claude-code-source-code/.../autoCompact.ts:241` `autoCompactIfNeeded` —
  tries SM compaction FIRST (`:288`), only `null` falls through to full
  `compactConversation` (`:312`). Replicate the two-tier "try cheap, fall
  back to full": a warm `SessionMemorySummarizer` returns the prewarmed
  text at O(0); a cold/empty state returns `null`-equivalent and
  `_force_compact` falls back to the configured Rule/LLM summarizer. This
  is the **null-vs-throw contract** — cold SM must NOT crash, it must
  fall through.
- `claude-code-source-code/.../sessionMemoryCompact.ts:498` —
  "SM-compact has no compact-API-call" (the confirmed basis for the time
  saving M4 will benchmark). This is WHY the warm path makes ZERO
  summarization provider calls.
- `claude-code-source-code/.../SessionMemory/sessionMemory.ts:488`
  `updateLastSummarizedMessageIdIfSafe` — only advance the keep-boundary
  cursor when the last turn has NO tool calls (do not orphan
  `tool_result`s across the cut). Mirror this: the incremental fold's
  cursor advances only when the trailing turn is safe to cut.
- `claude-code-source-code/.../compact.ts:1136` `streamCompactSummary` —
  the LLM call we SKIP when the SM state is warm.

Replica integration points (concrete fire sites — read these in `loop.py`
before editing; line numbers are M2-era approximations, re-grep to confirm):

- `loop.py::_run_stop_hooks` (~`:578`) — add the SYNCHRONOUS incremental
  update beside `maybe_extract_memories`. This is the stand-in for TS's
  fire-and-forget background extraction (see DIVERGENCE below).
- `loop.py::_force_compact` (~`:690`) — inject `SessionMemorySummarizer`
  into the compactor when `--session-memory` is on; cold → fall back.
- `session_store.py::_summary_to_dict` / `_summary_from_dict` /
  `save_session` / `load_session` — the round-trip choke points.

**DIVERGENCE TO DOCUMENT** (module header of the file that hosts
`maybe_update_session_memory`, AND flag it in your HANDOFF Section 5 for
the final review's Current-Limitations update): the replica has no asyncio
loop, so TS's fire-and-forget background extraction
(`query.ts:1001` `void executePostSamplingHooks`) becomes a **SYNCHRONOUS
incremental fold at the stop hook**. Net effect is the same — the
summarization cost is amortized across turns so compaction-time reuse is
~O(0) — but the producer is not a separate thread. State this the SAME way
the loop already documents synchronous sideQuery recall (see
`recall_hooks.py` per-file summary and the Current-Limitations entry on
sideQuery). A thread-backed truly-concurrent updater is **OPTIONAL and
explicitly NOT required this milestone** — leave a one-line note, do not
build it.

This is the **WIRING milestone** — the heaviest of the S-line but under
thresholds (≤5 components, ~10 tests, "wire" not "introduce+wire+CLI").
The two-tier fallback is **mandatory**: cold/empty SM → full Rule/LLM
compaction with no crash (the null-vs-throw contract above). Do not let
"warm reuse" become the only path.

Scope is **tight**. Doing more than this is failure. §2.5 below lists
what you must NOT do even if it seems natural.

## §2.5 Out of scope (do NOT do these, even if tempting)

- **Do not implement any other milestone** in this initiative. Even if
  you "have time" or "see the obvious next step", stop at `M3`. In
  particular: **NO MetricsCollector `sm_compact_reuses`/`sm_compact_misses`
  counters, NO `reused=<bool>` trace field, NO `bench_sm_compact_latency.py`**
  — that is all M4. **NO `consolidation_lock.py`, NO `dream.py`, NO
  `memory dream` CLI subcommand** — that is M5/M6/M7.
- **Do not refactor unrelated modules.** Touch only files in the
  Expected-files list (§2), plus tests for those files.
- **Do not re-introduce or re-design** `forked_agent.py::ForkedAgentRunner`
  (M1), the M2 deterministic `update_session_memory` fold, or
  `SessionMemorySummarizer` (M2). M3 WIRES them. If you find a real bug in
  any of them, note it in HANDOFF Section 5 and work around it minimally —
  do not rewrite.
- **Do not add a new trace channel.** The 11-name trace vocabulary in
  `trace.py` is FROZEN and test-pinned. SM-compact reuses the existing
  `compact` channel; the `reused=<bool>` field is M4's job, not yours. Do
  not emit a new channel here.
- **Do not modify** `automation/scripts/run_all_milestones.sh`,
  `automation/RUNBOOK.md`, or `automation/templates/*.md` — those are
  the harness, not the project.
- **Do not modify** `CLAUDE.md` per-file summaries or `README.md` beyond
  the minimum required by the new `--session-memory` CLI flag. The review
  session at the end of the initiative handles project-doc updates (per
  RUNBOOK Doc-update tiers); doing more here causes merge conflicts and
  wastes the review's audit. (The no-async divergence belongs in your
  HANDOFF Section 5 and the module header, NOT in CLAUDE.md Current
  Limitations — the final review folds that in.)
- **Do not break `session_store.py` backward compatibility.** Old session
  files without a `session_memory_state` key MUST still load (absent →
  empty state). This is a carried-forward constraint, not optional.
- **Do not change public API signatures** of existing modules unless §2
  requires it (the `ContextCompactor` / `CompactSummary` / `Summarizer`
  Protocol contracts are frozen drop-in surfaces from M2 — do not alter
  them).
- **Do not introduce new dependencies** in `pyproject.toml`.

If you find an obvious bug or improvement outside scope, **write it in
your HANDOFF.md Section 5 ("risks" or as a deferred item) and move on**.
Do not fix it.

## §3 Mandatory reading (in order, before any code change)

1. **`python-replica/CLAUDE.md`** — architecture, per-file summaries,
   project-wide execution rules (TDD, immutability, file-size limits).
   Pay special attention to the `loop.py`, `extraction_hooks.py`,
   `compact.py`, `session_store.py`, and `recall_hooks.py` summaries —
   they describe the exact stop-hook, compaction, persistence, and
   synchronous-divergence patterns you are extending.
2. **`python-replica/initiatives/current/PLAN.md`** — this initiative's
   full brief, including the milestone tables and the locked design
   decisions (default LLM mode + deterministic fallback; path-scoped
   `can_use_tool` gate).
3. **`python-replica/initiatives/current/config.yaml`** — machine-readable
   milestone table; confirms `exit_gate` + `phase_ids` for `M3`.
4. **`python-replica/initiatives/current/HANDOFF.md`** — current state.
   Section 2 "Completed milestones" tells you what M1 and M2 shipped.
   Section 4 "Important constraints" lists invariants you MUST respect
   (the frozen 11-channel trace vocab + the `session_store` backward-compat
   rule are already recorded there). Section 5 "Next milestone guidance"
   is written FOR you by the M2 session.
5. **`python-replica/initiatives/current/PROGRESS.md`** — terse fact log
   of each completed milestone (commit, test delta, files touched).
6. **The Expected-files list from §2** — read every source file this
   milestone will touch, plus their existing tests, before writing any
   code. In particular: `forked_agent.py` (M1's runner — confirm the
   `run(task_prompt, context_messages=())` signature and the
   `can_use_tool(name, input) -> (allow, reason)` gate shape), and
   `session_memory_state.py` + its M2 tests (`tests/test_session_memory_state.py`,
   `tests/test_session_memory_summarizer.py`) — confirm the warm/cold
   contract and `to_jsonable()`/`from_jsonable()` round-trip before wiring
   persistence.
7. **M3 is not M1**: run `git -C python-replica log --oneline -10` and
   `git -C python-replica show HEAD~1` to see what the immediately prior
   milestone (M2) changed. (`HEAD~1` is M2's commit because each milestone
   commits exactly once before the loop advances; if you suspect that
   invariant has been violated, use
   `git -C python-replica log 094cf90d09fef37b3f8357b4e2b8de0434834dfd..HEAD --oneline | grep -F "[sm-dream/" | head -1`
   instead and `git -C python-replica show <sha>` to inspect that SHA. The
   `-C python-replica` is required because the autonomous loop launches
   `claude --print` from `/Users/leng/my-cc-py`, not from inside the repo.
   The `grep -F` is required because the literal `[` in the marker would
   otherwise be parsed as an unclosed character class (e.g. `[sm-dream/`
   triggers `grep: invalid character range`); `-F` forces fixed-string
   matching. The `094cf90d09fef37b3f8357b4e2b8de0434834dfd..HEAD` range
   matches what `run_all_milestones.sh` uses, so the fallback stays
   consistent with the resumability check and is robust against any
   prefix-collision edge case Phase 1 pre-flight may have missed.)
8. **M3 is > M2**: skim `initiatives/current/logs/M2.log` for any
   anomalies (warnings, retries, KeyboardInterrupts) from the M2 session
   that didn't make it into HANDOFF Section 2 design-decisions. M2
   introduced `SessionMemoryState`, the incremental fold, and
   `SessionMemorySummarizer`; an M2-log surprise about the warm/cold
   contract or the section accumulator directly affects your wiring.

**Before invoking any Edit or Write tool**, print a **5-bullet summary**
to stdout (plain text, not a tool call, not a TaskCreate todo, not a
file write) of what you learned from PLAN / HANDOFF / PROGRESS / the M2
commit. If those sources disagree on a fact, treat HANDOFF as advisory
and `git diff` + test output as source of truth. This summary lives only
in the captured session log under `initiatives/current/logs/` (not in any
committed file) — its purpose is to force genuine reading rather than
skimming. If you cannot produce a substantive 5-bullet summary, you have
not read enough; go back to §3.

## §4 Implementation requirements

Follow execution rules documented in `python-replica/CLAUDE.md`. In
particular:

- **TDD**: write tests first (RED — must actually run and fail before
  implementation), implement (GREEN), refactor. Every new code path
  needs a test before the implementation lands. The three required test
  surfaces (cross-process warm-resume, stop-hook fold, persistence
  round-trip) must each start RED.
- **Immutability**: dataclasses prefer `frozen=True` where appropriate;
  `maybe_update_session_memory` must return a NEW `SessionMemoryState`
  (input unchanged), mirroring the M2 fold's immutability contract and the
  `ExtractionHookOutcome` pattern in `extraction_hooks.py`. Never mutate
  inputs; return new objects.
- **File limits**: functions ≤50 lines, files ≤800 lines. `loop.py` and
  `extraction_hooks.py` are already large — if a wiring edit would push
  either past 800 lines, extract a focused helper (mirror how M5/M7
  extracted `extraction_hooks.py` / `recall_hooks.py` to keep `loop.py`
  ≤800).
- **Determinism in tests**: `MockProvider`, `tmp_path`, `monkeypatch`.
  No network. No real API key. The ZERO-provider-call assertion is the
  spine of this milestone — use `MockProvider.history` (call-count delta)
  to prove warm reuse adds 0 summarization calls and the cold/full path
  adds ≥1.
- **Two-tier fallback is a hard requirement**: a cold/empty
  `SessionMemoryState` must make `_force_compact` fall back to the
  configured Rule/LLM summarizer WITHOUT crashing (null-vs-throw
  contract). Add at least one test that drives compaction with a cold SM
  and asserts a valid `CompactSummary` with non-empty `summary_text`.
- **No swallowed exceptions** — except the documented hook contract:
  `maybe_update_session_memory` never raises (mirrors
  `maybe_extract_memories` / `inject_memory_attachments`); on any
  exception the prior state cursor is preserved (at-least-once retry) and
  the failure is surfaced on the returned outcome, not swallowed silently.
- **No `git add -A`** — stage explicit paths only.
- **Match style** of existing modules (see CLAUDE.md per-file summaries) —
  especially the gate-then-construct shape of `maybe_extract_memories` and
  the optional-key tolerance of `session_store.py`'s existing
  `restored_files`/`timestamp` round-trip.

After every meaningful diff, run:

```
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

All three must be green before you commit.

## §5 Exit ritual (MANDATORY)

After your milestone work passes the exit gate in §2, before stopping,
perform these steps **in order**. The shell script independently
verifies the **6 side-effects** listed at the end of this section.
Step 1 below ("Confirm exit gate met") is **honor-system** — the script
cannot inspect whether you actually quoted the right command output, so
the mechanical checks are produced by steps 2-4 (commit, PROGRESS
append, HANDOFF rewrite) plus the standing pytest-green requirement
from §4. Skipping step 1 makes a green run meaningless even though it
would still pass the gate; do not skip it.

### 1. Confirm exit gate met (objectively)

Quote the concrete check from §2 verbatim and show the command output
that proves it. Example:

```
$ pytest tests/test_end_to_end_long_session.py tests/test_session_store.py --tb=no -q
..........  N passed in 0.6s

$ pytest tests/<your-new-stop-hook-test>.py -q -k "warm_reuse_zero_calls"
.  1 passed
(MockProvider.history call-count delta across the budget-driven compaction == 0 for warm SM, ≥1 for cold/full path)
```

"Implementation feels complete" is NOT acceptable. The gate must be
objectively verified by a command's output. The load-bearing assertion is
the **ZERO summarization provider-call delta on the warm path** and the
**cross-process warm-resume** — both must show in your quoted output.

### 2. Append a block to `initiatives/current/PROGRESS.md`

**Write this BEFORE committing** so it can be staged in the same commit
as your code. PROGRESS.md is the **fact log for the final review** —
narrative belongs in HANDOFF, not here.

Block shape (use terse format from `automation/templates/progress_entry.md`):

```
## M3 — done YYYY-MM-DD

- commit: [sm-dream/M3] (see git log)
- tests: <before> -> <after> (+N)
- mypy: clean | ruff: clean
- files changed: `<file1>`, `<file2>`, ...
- exit gate: <quote from §2> -> PASS (<one-line evidence>)
- notes: <optional, ≤1 line>
```

Leave the SHA as `(see git log)` — you do not know it yet and must NOT
amend to fill it in (see note in step 4 below).

### 3. Rewrite `initiatives/current/HANDOFF.md`

Use the 5-section structure in `automation/templates/handoff_milestone.md`.
The structure is non-negotiable.

**Section 2 "Completed milestones"**: APPEND a new `### M3` subsection.
Do NOT delete or rewrite the prior `### M1` and `### M2` blocks — each
milestone is the source of truth on itself.

**Section 4 "Important constraints"**: APPEND the invariants M3 freezes.
At minimum: the `--session-memory` flag is default OFF (M4+ must keep it
opt-in); the `session_memory_state` key in `session_store.py` is OPTIONAL
and backward-compatible (already carried forward from bootstrap — re-affirm
it now applies to a real key); the two-tier null-vs-throw compaction
contract (cold SM falls through, never crashes) must not be broken by M4's
observability wiring.

**Section 5 "Next milestone guidance"**: write as if onboarding the M4
agent in person. Include:
- next scope (M4 = SM-compact observability + dual-arm latency benchmark:
  `MetricsCollector.sm_compact_reuses`/`sm_compact_misses`, the
  `reused=<bool>` field on the EXISTING `compact` trace channel, and
  `benchmarks/bench_sm_compact_latency.py` — paraphrase from PLAN + add
  anything you learned wiring M3 that sharpens it, e.g. exactly where the
  reuse/miss decision is observable in `_force_compact`).
- relevant files (which src + test files M4 will likely touch — at least
  `metrics.py`, `trace.py`, `loop.py::_force_compact`, and the new
  `benchmarks/bench_sm_compact_latency.py`).
- expected tests (`tests/test_bench_sm_compact.py` + the metrics/trace
  extensions).
- risks: **the no-async divergence** (record it here explicitly so the
  final review folds it into Current Limitations) plus any surprise you hit
  wiring the warm/cold fallback or the cross-process persistence.

### 4. Commit everything in ONE commit with the required prefix

Never `git add -A`. Stage your implementation files **plus**
`initiatives/current/PROGRESS.md` and `initiatives/current/HANDOFF.md`
together. The commit subject MUST start with `[sm-dream/M3]`:

```
git -C python-replica add <implementation files> \
    initiatives/current/PROGRESS.md \
    initiatives/current/HANDOFF.md
git -C python-replica commit -m "[sm-dream/M3] <one-line summary>"
```

The shell loop's check 1 is `git log -1 | grep -qF "[sm-dream/M3]"`,
and check 2 requires HANDOFF.md to appear in **that same commit**. Both
checks fail if PROGRESS/HANDOFF land in a separate commit that lacks the
prefix. **Every commit you make during the exit ritual must carry the
`[sm-dream/M3]` prefix.**

**Do NOT `git commit --amend`** to embed this commit's own SHA into
PROGRESS.md. Instead, leave the SHA as `(see git log)` in the PROGRESS
block (step 2 above). If traceability demands the exact SHA, add a
*second* `[sm-dream/M3]` commit that fills it in — the script counts ALL
matching commits, so a follow-up does not break the exit gate. See
`obs-thr-harden` M3 → `4582997` for the canonical two-commit SHA-fill
pattern.

---

Do not skip any step. After your commit, the shell script independently
verifies:

1. The commit subject contains `[sm-dream/M3]`.
2. `initiatives/current/HANDOFF.md` was modified in your commit.
3. `initiatives/current/PROGRESS.md` contains a heading matching the
   regex `^## M3 — done YYYY-MM-DD` (anchored — a stray `M3` substring in
   a notes line does NOT satisfy this).
4. `pytest --tb=no -q` is still green.
5. `initiatives/current/HANDOFF.md` contains the required 5-section
   structure (verbatim headers: `## 1. Current initiative`, `## 2.
   Completed milestones`, `## 3. Current repo state`, `## 4. Important
   constraints`, `## 5. Next milestone guidance`). A free-form HANDOFF
   fails this check even if the file was modified.
6. **Append-only contract.** For every prior milestone `M{i}` your
   initiative has already shipped (every `[sm-dream/M{i}]` commit in
   `094cf90d09fef37b3f8357b4e2b8de0434834dfd..HEAD` — i.e. M1 and M2):
   - `initiatives/current/PROGRESS.md` still contains the
     `^## M{i} — done YYYY-MM-DD` block from that milestone;
   - `initiatives/current/HANDOFF.md` still contains the
     `^### M{i}$` subsection under "## 2. Completed milestones".
   Rewriting either file from scratch and only including your own `M3`
   block is a HARD FAILURE here — prior milestones are the source of truth
   on themselves; you APPEND only.

Any of those six failing halts the loop. The final review session will
also audit whether you stayed in scope and whether your HANDOFF accurately
describes your diff.
