# auto-memory-overhaul — M3: memory-system-prompt-wiring

You are an autonomous agent executing milestone **M3** of the
**auto-memory-overhaul** initiative. This session was launched by
`automation/scripts/run_all_milestones.sh`. There is no user available to
ask — every decision must be made and documented now.

When you are done, the shell loop will start the next milestone if (and
only if) you have honored the exit ritual in §5 below.

## §1 Baseline (verify before any code change)

- Last commit: the M2 commit (`[auto-mem/M2]`)
- pytest: 717+ passing (M1 +10, M2 +7 minimum)
- mypy: clean
- ruff: clean

Verify with:

```
git -C python-replica log --oneline -3
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

If the numbers do not match, **stop and report**.

## §2 Scope (what THIS milestone implements)

- **Phase IDs**: [B2]
- **Exit gate (must be objectively verifiable)**:
  `ContextBuilder._build_system_prompt()` inserts a `## Memory Management`
  teaching section before `## Memory` when project_memory is provided AND
  the section explains 4 types, what to save, what NOT to save, and points
  to the existing index AND CLI entrypoints (`simple-agent`,
  `simple-agent-openai`) construct ProjectMemory once and pass to both
  AgentLoop and the existing `/remember` REPL handler (single instance
  shared) AND a MockProvider e2e test proves a model-emitted
  `write_memory_entry` tool_use lands a `.md` file with correct frontmatter
  AND `pytest --tb=no -q` is green with at least 5 new tests.
- **Expected files to touch**:
  - `src/simple_coding_agent/context.py` (add `## Memory Management` section)
  - `src/simple_coding_agent/cli.py` (share ProjectMemory with REPL handler)
  - `src/simple_coding_agent/openai_cli.py` (share ProjectMemory with REPL handler)
  - `tests/test_loop_memory_prompt.py` (new)
  - `tests/test_loop_write_memory_e2e.py` (new)

### Implementation notes from PLAN

**Teaching section placement in `_build_system_prompt()`:**
- Current order (from CLAUDE.md P6): CLAUDE.md content prepended with
  `\n\n---\n\n` separator, then `## Memory` snippets block.
- M3 inserts `## Memory Management` between the CLAUDE.md section and
  the `## Memory` snippets block so the static teaching is upstream of
  the dynamic snippets and the cache prefix length stays stable per turn.
- The section is inserted **only when `project_memory is not None`** and
  `project_memory` is accessible to `ContextBuilder` (pass it in as a
  constructor arg or via the `build()` call — match the existing style).

**Teaching section text** (~250 tokens, fully static, English):
```
## Memory Management

You have access to a persistent memory system. Use the `write_memory_entry`
tool to save important information across conversations.

**Four memory types:**
- `user` — information about the user (role, preferences, expertise)
- `feedback` — guidance the user has given you (what to do / avoid)
- `project` — ongoing work context, goals, decisions, deadlines
- `reference` — pointers to external resources, docs, tickets

**What to save:** non-obvious facts, user preferences explicitly stated,
recurring patterns you notice, decisions with lasting impact.

**What NOT to save:** code snippets (use the codebase), git history (use
`git log`), ephemeral task details, anything already in CLAUDE.md,
speculative ideas the user hasn't confirmed.

**How to save:** use `write_memory_entry(type, id, name, description, body)`.
The `id` is a kebab-case slug (optionally `type/slug` for organization).
The `description` is a ≤150-char summary used in the memory index.

The existing memory index is shown in the `## Memory` section below.
```

**Single ProjectMemory instance shared between REPL and tool path:**
- In `cli.py` and `openai_cli.py`, construct `ProjectMemory(memory_dir)` once
  before creating `AgentLoop`.
- Pass the same instance to:
  1. `AgentLoop(..., project_memory=pm)` (M2 added this kwarg)
  2. The existing `/remember` REPL slash-command handler
- This means `/remember` writes and `write_memory_entry` writes go to the
  same store and MEMORY.md is consistent.
- The `/remember` handler behavior MUST NOT change — it still works exactly
  as before, just using the shared `pm` rather than constructing its own.

**E2E test in `test_loop_write_memory_e2e.py`:**
- Construct an AgentLoop with MockProvider and a real tmp_path ProjectMemory.
- Script MockProvider to return one turn: tool_use with `write_memory_entry`
  ({type="feedback", id="test-entry", name="test", description="desc", body="body"}).
- Then MockProvider returns a stop turn.
- Run the loop and assert:
  1. `(tmp_path / "test-entry.md")` exists.
  2. The `.md` file has correct frontmatter (name, type, description).
  3. `MEMORY.md` contains an index entry for `test-entry`.
  4. The REPL `/remember` command can still write to the same store (separate test).

Scope is **tight**. §2.5 below lists what you must NOT do.

## §2.5 Out of scope (do NOT do these, even if tempting)

- **Do not implement ExtractMemoriesRunner** (M4) or stop hooks (M5).
- **Do not add `call_selector`** (M6) or sideQuery injection (M7).
- **Do not change** the `## Memory` snippets section that already lists
  loaded memories — M3 only adds the static teaching section above it.
- **Do not refactor unrelated modules.** Touch only files in the Expected-files list.
- **Do not modify** `automation/scripts/run_all_milestones.sh`,
  `automation/RUNBOOK.md`, or `automation/templates/*.md`.
- **Do not modify** `CLAUDE.md` per-file summaries or `README.md`.
- **Do not change the behavior** of existing REPL slash commands other than
  ensuring `/remember` uses the shared `project_memory` instance.

## §3 Mandatory reading (in order, before any code change)

1. **`python-replica/CLAUDE.md`** — architecture, per-file summaries, execution rules.
2. **`python-replica/initiatives/current/PLAN.md`** — full initiative brief.
3. **`python-replica/initiatives/current/config.yaml`** — exit_gate for M3.
4. **`python-replica/initiatives/current/HANDOFF.md`** — Section 2 (M1+M2 shipped),
   Section 4 (invariants), Section 5 (guidance for you).
5. **`python-replica/initiatives/current/PROGRESS.md`** — M1+M2 fact logs.
6. **Source files to touch**:
   - `src/simple_coding_agent/context.py` — read fully; understand
     `_build_system_prompt()`, where it inserts `## Memory`, and how
     `project_memory` (if any) is currently threaded in.
   - `src/simple_coding_agent/cli.py` — read the REPL loop; find where
     `/remember` is handled; find where `AgentLoop` is constructed.
   - `src/simple_coding_agent/openai_cli.py` — same for the OpenAI variant.
7. Run `git -C python-replica log --oneline -10` and `git show HEAD~1`
   to see what M2 changed.
8. Skim `initiatives/current/logs/M2.log` for any anomalies that didn't
   make it into HANDOFF Section 2.

**Before invoking any Edit or Write tool**, print a **5-bullet summary**
of what you learned from PLAN / HANDOFF / the source files. Session log only.

## §4 Implementation requirements

Follow execution rules in `python-replica/CLAUDE.md`.

- **TDD**: write tests first (RED), implement (GREEN), refactor.
- **Test matrix (at least 5 new tests)**:
  - `test_memory_management_section_present` — system prompt contains
    `## Memory Management` when project_memory provided
  - `test_memory_management_section_absent_without_project_memory` — NOT
    present when project_memory is None
  - `test_memory_management_section_before_memory_snippets` — section
    appears before `## Memory` in the system prompt
  - `test_e2e_model_emitted_write_lands_on_disk` — MockProvider scripted
    tool_use → `.md` file + correct frontmatter
  - `test_remember_repl_uses_shared_project_memory` — `/remember` write
    visible to the AgentLoop's project_memory instance (same store)

- **Cache safety**: the teaching text must be byte-for-byte identical
  each turn (no f-string interpolation, no per-turn variables). It is a
  plain string constant — define it as a module-level constant in `context.py`.

After every meaningful diff:

```
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

## §5 Exit ritual (MANDATORY)

### 1. Confirm exit gate met (objectively)

Show command output proving:
- `## Memory Management` appears in the system prompt when `project_memory` is provided.
- E2E test showing `write_memory_entry` tool_use → `.md` file on disk.
- `pytest --tb=no -q` green.

### 2. Append a block to `initiatives/current/PROGRESS.md`

```
## M3 — done YYYY-MM-DD

- commit: [auto-mem/M3] (see git log)
- tests: <before> → <after> (+<delta>)
- mypy: clean | ruff: clean
- files changed: `context.py`, `cli.py`, `openai_cli.py`,
  `test_loop_memory_prompt.py`, `test_loop_write_memory_e2e.py`
- exit gate: system prompt has ## Memory Management AND e2e write lands on disk → PASS
- notes: <optional>
```

### 3. Rewrite `initiatives/current/HANDOFF.md`

Use the 5-section structure in `automation/templates/handoff_milestone.md`.

Section 2: Preserve `### M1` and `### M2` verbatim. Append `### M3`.
Section 4: Add any invariants for M4: e.g., "M4's ExtractMemoriesRunner must
  receive a snapshot of base_messages — not a live reference."
Section 5: Write for the M4 agent. M4 creates the pure `ExtractMemoriesRunner`
  class in a new `extract_memories.py` module with no AgentLoop coupling.

### 4. Commit with explicit paths

Stage your implementation files **plus** `initiatives/current/PROGRESS.md` and `initiatives/current/HANDOFF.md` together — every commit during the exit ritual must carry the `[auto-mem/M3]` prefix.

```
git -C python-replica add src/simple_coding_agent/context.py \
    src/simple_coding_agent/cli.py \
    src/simple_coding_agent/openai_cli.py \
    tests/test_loop_memory_prompt.py \
    tests/test_loop_write_memory_e2e.py \
    initiatives/current/PROGRESS.md \
    initiatives/current/HANDOFF.md
git -C python-replica commit -m "[auto-mem/M3] Memory Management teaching section + shared ProjectMemory"
```

---

Do not skip any step. The shell script verifies checks 1-6, including that
`### M1` and `### M2` subsections in HANDOFF Section 2 are preserved.
