# obs-thr-harden — M2: aggressive-thresholds-precedence-matrix-and-bug-fix

You are an autonomous agent executing milestone **M2** of the
**obs-thr-harden** initiative. This session was launched by
`automation/scripts/run_all_milestones.sh`. There is no user available to
ask — every decision must be made and documented now.

When you are done, the shell loop will start the next milestone if (and
only if) you have honored the exit ritual in §5 below.

## §1 Baseline (verify before any code change)

> M2 starts from M1's commit, not from the initiative baseline.

- Last commit: `<HEAD as of M2 start — the [obs-thr-hd/M1] commit>` —
  read from `git -C python-replica log --oneline -1`
- pytest: >= 565 passing (M1 left it at 565+)
- mypy: clean
- ruff: clean

Verify with:

```
git -C python-replica log --oneline -3
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

If pytest is below 565 or mypy/ruff are not clean, **stop and report**.
You are looking at a broken M1 commit and should not build on top of it.

## §2 Scope (what THIS milestone implements)

- **Phase IDs**: B1, B2, B3
- **Exit gate (must be objectively verifiable)**:
  `pytest --tb=no -q` is green with total >= 577 (after M1, 565, +12).
  `tests/test_repl.py` must contain a `@pytest.mark.parametrize` matrix
  covering the effective value of every field in
  `_AGGRESSIVE_THRESHOLDS` (all 8 keys) under three states:
    (i)   user passes no relevant flag, no --aggressive-thresholds
          -> expected: the `_DEFAULT_*` constant
    (ii)  user passes no relevant flag, --aggressive-thresholds set
          -> expected: `_AGGRESSIVE_THRESHOLDS[<field>]`
    (iii) user passes the relevant flag explicitly,
          --aggressive-thresholds also set
          -> expected: the user's explicit value (preset overridden)
  Field set: compact_threshold, keep_recent, microcompact_minutes,
    max_inline_chars, total_budget_chars, snip_keep_recent,
    context_tokens, reserved_output_tokens. Fields without a CLI flag
    backing (snip_keep_recent, max_inline_chars, etc.) only need
    states (i) and (ii).
  `MicroCompactor(threshold_minutes=0)` must raise `ValueError`
  mirroring `SnipTool(keep_recent=0)`; `tests/test_compact.py` gains
  one negative test.
  `tests/test_openai_cli_repl.py` must include at least one case
  verifying that `openai_cli`'s sentinel handling matches `cli`'s (so
  the two REPLs cannot drift apart).
  `mypy src` and `ruff check .` are clean.

- **Expected files to touch**:
  - `src/simple_coding_agent/cli.py` (sentinel handling in `_run_repl`)
  - `src/simple_coding_agent/openai_cli.py` (mirror the same logic in
    `_run_openai_repl`)
  - `src/simple_coding_agent/compact.py` (MicroCompactor +3-line guard)
  - `tests/test_repl.py` (precedence matrix)
  - `tests/test_openai_cli_repl.py` (symmetry test)
  - `tests/test_compact.py` (negative test for the guard)

### Implementation notes (from PLAN.md)

Touch list (ceiling): 3 src + 3 test files. Do NOT touch trace.py,
do NOT touch the 9-channel list, do NOT add or remove keys from
`_AGGRESSIVE_THRESHOLDS` (values may be tweaked but the key set is
frozen).

**Core bug fix (M2 MUST fix this — it is a real bug that was masked
by the way the M3 demo reads the preset dict directly)**:

Change the argparse defaults to a `None` sentinel:
```
--max-context-tokens: default=None (was 200_000)
--reserved-output-tokens: default=None (was 8_192)
--max-steps: default=None (if applicable — confirm during prompt)
```
At the top of `_run_repl`, unify a three-state decision:
```python
explicit_value = args.max_context_tokens  # or None
if explicit_value is not None:
    ctx_tokens = explicit_value
elif args.aggressive_thresholds:
    ctx_tokens = _AGGRESSIVE_THRESHOLDS["context_tokens"]
else:
    ctx_tokens = _DEFAULT_MAX_CONTEXT_TOKENS
```
Apply the same shape to the other two int flags. Add
`_DEFAULT_MAX_CONTEXT_TOKENS = 200_000`,
`_DEFAULT_RESERVED_OUTPUT_TOKENS = 8_192`, and
`_DEFAULT_MAX_STEPS = 50` (if applicable) at module level so the old
defaults are preserved as named constants.

**`--help` text gets one new line**:
> "If --aggressive-thresholds is also set and you do not pass this
> flag, the preset value applies; otherwise the built-in default
> applies."

This wording is pinned by M3's `--help` snapshot test. Use it verbatim
or coordinate the change.

**`openai_cli.py` must mirror this change** inside `_run_openai_repl`,
otherwise the two REPLs drift and the review session will catch it.
The M5 P9 record explicitly states "the slash command surface is
identical between MockProvider and live-provider REPLs" — sentinel
handling must preserve that symmetry.

**MicroCompactor guard**:
```python
if threshold_minutes < 1:
    raise ValueError(
        f"threshold_minutes must be >= 1, got {threshold_minutes}"
    )
```
Add one `pytest.raises(ValueError)` case to `tests/test_compact.py`.

**Precedence matrix assertion strategy**: construct a loop via
`cli._build_repl_loop` directly, then read internal attributes
(`loop._max_steps`, `loop._budget.max_tokens`,
`loop._budget.reserved_output_tokens`, the underlying
`_tool_result_store._max_inline_chars`,
`loop._snip_tool._keep_recent`, etc.) and assert the expected
effective value. The `_LAST_LOOPS` hook introduced in M1 of the prior
initiative (`cli.py:161`) supports this — reuse it.

**Test cases added (~12)**: precedence matrix 8-10 rows + microcompact
guard 1 + openai_cli symmetry 1-2.

**Regression scan checklist (MANDATORY before committing)**:

```
grep -rn "200_000\|8_192" /Users/leng/my-cc-py/python-replica/tests
```

Find existing tests that rely on the old argparse defaults and update
them (expect 2-4 hits). Any pytest failure from these MUST be fixed in
this same milestone — do not push regressions onto M3.

**Risk assessment**: 3 src + 3 test. Per-file diff is small (~10 lines
of sentinel logic in cli.py and openai_cli.py plus a module-level
constant; +3 guard lines in compact.py). No new protocol, no new
module, no `NullTracer.emit` change. Changing argparse defaults to
`None` is a user-visible behavioural shift, but `--help` text is
updated in sync and the change fixes a real bug (the preset never
being honoured).

pytest baseline: 565 -> >= 577 (+12 minimum).

Scope is **tight**. Doing more than this is failure. §2.5 below lists
what you must NOT do even if it seems natural.

## §2.5 Out of scope (do NOT do these, even if tempting)

- **Do not implement M3.** Even if the openai_cli symmetry test looks
  like a good place to also extend `_parse_trace_events`, stop at M2.
- **Do not touch `trace.py`** — M1 modified it; the M1 invariants in
  HANDOFF Section 4 carry forward.
- **Do not add or remove keys from `_AGGRESSIVE_THRESHOLDS`** — the
  8-key set is frozen. Values may be tweaked only if necessary, but
  prefer the existing values.
- **Do not change the 9-channel list** (`compact`, `reactive`,
  `microcompact`, `snip`, `externalize`, `memory_select`, `claude_md`,
  `auto_learn`, `budget`).
- **Do not change `NullTracer.emit` body** — it MUST remain literally
  `pass`. (Same invariant as M1.)
- **Do not modify** `automation/scripts/run_all_milestones.sh`,
  `automation/RUNBOOK.md`, or `automation/templates/*.md`.
- **Do not modify** `CLAUDE.md` per-file summaries or `README.md` —
  M3 / the final review session handles project-doc updates.
- **Do not introduce new dependencies** in `pyproject.toml`.
- **Do not change `_AGGRESSIVE_BANNER`** or `_format_aggressive_banner`
  unless the banner string itself needs to mention the bug fix (it
  doesn't — the banner is independent of which fields actually take
  effect).
- **Do not extend `_parse_trace_events`** in `visibility_full_demo.py`
  — that's M3's job, and it depends on M1's `_render_value` output
  format already in place.

If you find an obvious bug or improvement outside scope, **write it in
your HANDOFF.md Section 5 ("risks" or as a deferred item) and move on**.

## §3 Mandatory reading (in order, before any code change)

1. **`python-replica/CLAUDE.md`** — architecture, per-file summaries
   (especially the `cli.py`, `compact.py`, `snip.py` entries).
2. **`python-replica/initiatives/current/PLAN.md`** — this initiative's
   full brief.
3. **`python-replica/initiatives/current/config.yaml`** — confirms
   `exit_gate` + `phase_ids` for `M2`.
4. **`python-replica/initiatives/current/HANDOFF.md`** — Section 2
   `### M1` subsection tells you what M1 shipped. Section 4 lists
   invariants. Section 5 is written for you.
5. **`python-replica/initiatives/current/PROGRESS.md`** — terse fact
   log of M1 (commit, tests, files touched).
6. **The Expected-files list from §2** — read every source file this
   milestone will touch, plus their existing tests, before writing any
   code.
7. **Run** `git -C python-replica log --oneline -10` and
   `git -C python-replica show HEAD` — see what M1 changed. (`HEAD~1`
   is the prior milestone's commit because each milestone commits
   exactly once before the loop advances.)
8. **Skim** `initiatives/current/logs/M1.log` for any anomalies
   (warnings, retries, KeyboardInterrupts) that didn't make it into
   HANDOFF Section 2 design-decisions.

**Before invoking any Edit or Write tool**, print a **5-bullet summary**
to stdout of what you learned from PLAN / HANDOFF / PROGRESS / M1's
commit / the current `cli.py` `_run_repl` body. If those sources
disagree on a fact, treat HANDOFF as advisory and `git diff` + test
output as source of truth. This summary lives only in the captured
session log — its purpose is to force genuine reading. If you cannot
produce a substantive 5-bullet summary, you have not read enough; go
back to §3.

## §4 Implementation requirements

Follow execution rules documented in `python-replica/CLAUDE.md`. In
particular:

- **TDD**: write tests first (RED — must actually run and fail before
  implementation), implement (GREEN), refactor.
- **Immutability**: dataclasses prefer `frozen=True`; never mutate
  inputs; return new objects.
- **File limits**: functions ≤50 lines, files ≤800 lines.
- **Determinism in tests**: `MockProvider`, `tmp_path`, `monkeypatch`.
  No network. No real API key.
- **No swallowed exceptions**.
- **No `git add -A`** — stage explicit paths only.
- **Match style** of existing modules.

After every meaningful diff, run:

```
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

All three must be green before you commit.

## §5 Exit ritual (MANDATORY)

After your milestone work passes the exit gate in §2, before stopping,
perform these steps **in order**.

### 1. Confirm exit gate met (objectively)

Quote the concrete checks from §2 verbatim and show the command output
that proves them. Example:

```
$ cd python-replica && pytest --tb=no -q | tail -3
577 passed in 4.30s

$ cd python-replica && pytest tests/test_repl.py -v -k precedence | head -25
PASSED  tests/test_repl.py::test_precedence_matrix[compact_threshold-default]
PASSED  tests/test_repl.py::test_precedence_matrix[compact_threshold-preset]
PASSED  tests/test_repl.py::test_precedence_matrix[context_tokens-default]
PASSED  tests/test_repl.py::test_precedence_matrix[context_tokens-preset]
PASSED  tests/test_repl.py::test_precedence_matrix[context_tokens-explicit]
...

$ cd python-replica && pytest tests/test_compact.py::test_microcompactor_rejects_zero_minutes -v
PASSED

$ cd python-replica && pytest tests/test_openai_cli_repl.py -v -k sentinel
PASSED
```

"Implementation feels complete" is NOT acceptable. The gate must be
objectively verified by command output.

### 2. Commit with explicit paths

Never `git add -A`. The commit subject MUST start with
`[obs-thr-hd/M2]`:

```
git -C python-replica add src/simple_coding_agent/cli.py src/simple_coding_agent/openai_cli.py src/simple_coding_agent/compact.py tests/test_repl.py tests/test_openai_cli_repl.py tests/test_compact.py initiatives/current/PROGRESS.md initiatives/current/HANDOFF.md
git -C python-replica commit -m "[obs-thr-hd/M2] fix preset bug + 8-field precedence matrix + MicroCompactor guard"
```

If your regression scan in §2 found additional test files that needed
updating (`grep -rn "200_000\|8_192"`), include those too. Do NOT leave
broken tests for M3.

### 3. Append a block to `initiatives/current/PROGRESS.md`

Use the terse format. APPEND only; do NOT delete or modify M1's block.

```
## M2 — done 2026-05-23

- commit: <sha> [obs-thr-hd/M2] <subject>
- tests: <M1-ending count> -> <after> (+<delta>)
- mypy: clean | ruff: clean
- files changed: `src/simple_coding_agent/cli.py`,
  `src/simple_coding_agent/openai_cli.py`,
  `src/simple_coding_agent/compact.py`, `tests/test_repl.py`,
  `tests/test_openai_cli_repl.py`, `tests/test_compact.py`
- exit gate: <one-line paraphrase of §2 gate> -> PASS (<evidence>)
- notes: <optional, ≤1 line. e.g., "regression scan found N extra
  hits, updated in same commit.">
```

### 4. Rewrite `initiatives/current/HANDOFF.md`

Use the 5-section structure in `automation/templates/handoff_milestone.md`.
The structure is non-negotiable.

**Section 2 "Completed milestones"**: APPEND your own `### M2`
subsection. The `### M1` subsection from M1 MUST remain verbatim —
exit-gate check 6 enforces this.

**Section 4 "Important constraints"**: add entries that M3 must
respect. Specifically:
- "argparse defaults for `--max-context-tokens`,
  `--reserved-output-tokens`, `--max-steps` are now `None` sentinels.
  The user-visible `--help` text includes the wording `If
  --aggressive-thresholds is also set and you do not pass this flag,
  the preset value applies; otherwise the built-in default applies.`
  This exact phrase is pinned by M3's `--help` snapshot test."
- "`MicroCompactor.__init__` rejects `threshold_minutes < 1` with
  `ValueError`. Any future `MicroCompactor(threshold_minutes=...)`
  construction must respect this."

**Section 5 "Next milestone guidance"**: write for M3 (`demo-fences-
zero-overhead-and-doc-sync`). Include:
- next scope: extend `visibility_full_demo._new_run_dir` for
  same-second collision; extend `_parse_trace_events` to tolerate
  M1's repr-quoted values; add NullTracer timeit perf assertion;
  add `--help` snapshot test (must match the wording you pinned in §4).
- relevant files: `examples/visibility_full_demo.py`,
  `tests/test_visibility_full_demo.py`, `tests/test_trace.py` (perf
  assert), `tests/test_cli.py` (--help snapshot), `README.md`,
  `python-replica/CLAUDE.md`.
- expected tests: ~10 new cases across the test files listed.
- risks: M3 must NOT touch `cli.py` or `openai_cli.py`. If the
  `--help` snapshot test fails after M3 writes it, the snapshot is
  wrong (you, M2, pinned the wording); investigate before changing
  cli.py.

---

Do not skip any step. After your commit, the shell script independently
verifies:

1. The commit subject contains `[obs-thr-hd/M2]`.
2. `initiatives/current/HANDOFF.md` was modified in your commit.
3. `initiatives/current/PROGRESS.md` contains a heading matching the
   regex `^## M2 — done YYYY-MM-DD`.
4. `pytest --tb=no -q` is still green.
5. `initiatives/current/HANDOFF.md` contains the required 5-section
   structure.
6. **Append-only contract.** `initiatives/current/PROGRESS.md` still
   contains the `^## M1 — done YYYY-MM-DD` block; `HANDOFF.md` still
   contains the `^### M1$` subsection under Section 2. Rewriting either
   file from scratch with only your `M2` block is a HARD FAILURE.

Any of those six failing halts the loop. The final review session will
also audit whether you stayed in scope and whether your HANDOFF accurately
describes your diff.
