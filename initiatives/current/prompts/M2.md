# ctx-mgmt-demo — M2: capture-real-api-artifacts-for-3-scenarios

You are an autonomous agent executing milestone **M2** of the
**ctx-mgmt-demo** initiative. This session was launched by
`automation/scripts/run_all_milestones.sh`. There is no user available to
ask — every decision must be made and documented now.

When you are done, the shell loop will start the next milestone if (and
only if) you have honored the exit ritual in §5 below.

## §1 Baseline (verify before any code change)

- Last commit: `9ba662bf65e45d08949d4524203773a63bf36902` (initiative baseline; HEAD will be M1's commit when you run)
- pytest at initiative baseline: 816 passing
- mypy: clean | ruff: clean

Verify with:

```
git -C python-replica log --oneline -5
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

HEAD must be M1's `[ctx-demo/M1]` commit (the prior milestone in this
initiative). If pytest is not green or HEAD is not M1, **stop and report**.

## §2 Scope (what THIS milestone implements)

- **Phase IDs**: [F2]
- **Exit gate (must be objectively verifiable)**:
  `python-replica/demo/_artifacts/` contains three subdirectories
  (`01_tool_result_management/`, `02_full_compact/`, `03_microcompact/`),
  each with `transcript.txt`, `trace.stderr`, `metrics.json`,
  `stats_output.txt`. The `metrics.json` values satisfy:
  - `01` → `snip_invocations >= 1` AND `externalized_bytes > 0`
  - `02` → `full_compacts >= 1`
  - `03` → `microcompact_invocations >= 1`

  All three captures use the real DashScope API via the existing
  `python-replica/.env`. No `env.sample` created. No notebook `.md`
  files written yet (those land in M3). The model used for each
  scenario is recorded in that scenario's `stats_output.txt` header
  (`# model: <name>`).
- **Expected files to touch / create**:
  - `python-replica/demo/_scripts/` (new): the capture driver(s).
    Prefer a single generic driver over three near-duplicates.
  - `python-replica/demo/_artifacts/01_tool_result_management/`
    (new): the 4 artifacts.
  - `python-replica/demo/_artifacts/02_full_compact/` (new).
  - `python-replica/demo/_artifacts/03_microcompact/` (new).
  - **No `src/` changes.** No new tests. No `pyproject.toml`
    changes.

### Implementation notes from PLAN

**Pure side-effect milestone.** Run real API, write artifacts.

**Provider + credentials:**

`simple-agent-openai` consumes the existing `python-replica/.env`,
which is already populated with `DASHSCOPE_API_KEY`, `OPENAI_BASE_URL`
(DashScope compatible-mode), and `SIMPLE_AGENT_MODEL`. **DO NOT
create `env.sample`. DO NOT ask the user for credentials.**

**Model is swappable — quota-exhaustion playbook:**

The default model in `.env` may exhaust its quota mid-capture
(free-tier limits, daily caps, etc.). On any quota / rate-limit
error that is NOT recoverable by waiting < 60s:

1. **Stop the failing capture immediately** (do NOT retry the same
   model — wastes more spend).
2. Edit `python-replica/.env`, change `SIMPLE_AGENT_MODEL` to one of
   the supported alternatives below (all reachable through the same
   `OPENAI_BASE_URL`):
   - `qwen3-coder-plus-2025-09-23`
   - `glm-5`
   - `deepseek-v3.2`
   - `qwen-plus-latest`
   - `qwen-long-latest`
3. Restart that scenario's capture.
4. Record which model produced which artifact in that scenario's
   `stats_output.txt` header — first line is `# model: <name>`.
   Counter-level exit_gate assertions are model-agnostic — they pin
   behavior, not text.

If **two consecutive models** from the list also fail with quota
errors, **STOP** and surface this to the owner via HANDOFF Section 5
rather than cycling through all five.

**Capture pattern:**

Mirror `examples/visibility_full_demo.py` (SDK-based driver, **not
shell pipe** — REPL stdin scripting is fragile). Reuse its
`_parse_trace_events` helper and `_new_run_dir`
collision-avoidance pattern.

A single generic driver that takes per-scenario config is preferred
over three near-duplicates. Put it under `demo/_scripts/` (with a
leading underscore so it's clearly internal infrastructure, not a
demo itself).

**Per-scenario configs** (the agent picks the exact tool-call script
that triggers the named counters; these are guidance):

**01 (snip + externalize):**

- flags: `--repl --verbose --max-turns 6 --aggressive-thresholds`
- script: `read_file(A)`, `read_file(A)` repeat (→ snip),
  `read_file(big_B large enough to externalize under aggressive's
  max_inline_chars=2000)`, `/stats`, `/exit`

**02 (full compact):**

- flags: `--repl --verbose --max-turns 6 --aggressive-thresholds`
- script: accumulate enough tool_result tokens to cross the
  aggressive threshold (`max_context_tokens=4000 *
  compact_threshold=0.2 ≈ 800` tokens used). `/stats; /exit`

**03 (microcompact):**

- flags: `--repl --verbose --max-turns 4 --aggressive-thresholds
  --microcompact-minutes 0` (from M1)
- script: `read_file(A); any input on turn 2 to trigger microcompact;
  /stats; /exit`

**IMPORTANT — read M1's HANDOFF Section 4 first.** If M1 set the
`--microcompact-minutes` flag minimum to 1 (rather than relaxing the
existing guard), this scenario must use `--microcompact-minutes 1`
plus a brief `time.sleep(65)` between turn 1 and turn 2 of the
script. The HANDOFF tells you which path M1 took.

**Reactive compact and any memory mechanism are explicitly OUT.**

**Cost guardrail:** total < $0.20 across all 3 captures. If any
single capture exceeds $0.10, **STOP** and report rather than
retry-burning more API spend.

**Per-scenario artifact layout** under `demo/_artifacts/<scenario>/`:

- `transcript.txt` (full message history captured by driver)
- `trace.stderr` (raw `[trace]` event stream)
- `metrics.json` (`MetricsCollector` serialized)
- `stats_output.txt` (raw `/stats` stdout; **first line is `# model:
  <SIMPLE_AGENT_MODEL value>`**)

Commit the artifacts and the driver under `demo/`. They ARE the
demo evidence.

Scope is **tight**. Doing more than this is failure. §2.5 below lists
what you must NOT do even if it seems natural.

## §2.5 Out of scope (do NOT do these, even if tempting)

- **Do not implement M3.** Even if you "have time", stop at M2. M3
  is the notebook-writing milestone.
- **Do not write the demo notebooks** (`01_*.md`, `02_*.md`,
  `03_*.md`, `README.md`) — those are M3.
- **Do not modify any `src/` file.** M2 has zero source changes. If
  you find a bug in `simple-agent-openai` that blocks a capture,
  write it in HANDOFF Section 5 and STOP. Do not fix it here.
- **Do not add new pytest tests.** M2 has no new tests.
- **Do not modify** `automation/scripts/run_all_milestones.sh`,
  `automation/RUNBOOK.md`, or `automation/templates/*.md`.
- **Do not create `env.sample`.** The `.env` is the source of truth.
- **Do not commit the original `.env`** even if you edit it to swap
  models. `.env` is gitignored; verify by `git status` before
  committing. If you cannot see `.env` in `git status` after editing
  it, that is correct.
- **Do not demo reactive compact.** It is impractical against the
  real model window — point M3's README at `examples/stress_demo.py`
  instead.
- **Do not exceed the cost guardrail** (< $0.10 per scenario,
  < $0.20 total). Two consecutive quota failures = STOP.

If you find an obvious bug or improvement outside scope, write it in
HANDOFF.md Section 5 and move on.

## §3 Mandatory reading (in order, before any code change)

1. **`python-replica/CLAUDE.md`** — architecture, per-file summaries,
   project-wide execution rules. Pay special attention to
   `examples/visibility_full_demo.py` (this is your structural
   reference) and the `metrics.py` `MetricsCollector` shape.
2. **`python-replica/initiatives/current/PLAN.md`** — full brief
   (the model-quota playbook lives here too).
3. **`python-replica/initiatives/current/config.yaml`** — confirms
   M2's `exit_gate` and `phase_ids`.
4. **`python-replica/initiatives/current/HANDOFF.md`** — **read this
   carefully**. Section 2's `### M1` subsection tells you whether
   `--microcompact-minutes 0` works or you need `1 + sleep`.
   Section 4 lists any invariants M1 froze. Section 5 was written
   FOR you.
5. **`python-replica/initiatives/current/PROGRESS.md`** — terse fact
   log of M1.
6. **The Expected-files list from §2** (no src reads needed — M2 is
   side-effect only).
7. **`examples/visibility_full_demo.py`** — copy its driver
   structure: `_new_run_dir`, `_parse_trace_events`, the
   subprocess-and-tee pattern. Your `demo/_scripts/` driver mirrors
   this but parameterizes per-scenario flags and the tool-use
   script.
8. **`python-replica/.env`** — read it once to confirm
   `DASHSCOPE_API_KEY`, `OPENAI_BASE_URL`, and
   `SIMPLE_AGENT_MODEL` are populated. Do NOT print the API key in
   any log or capture.
9. **`git -C python-replica log --oneline -10`** — confirm HEAD is
   M1's `[ctx-demo/M1]` commit.

**Before invoking any Edit or Write tool**, print a **5-bullet
summary** to stdout of what you learned from PLAN / HANDOFF /
PROGRESS / M1's outcome / visibility_full_demo. Specifically address:
"Does `--microcompact-minutes 0` work, or does scenario 03 need
`1 + sleep`?"

## §4 Implementation requirements

This is a side-effect milestone, so most code-quality rules in
CLAUDE.md don't apply directly. Still:

- **Drivers under `demo/_scripts/`**: Python, ≤ 200 LOC each,
  ≤ 50 lines per function. Single generic driver preferred over
  three duplicates.
- **No swallowed exceptions** in the driver. If a capture fails,
  surface the error to stdout AND in `stats_output.txt`.
- **No real API key in logs / artifacts.** Verify by `grep -F
  "$(cut -d= -f2 .env | head -1)" demo/_artifacts/` returns nothing
  (or equivalent — adapt to actual key shape).
- **Artifact paths are committed** to git. The `demo/_artifacts/`
  directory must NOT be `.gitignore`d. (If it is for some reason,
  add a `!demo/_artifacts/` rule.)
- **No `git add -A`** — stage explicit paths.
- **`.env` edits are NOT committed.** If you swap models, do not
  stage `.env`.

After each capture, run:

```
cd python-replica && python -c "import json; m = json.load(open('demo/_artifacts/<scenario>/metrics.json')); print(m)"
```

to confirm the counter assertions before declaring the scenario
complete.

After all 3 captures, run:

```
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

All three must still be green (M2 doesn't change src/ but
verifies the codebase still passes).

## §5 Exit ritual (MANDATORY)

After your milestone work passes the exit gate in §2, before stopping,
perform these steps **in order**.

### 1. Confirm exit gate met (objectively)

Quote the concrete check from §2 verbatim and show the command
output that proves it. Example:

```
$ ls demo/_artifacts/
01_tool_result_management/  02_full_compact/  03_microcompact/

$ for d in demo/_artifacts/*/; do
    echo "=== $d ==="
    head -1 "$d/stats_output.txt"
    python -c "import json; m=json.load(open('$d/metrics.json')); print(m)"
  done

=== demo/_artifacts/01_tool_result_management/ ===
# model: qwen3.6-plus
{'full_compacts': 0, 'snip_invocations': 2, 'microcompact_invocations': 0, 'externalized_bytes': 12345, ...}
=== demo/_artifacts/02_full_compact/ ===
# model: qwen3.6-plus
{'full_compacts': 1, ...}
=== demo/_artifacts/03_microcompact/ ===
# model: qwen3.6-plus
{'microcompact_invocations': 1, ...}

$ pytest --tb=no -q
... 819 passed in 14s
```

"Captures feel correct" is NOT acceptable. The gate must be
objectively verified by command output, including the counter values.

### 2. Append a block to `initiatives/current/PROGRESS.md`

**Write this BEFORE committing.**

Block shape:

```
## M2 — done YYYY-MM-DD

- commit: [ctx-demo/M2] (see git log)
- tests: <baseline_post_M1> -> <same> (+0; M2 is side-effect)
- mypy: clean | ruff: clean
- files changed: `demo/_scripts/<driver>.py`, `demo/_artifacts/01_*/`, `demo/_artifacts/02_*/`, `demo/_artifacts/03_*/`
- exit gate: 3 artifact dirs each with 4 files, counters >= named thresholds -> PASS (see /stats outputs)
- notes: <optional, ≤1 line — e.g., "scenario 02 used qwen-plus-latest after default quota hit; rest used default">
```

### 3. Rewrite `initiatives/current/HANDOFF.md`

Use the 5-section structure in
`automation/templates/handoff_milestone.md`. The structure is
non-negotiable.

**Section 2 "Completed milestones"**: PRESERVE `### M1` verbatim.
APPEND a `### M2` subsection with files created (driver + 3
artifact dirs), the model used per scenario, behavior implemented
(per-scenario summary), design decisions (driver shape; whether any
scenario hit quota and swapped models), and known limitations.

**Section 4 "Important constraints"**: anything M3 must preserve.
E.g., "the model name in `stats_output.txt` header is the source of
truth M3 reads — do not paraphrase it in the notebook".

**Section 5 "Next milestone guidance"**: write FOR the M3 agent.
Include:

- next scope: README + 3 notebooks (`01_tool_result_management.md`,
  `02_full_compact.md`, `03_microcompact.md`) embedding ≥ 5 lines
  per scenario's captured artifacts + source-mapping references.
  Pure docs, no src/, no tests.
- relevant files: M3 reads `demo/_artifacts/*/` (your output) and
  writes `demo/{README,01_*,02_*,03_*}.md`.
- expected tests: none.
- risks: name the model used per scenario (the M3 notebook's Setup
  section MUST surface this from `stats_output.txt`); flag any
  scenario that used a non-default model so M3's README documents
  the actual model.

### 4. Commit everything in ONE commit with the required prefix

Stage the driver, the 3 artifact directories, PROGRESS.md, and
HANDOFF.md together:

```
git -C python-replica add \
    demo/_scripts/ \
    demo/_artifacts/ \
    initiatives/current/PROGRESS.md \
    initiatives/current/HANDOFF.md
git -C python-replica commit -m "[ctx-demo/M2] real-API artifact captures for snip+externalize, full compact, microcompact"
```

Confirm `.env` is NOT staged: `git -C python-replica status --short`
must not list `.env`.

**Do NOT `git commit --amend`** to embed the SHA into PROGRESS.md.

---

Do not skip any step. The shell script independently verifies the 6
exit-gate checks. The most likely failure mode for M2 is forgetting
to commit the artifacts (check 1 will fail) or rewriting M1's PROGRESS
block (check 6 will fail).
