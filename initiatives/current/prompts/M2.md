# observable-thresholds — M2: aggressive-thresholds-preset

You are an autonomous agent executing milestone **M2** of the
**observable-thresholds** initiative. This session was launched by
`automation/scripts/run_all_milestones.sh`. There is no user available to
ask — every decision must be made and documented now.

When you are done, the shell loop will start the next milestone if (and
only if) you have honored the exit ritual in §5 below.

## §1 Baseline (verify before any code change)

The numbers below are the initiative's Phase-1 baseline. M1 has shipped
by the time you run, so the *effective* baseline is M1's commit and
M1's pytest total — read those from `git log -1` and the most recent
`PROGRESS.md` block before starting.

- Initiative baseline commit: `2d414d91da65cc5998563e9c63b2d2be7028315d`
- Initiative baseline pytest: 520 passing
- M1 target pytest: >= 540 (M1 PROGRESS will show the real number)
- mypy: clean
- ruff: clean

Verify with:

```
git -C python-replica log --oneline -5
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

If pytest is not green or M1's commit is not visible in `git log`,
**stop and report**. Do not start work against an inconsistent
baseline.

## §2 Scope (what THIS milestone implements)

- **Phase IDs**: V3
- **Exit gate (must be objectively verifiable)**:
  `simple-agent --repl --verbose --aggressive-thresholds`
  running 8 turns that include repeated `read_file` calls
  produces at least one `[trace] [compact]` line and at least
  one `[trace] [snip]` line on stderr. Without
  `--aggressive-thresholds`, the same 8-turn input produces zero
  `[trace] [compact]` and zero `[trace] [snip]` lines. The REPL
  startup banner includes a single line summarizing the active
  preset, prefixed `[aggressive-thresholds]`. `pytest --tb=no -q`
  total >= 545. `mypy src` and `ruff check .` are clean.
- **Expected files to touch**:
  - `src/simple_coding_agent/snip.py` (extract `keep_recent` constructor parameter, default 3)
  - `src/simple_coding_agent/cli.py` (add module-level `_AGGRESSIVE_THRESHOLDS` dict, `--aggressive-thresholds` flag, banner emission, wire preset into `_build_repl_loop`)
  - `src/simple_coding_agent/openai_cli.py` (add `--aggressive-thresholds` flag, wire into `_run_openai_repl`)
  - `examples/aggressive_thresholds_demo.py` (new, ~80 LoC, MockProvider-based)
  - `tests/test_snip.py` (add 1-2 cases for `keep_recent=1`; existing cases must remain green at default)
  - `tests/test_repl.py` (add ~3 cases: flag wiring, banner emission, precedence rule)
  - `tests/test_openai_cli_repl.py` (add ~2 cases: flag wiring + banner)

Add a module-level dict `_AGGRESSIVE_THRESHOLDS` at the top of
`cli.py`:

```
compact_threshold: 0.2           (default 0.8)
keep_recent: 2                   (default 4)
microcompact_minutes: 1          (default 60)
max_inline_chars: 2_000          (default 50_000)
total_budget_chars: 8_000        (default 200_000)
snip_keep_recent: 1              (default 3)
context_tokens: 4_000            (default 200_000)
reserved_output_tokens: 512      (default 8_192)
```

Add `--aggressive-thresholds` flag (argparse `store_true`) to
both `cli.py` and `openai_cli.py`. When set, `_build_repl_loop`
passes these values into `ContextBudget`, `ContextCompactor`,
`MicroCompactor`, `ToolResultStore`, and `SnipTool` at
construction time.

Precedence rule: an explicit `--max-context-tokens` /
`--reserved-output-tokens` / `--max-steps` from the user
always overrides the preset value for that single field. Only
the unspecified fields take the aggressive value. Test this
explicitly.

`snip.py` needs a small refactor first: extract `keep_recent`
as a constructor parameter on `SnipTool` with default value 3
so existing behavior is unchanged. Internal algorithm uses
`self._keep_recent` instead of the current hard-coded
constants. Existing `tests/test_snip.py` cases must remain
green at default; add 1-2 new cases for `keep_recent=1`.

Banner format (printed once at REPL start when the preset is
active):

```
[aggressive-thresholds] compact=0.2, microcompact=1min,
  inline=2k, total=8k, snip_keep=1, ctx=4k, out=512
```

New example: `examples/aggressive_thresholds_demo.py` (~80
LoC) -- MockProvider-based, deterministic, demonstrates the
flag wiring without a network call.

New tests: ~5 cases across `tests/test_repl.py` and
`tests/test_openai_cli_repl.py` covering flag wiring,
banner emission, precedence rule (explicit flags override
preset), and `examples/aggressive_thresholds_demo.py`
end-to-end behavior.

Scope is **tight**. Doing more than this is failure. §2.5 below lists
what you must NOT do even if it seems natural.

## §2.5 Out of scope (do NOT do these, even if tempting)

- **Do not implement M3.** The `visibility_full_demo.py` script and
  the real-API artifact-saving pipeline belong to M3. Even if your
  preset already makes the demo trivial to write, stop at M2.
- **Do not modify M1's `trace.py` or `Tracer` Protocol** — that
  surface is frozen per M1's HANDOFF Section 4.
- **Do not refactor unrelated modules.** Touch only files in the
  Expected-files list (§2), plus tests for those files.
- **Do not modify** `automation/scripts/run_all_milestones.sh`,
  `automation/RUNBOOK.md`, or `automation/templates/*.md` — those are
  the harness, not the project.
- **Do not modify** `CLAUDE.md` per-file summaries or `README.md` beyond
  what is strictly required by the new flag's `--help` text. The
  review session at the end of the initiative handles project-doc
  updates (per RUNBOOK Doc-update tiers).
- **Do not expose snip configuration beyond `keep_recent`** as CLI
  flags — explicitly deferred per PLAN.md "Out of scope".
- **Do not change the default `SnipTool` behavior.** `keep_recent=3`
  must be byte-equivalent to the current hard-coded constants;
  `tests/test_snip.py` is the verification surface.
- **Do not change the trace channel names** introduced in M1
  (`compact`, `reactive`, `microcompact`, `snip`, `externalize`,
  `memory_select`, `claude_md`, `auto_learn`, `budget`).
- **Do not introduce new dependencies** in `pyproject.toml`.
- **Do not delete or rewrite M1's `### M1` HANDOFF subsection or
  `## M1 — done` PROGRESS block.** Append-only contract enforced by
  exit-gate check 6.

If you find an obvious bug or improvement outside scope, **write it in
your HANDOFF.md Section 5 ("risks" or as a deferred item) and move on**.
Do not fix it.

## §3 Mandatory reading (in order, before any code change)

1. **`python-replica/CLAUDE.md`** — architecture, per-file summaries,
   project-wide execution rules (TDD, immutability, file-size limits).
2. **`python-replica/initiatives/current/PLAN.md`** — this initiative's
   full brief, including the milestone tables.
3. **`python-replica/initiatives/current/config.yaml`** — machine-readable
   milestone table; confirms `exit_gate` + `phase_ids` for `M2`.
4. **`python-replica/initiatives/current/HANDOFF.md`** — current state.
   Section 2 "Completed milestones" tells you what shipped in M1.
   Section 4 "Important constraints" lists invariants you MUST respect
   (in particular M1's secret-leak and constructor-default rules).
   Section 5 "Next milestone guidance" is written FOR you.
5. **`python-replica/initiatives/current/PROGRESS.md`** — terse fact log
   of M1's commit, test delta, files touched.
6. **The Expected-files list from §2** — read the source files this
   milestone will touch, plus their existing tests, before writing any
   code.
7. Run `git -C python-replica log --oneline -10` and
   `git -C python-replica show HEAD~1` to see what M1 changed.
   `HEAD~1` is M1's commit because each milestone commits exactly once
   before the loop advances; if you suspect that invariant has been
   violated, use `git -C python-replica log
   2d414d91da65cc5998563e9c63b2d2be7028315d..HEAD --oneline | grep -F
   "[obs-thr/" | head -1` instead and `git -C python-replica show
   <sha>` to inspect that SHA. The `-C python-replica` is required
   because the autonomous loop launches `claude --print` from
   `/Users/leng/my-cc-py`, not from inside the repo. The `grep -F` is
   required because the literal `[` in the marker would otherwise be
   parsed as an unclosed character class; `-F` forces fixed-string
   matching.

**Before invoking any Edit or Write tool**, print a **5-bullet summary**
to stdout (plain text, not a tool call, not a TaskCreate todo, not a
file write) of what you learned from PLAN / HANDOFF / PROGRESS / M1's
commit. If those sources disagree on a fact, treat HANDOFF as advisory
and `git diff` + test output as source of truth. This summary lives
only in the captured session log under `initiatives/current/logs/`
(not in any committed file) — its purpose is to force genuine reading
rather than skimming. If you cannot produce a substantive 5-bullet
summary, you have not read enough; go back to §3.

## §4 Implementation requirements

Follow execution rules documented in `python-replica/CLAUDE.md`. In
particular:

- **TDD**: write tests first (RED — must actually run and fail before
  implementation), implement (GREEN), refactor. Every new code path
  needs a test before the implementation lands.
- **Immutability**: dataclasses prefer `frozen=True` where appropriate;
  never mutate inputs; return new objects.
- **File limits**: functions ≤50 lines, files ≤800 lines.
- **Determinism in tests**: `MockProvider`, `tmp_path`, `monkeypatch`,
  `capsys`/`capfd` for stderr capture. No network. No real API key.
- **No swallowed exceptions**.
- **No `git add -A`** — stage explicit paths only.
- **Match style** of existing modules (see CLAUDE.md per-file summaries).

Additional M2-specific requirements:

- **SnipTool refactor first.** Land the `keep_recent: int = 3`
  constructor parameter in a separate test-first cycle, verify existing
  `tests/test_snip.py` is still green, then add the new `keep_recent=1`
  case before the rest of M2.
- **Precedence rule has its own test.** Construct a parser with
  `--aggressive-thresholds --max-context-tokens 16000`; assert that
  `ContextBudget.max_total_tokens == 16000` (explicit override wins)
  while `_build_repl_loop` still applies aggressive values for
  `keep_recent`, `compact_threshold`, etc.
- **Banner is emitted exactly once** per REPL start when the preset is
  active, on stdout (not stderr). The line MUST start with the literal
  prefix `[aggressive-thresholds]`. Test for the prefix, not the exact
  line — wording may be refined.
- **`examples/aggressive_thresholds_demo.py`** is a MockProvider demo,
  no network calls, no API key required. It demonstrates the flag
  wiring and prints the banner; it does NOT need to actually trigger
  a full-compact (that's M3's job with the real-API demo).

After every meaningful diff, run:

```
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

All three must be green before you commit.

## §5 Exit ritual (MANDATORY)

After your milestone work passes the exit gate in §2, before stopping,
perform these steps **in order**. The shell script independently
verifies the **5 side-effects** listed at the end of this section.
Step 1 below ("Confirm exit gate met") is **honor-system** — the script
cannot inspect whether you actually quoted the right command output, so
the 5 mechanical checks are produced by steps 2-4 (commit, PROGRESS
append, HANDOFF rewrite) plus the standing pytest-green requirement
from §4. Skipping step 1 makes a green run meaningless even though it
would still pass the gate; do not skip it.

### 1. Confirm exit gate met (objectively)

Quote the concrete check from §2 verbatim and show the command output
that proves it. Example:

```
$ printf '<8 turns including repeated read_file>\n/exit\n' \
    | python -m simple_coding_agent.cli --repl --verbose --aggressive-thresholds 2>/tmp/aggressive.stderr
$ grep -c '^\[trace\] \[compact\]' /tmp/aggressive.stderr
1
$ grep -c '^\[trace\] \[snip\]' /tmp/aggressive.stderr
2

$ printf '<same 8 turns>\n/exit\n' \
    | python -m simple_coding_agent.cli --repl --verbose 2>/tmp/baseline.stderr
$ grep -c '^\[trace\] \[compact\]' /tmp/baseline.stderr
0
$ grep -c '^\[trace\] \[snip\]' /tmp/baseline.stderr
0

$ python -m simple_coding_agent.cli --repl --aggressive-thresholds < /dev/null | head -1
[aggressive-thresholds] compact=0.2, microcompact=1min, inline=2k, total=8k, snip_keep=1, ctx=4k, out=512

$ pytest --tb=no -q
... 545 passed
$ mypy src/
Success: no issues found
$ ruff check .
All checks passed!
```

"Implementation feels complete" is NOT acceptable. The gate must be
objectively verified by a command's output.

### 2. Commit with explicit paths

Never `git add -A`. The commit subject MUST start with `[obs-thr/M2]`:

```
git -C python-replica add <list each modified/new file>
git -C python-replica commit -m "[obs-thr/M2] <one-line summary>"
```

The shell loop's first exit-gate check is
`git log -1 | grep -qF "[obs-thr/M2]"`. Without this commit the loop
halts and subsequent milestones will NOT run.

### 3. Append a block to `initiatives/current/PROGRESS.md`

Use the terse format in `automation/templates/progress_entry.md`. One
block per milestone. PROGRESS.md is the **fact log for the final
review** — narrative belongs in HANDOFF, not here.

Block shape:

```
## M2 — done 2026-05-22

- commit: <sha> [obs-thr/M2] <subject>
- tests: <M1's count> -> <after> (+N)
- mypy: clean | ruff: clean
- files changed: `<file1>`, `<file2>`, ...
- exit gate: <quote from §2> -> PASS (<one-line evidence>)
- notes: <optional, ≤1 line>
```

Replace `2026-05-22` with `$(date -u +%Y-%m-%d)` at the moment of
commit if the loop spans midnight. The `## M1 — done ...` block above
yours MUST remain verbatim — append-only contract enforced by
exit-gate check 6.

### 4. Rewrite `initiatives/current/HANDOFF.md`

Use the 5-section structure in `automation/templates/handoff_milestone.md`.
The structure is non-negotiable.

**Section 2 "Completed milestones"**: PRESERVE M1's `### M1` subsection
verbatim (do NOT edit or delete it) and APPEND your own `### M2`
subsection below it.

**Section 4 "Important constraints"**: PRESERVE M1's constraint entries
verbatim. APPEND any new invariants you introduced — at minimum, the
`_AGGRESSIVE_THRESHOLDS` dict surface ("M3's `visibility_full_demo.py`
imports these values; the key set is frozen") and the SnipTool
constructor signature ("`keep_recent: int = 3` default — M3 must NOT
change this default").

**Section 5 "Next milestone guidance"**: write as if onboarding M3 in
person. Include:
- next scope (paraphrase M3 from PLAN + anything you learned in M2 that
  sharpens it — e.g. which channels actually fire under aggressive
  thresholds, so M3's `summary.md` row generation is deterministic)
- relevant files (`examples/visibility_full_demo.py` new file,
  `.gitignore` update, `tests/test_visibility_full_demo.py` new test
  file, README.md "Examples" section)
- expected tests (~6 cases in `tests/test_visibility_full_demo.py`
  including the `_ExplodingProvider` key-less path)
- risks (real-API cost gating via `--confirm-api-call`, the artifact
  directory MUST be in `.gitignore` in the same M3 commit, exit codes
  2 and 3 must be distinct)

---

Do not skip any step. After your commit, the shell script independently
verifies:

1. The commit subject contains `[obs-thr/M2]`.
2. `initiatives/current/HANDOFF.md` was modified in your commit.
3. `initiatives/current/PROGRESS.md` contains a heading matching the
   regex `^## M2 — done YYYY-MM-DD` (anchored — a stray `M2` substring
   in a notes line does NOT satisfy this).
4. `pytest --tb=no -q` is still green.
5. `initiatives/current/HANDOFF.md` contains the required 5-section
   structure (verbatim headers: `## 1. Current initiative`, `## 2.
   Completed milestones`, `## 3. Current repo state`, `## 4. Important
   constraints`, `## 5. Next milestone guidance`). A free-form HANDOFF
   fails this check even if the file was modified.
6. **Append-only contract.** M1's `[obs-thr/M1]` commit is in
   `baseline_commit..HEAD`, so:
   - `initiatives/current/PROGRESS.md` still contains the
     `^## M1 — done YYYY-MM-DD` block from M1;
   - `initiatives/current/HANDOFF.md` still contains the `^### M1$`
     subsection under "## 2. Completed milestones".
   Rewriting either file from scratch and only including your own `M2`
   block is a HARD FAILURE here — M1 is the source of truth on itself;
   you APPEND only.

Any of those six failing halts the loop. The final review session will
also audit whether you stayed in scope and whether your HANDOFF accurately
describes your diff.
