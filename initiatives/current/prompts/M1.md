# obs-thr-harden — M1: trace-robustness-and-leak-coverage

You are an autonomous agent executing milestone **M1** of the
**obs-thr-harden** initiative. This session was launched by
`automation/scripts/run_all_milestones.sh`. There is no user available to
ask — every decision must be made and documented now.

When you are done, the shell loop will start the next milestone if (and
only if) you have honored the exit ritual in §5 below.

## §1 Baseline (verify before any code change)

- Last commit: `e8e2206c2fc6737f509229b2414bb578dc4d99e1`
- pytest: 557 passing
- mypy: clean
- ruff: clean

Verify with:

```
git -C python-replica log --oneline -3
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

If the numbers do not match, **stop and report**. Do not start work
against a different baseline.

## §2 Scope (what THIS milestone implements)

- **Phase IDs**: A1, A2
- **Exit gate (must be objectively verifiable)**:
  `pytest --tb=no -q` is green with total >= 565 (baseline 557, +8).
  `tests/test_trace.py` must include the following new coverage:
  (a) >= 3 parametrize cases covering different secret shapes (Bearer
      token, AWS access key, long OpenAI key, Unicode username /
      Chinese-character secret string). None of the secret substrings
      may appear in captured stderr after the value passes through the
      REPL.
  (b) one stream-closed negative test: construct an already-closed
      `io.StringIO`, call `StderrTracer(stream=closed).emit("budget",
      ...)` and assert no `ValueError` / `OSError` propagates and the
      agent does not crash.
  (c) one reactive end-to-end test: a MockProvider whose first call
      raises `PromptTooLongError` and whose second call succeeds; run a
      one-turn CLI REPL session and assert captured stderr contains at
      least one `[trace] [reactive]` line.
  (d) one value-with-whitespace test: emit a field whose value contains
      whitespace (for example `tracer.emit("budget", payload={"a": 1,
      "b": 2})`); the emitted line, when parsed by
      `examples/visibility_full_demo._parse_trace_events`, must not
      break adjacent fields.
  `mypy src` and `ruff check .` are clean.

- **Expected files to touch**:
  - `src/simple_coding_agent/trace.py` (the only source file edited)
  - `tests/test_trace.py` (all new tests land here)
  - Read-only references (do NOT edit in M1):
    `tests/test_stress_full_compact.py` (existing MockProvider +
    `PromptTooLongError` fixture pattern to reuse),
    `examples/visibility_full_demo.py` (read `_parse_trace_events` so
    the whitespace-roundtrip test correctly verifies the contract M3
    will extend).

### Implementation notes (from PLAN.md)

Touch list (ceiling):
  src: src/simple_coding_agent/trace.py (core change)
  test: tests/test_trace.py
Do NOT touch cli.py / openai_cli.py / any of the other 9 fire-site
components, do NOT change the `Tracer` Protocol signature, do NOT
change `NullTracer.emit` body (it must remain literally `pass`).

**Change 1**: wrap `StderrTracer.emit`'s `self._stream.write(line)` in
  `try: ... except (OSError, ValueError): return`
Catch only `OSError` / `ValueError` (covers "I/O on closed file" and
broken-pipe). Do NOT catch `KeyboardInterrupt` / `SystemExit` or
`Exception`. `flush` is already defensive — leave it alone.

**Change 2**: add a module-level helper `_render_value(value)`:
  - bool / int / float / None  -> `str(value)`
  - str without whitespace     -> `value`
  - str with whitespace, OR any other type -> `repr(value)` (quoted)
Replace the bare `fields[key]` read in `emit` with this helper.
Keep the line ASCII so `visibility_full_demo._parse_trace_events`
keeps working; M3 will extend that parser to tolerate the quoted form.

**Reactive end-to-end fixture**: a MockProvider subclass whose Nth
call raises `PromptTooLongError` and others return normally. The
pattern already exists in `tests/test_stress_full_compact.py` —
reuse rather than reinvent.

**Test cases added (~8)**: 4-shape secret-leak parametrize counts as
one parametrize block + closed-stream + reactive-e2e + whitespace-
roundtrip + non-string value (dict / list / None) roundtrip + a
9-channel format regression + `_render_value` unit tests + bool/int
edge cases.

**Risk assessment**: touches 1 src + 1 test only. Single-file
deep-hardening. No cross-component wiring, no new Protocol, no CLI
surface change. Every red line from the M1-243-turn lesson
(>6 src / >4 components changing protocol / >15 tests / the
"abstraction + wire + CLI" triple-combo) is far below threshold.

pytest baseline: 557 -> >= 565 (+8 minimum).

Scope is **tight**. Doing more than this is failure. §2.5 below lists
what you must NOT do even if it seems natural.

## §2.5 Out of scope (do NOT do these, even if tempting)

- **Do not implement any other milestone** in this initiative. Even if
  you "have time" or "see the obvious next step", stop at `M1`.
- **Do not refactor unrelated modules.** Touch only `trace.py` and
  `tests/test_trace.py`. Read-only files listed in §2 are
  READ-ONLY — do not edit them.
- **Do not modify** `automation/scripts/run_all_milestones.sh`,
  `automation/RUNBOOK.md`, or `automation/templates/*.md`.
- **Do not modify** `CLAUDE.md` per-file summaries or `README.md` —
  M3 / the final review session handles project-doc updates per
  RUNBOOK Doc-update tiers.
- **Do not change the `Tracer` Protocol signature** — backward
  compatible additions only.
- **Do not change `NullTracer.emit` body** — it MUST remain literally
  `pass`. Adding any operation here regresses the zero-overhead promise
  that M3 will assert via timeit.
- **Do not add or remove keys from `_AGGRESSIVE_THRESHOLDS`** — that
  dict is defined in `cli.py` and outside M1's scope anyway, but the
  invariant carries forward through the whole initiative.
- **Do not change the 9-channel list** (`compact`, `reactive`,
  `microcompact`, `snip`, `externalize`, `memory_select`, `claude_md`,
  `auto_learn`, `budget`).
- **Do not introduce new dependencies** in `pyproject.toml`.
- **Do not catch broad exceptions** in the new closed-stream guard.
  Catch only `(OSError, ValueError)`; `KeyboardInterrupt` / `SystemExit`
  / `Exception` must propagate.

If you find an obvious bug or improvement outside scope, **write it in
your HANDOFF.md Section 5 ("risks" or as a deferred item) and move on**.
Do not fix it.

## §3 Mandatory reading (in order, before any code change)

1. **`python-replica/CLAUDE.md`** — architecture, per-file summaries
   (especially the `trace.py` entry), project-wide execution rules.
2. **`python-replica/initiatives/current/PLAN.md`** — this initiative's
   full brief.
3. **`python-replica/initiatives/current/config.yaml`** — confirms
   `exit_gate` + `phase_ids` for `M1`.
4. **`python-replica/initiatives/current/HANDOFF.md`** — Section 4
   "Important constraints" lists initiative-level invariants you MUST
   respect. Section 5 "Next milestone guidance" is written for you.
5. **`python-replica/initiatives/current/PROGRESS.md`** — empty (M1 is
   the first milestone), but confirm the shape.
6. **`python-replica/src/simple_coding_agent/trace.py`** — the entire
   current file (~87 lines). Pay special attention to lines 69-83
   (`StderrTracer.emit`).
7. **`python-replica/tests/test_trace.py`** — every existing test, so
   you know what regression coverage already exists and what shapes the
   new parametrize blocks should follow.
8. **`python-replica/tests/test_stress_full_compact.py`** — read-only
   reference for the existing MockProvider + `PromptTooLongError`
   reactive-fixture pattern.
9. **`python-replica/examples/visibility_full_demo.py`** — read-only;
   understand `_parse_trace_events` so the M1 whitespace-roundtrip test
   correctly verifies the parser-contract M3 will extend.

**Before invoking any Edit or Write tool**, print a **5-bullet summary**
to stdout (plain text, not a tool call, not a TaskCreate todo, not a
file write) of what you learned from PLAN / HANDOFF / PROGRESS /
trace.py / test_trace.py. If those sources disagree on a fact, treat
HANDOFF as advisory and `git diff` + test output as source of truth.
This summary lives only in the captured session log under
`initiatives/current/logs/` (not in any committed file) — its purpose is
to force genuine reading rather than skimming. If you cannot produce a
substantive 5-bullet summary, you have not read enough; go back to §3.

## §4 Implementation requirements

Follow execution rules documented in `python-replica/CLAUDE.md`. In
particular:

- **TDD**: write tests first (RED — must actually run and fail before
  implementation), implement (GREEN), refactor. Every new code path
  needs a test before the implementation lands.
- **Immutability**: dataclasses prefer `frozen=True` where appropriate;
  never mutate inputs; return new objects.
- **File limits**: functions ≤50 lines, files ≤800 lines.
- **Determinism in tests**: `MockProvider`, `tmp_path`, `monkeypatch`.
  No network. No real API key.
- **No swallowed exceptions** outside the explicit `(OSError, ValueError)`
  guard documented in §2 Change 1.
- **No `git add -A`** — stage explicit paths only.
- **Match style** of existing modules (see CLAUDE.md per-file summaries).

After every meaningful diff, run:

```
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

All three must be green before you commit.

## §5 Exit ritual (MANDATORY)

After your milestone work passes the exit gate in §2, before stopping,
perform these steps **in order**. The shell script independently
verifies the **5 side-effects** listed at the end of this section.
Step 1 below ("Confirm exit gate met") is **honor-system** — the script
cannot inspect whether you actually quoted the right command output, so
the 5 mechanical checks are produced by steps 2-4 (commit, PROGRESS
append, HANDOFF rewrite) plus the standing pytest-green requirement
from §4. Skipping step 1 makes a green run meaningless even though it
would still pass the gate; do not skip it.

### 1. Confirm exit gate met (objectively)

Quote the concrete checks from §2 verbatim and show the command output
that proves them. Example:

```
$ cd python-replica && pytest --tb=no -q | tail -3
565 passed in 4.12s

$ cd python-replica && pytest tests/test_trace.py -v | grep -E "test_(secret_leak|stream_closed|reactive_channel|whitespace_roundtrip)"
PASSED  tests/test_trace.py::test_secret_leak_negative[bearer-token]
PASSED  tests/test_trace.py::test_secret_leak_negative[aws-key]
PASSED  tests/test_trace.py::test_secret_leak_negative[openai-key]
PASSED  tests/test_trace.py::test_secret_leak_negative[unicode-secret]
PASSED  tests/test_trace.py::test_stream_closed_does_not_propagate
PASSED  tests/test_trace.py::test_reactive_channel_end_to_end
PASSED  tests/test_trace.py::test_whitespace_value_roundtrip_through_demo_parser
```

"Implementation feels complete" is NOT acceptable. The gate must be
objectively verified by command output.

### 2. Commit with explicit paths

Never `git add -A`. The commit subject MUST start with
`[obs-thr-hd/M1]`:

```
git -C python-replica add src/simple_coding_agent/trace.py tests/test_trace.py initiatives/current/PROGRESS.md initiatives/current/HANDOFF.md
git -C python-replica commit -m "[obs-thr-hd/M1] harden StderrTracer + expand leak/roundtrip coverage"
```

The shell loop's first exit-gate check is `git log -1 | grep -qF
"[obs-thr-hd/M1]"`. Without this commit the loop halts and subsequent
milestones will NOT run.

### 3. Append a block to `initiatives/current/PROGRESS.md`

Use the terse format in `automation/templates/progress_entry.md`. One
block per milestone. PROGRESS.md is the **fact log for the final
review** — narrative belongs in HANDOFF, not here.

Block shape:

```
## M1 — done 2026-05-23

- commit: <sha> [obs-thr-hd/M1] <subject>
- tests: 557 -> <after> (+<delta>)
- mypy: clean | ruff: clean
- files changed: `src/simple_coding_agent/trace.py`, `tests/test_trace.py`
- exit gate: <one-line paraphrase of §2 gate> -> PASS (<evidence>)
- notes: <optional, ≤1 line>
```

### 4. Rewrite `initiatives/current/HANDOFF.md`

Use the 5-section structure in `automation/templates/handoff_milestone.md`.
The structure is non-negotiable.

**Section 2 "Completed milestones"**: APPEND your own `### M1`
subsection. Do NOT delete or rewrite prior milestone blocks (there are
none yet — but the rule applies from M1's commit onward).

**Section 4 "Important constraints"**: add entries that M2 and M3 must
respect. Specifically:
- "`StderrTracer.emit` MUST keep the `(OSError, ValueError)` guard on
  the `write` call. Removing it would re-introduce the closed-stream
  crash that M1 fixed."
- "`_render_value` helper signature in `trace.py` is now part of the
  internal interface that the M3 demo-parser tolerance work depends
  on. Do not rename or change semantics in M2/M3 without coordinating."

**Section 5 "Next milestone guidance"**: write for M2 (`aggressive-
thresholds-precedence-matrix-and-bug-fix`). Include:
- next scope (paraphrase from PLAN + anything you learned in M1)
- relevant files: `src/simple_coding_agent/cli.py`,
  `src/simple_coding_agent/openai_cli.py`,
  `src/simple_coding_agent/compact.py`, plus the three test files.
- expected tests: precedence matrix in `test_repl.py`, microcompact
  guard in `test_compact.py`, openai_cli symmetry in
  `test_openai_cli_repl.py`.
- risks: M1 may have updated tests in `test_trace.py` that use field
  names overlapping with M2's precedence-matrix work — call out any
  such overlap.

---

Do not skip any step. After your commit, the shell script independently
verifies:

1. The commit subject contains `[obs-thr-hd/M1]`.
2. `initiatives/current/HANDOFF.md` was modified in your commit.
3. `initiatives/current/PROGRESS.md` contains a heading matching the
   regex `^## M1 — done YYYY-MM-DD` (anchored — a stray `M1` substring
   in a notes line does NOT satisfy this).
4. `pytest --tb=no -q` is still green.
5. `initiatives/current/HANDOFF.md` contains the required 5-section
   structure (verbatim headers: `## 1. Current initiative`, `## 2.
   Completed milestones`, `## 3. Current repo state`, `## 4. Important
   constraints`, `## 5. Next milestone guidance`). A free-form HANDOFF
   fails this check even if the file was modified.
6. **Append-only contract.** M1 is the first milestone, so no prior
   blocks need preserving. The rule applies from M1's commit onward
   for M2/M3.

Any of those six failing halts the loop. The final review session will
also audit whether you stayed in scope and whether your HANDOFF accurately
describes your diff.
