# observable-thresholds — M3: real-api-visibility-demo-and-guard

You are an autonomous agent executing milestone **M3** of the
**observable-thresholds** initiative. **This is the LAST milestone.**
This session was launched by
`automation/scripts/run_all_milestones.sh`. There is no user available to
ask — every decision must be made and documented now.

When you are done, the shell loop will start the review/wrap-up session
if (and only if) you have honored the exit ritual in §5 below. There is
no "next milestone agent" to read your HANDOFF — but the review session
WILL audit it.

## §1 Baseline (verify before any code change)

The numbers below are the initiative's Phase-1 baseline. M1 and M2 have
shipped by the time you run, so the *effective* baseline is M2's commit
and M2's pytest total — read those from `git log -1` and the most
recent `PROGRESS.md` block before starting.

- Initiative baseline commit: `2d414d91da65cc5998563e9c63b2d2be7028315d`
- Initiative baseline pytest: 520 passing
- M2 target pytest: >= 545 (M2 PROGRESS will show the real number)
- mypy: clean
- ruff: clean

Verify with:

```
git -C python-replica log --oneline -5
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

If pytest is not green or M1's and M2's commits are not visible in
`git log`, **stop and report**. Do not start work against an
inconsistent baseline.

## §2 Scope (what THIS milestone implements)

- **Phase IDs**: V4, V5
- **Exit gate (must be objectively verifiable)**:
  Running `python examples/visibility_full_demo.py
  --confirm-api-call` with `OPENAI_API_KEY` or
  `DASHSCOPE_API_KEY` set produces a directory
  `examples/_artifacts/visibility-demo-YYYYMMDD-HHMMSS/`
  containing four non-empty files: `transcript.txt`,
  `trace.stderr`, `metrics.json`, `summary.md`. Without
  `--confirm-api-call`, the script exits with code 2 and prints
  an explanatory message. With `--confirm-api-call` but no API
  key in env, the script exits with code 3 and prints an
  explanatory message. `tests/test_visibility_full_demo.py`
  (key-less path, using the `_ExplodingProvider` pattern from
  `tests/test_openai_chat_demo.py`) verifies the script never
  constructs a real provider in its safe-by-default
  configuration. `pytest --tb=no -q` total >= 550. `mypy src`
  and `ruff check .` are clean.
- **Expected files to touch**:
  - `examples/visibility_full_demo.py` (new, ~200 LoC)
  - `.gitignore` (add `examples/_artifacts/*/`)
  - `tests/test_visibility_full_demo.py` (new, ~6 cases)
  - `README.md` (append one row to the "Examples" section describing `visibility_full_demo`)
  - `initiatives/current/PLAN.md` (mark `STATUS: complete` — last-milestone-only requirement, see §5)

Main script: `examples/visibility_full_demo.py` (~200 LoC).
Structure:

1. `argparse` requires `--confirm-api-call`; missing flag ->
   print explanation -> `sys.exit(2)`.
2. Check `OPENAI_API_KEY` or `DASHSCOPE_API_KEY` is set;
   missing -> print explanation -> `sys.exit(3)`.
3. Create artifact directory
   `examples/_artifacts/visibility-demo-<ISO-timestamp>/`.
4. Instantiate `StderrTracer(stream=open(.../trace.stderr,
   "w"))` plus `OpenAIProvider` plus `AgentLoop`, all wired
   with the `cli._AGGRESSIVE_THRESHOLDS` preset values.
5. Drive a 3-turn scripted conversation designed to exercise
   as many mechanisms as possible:
      turn 1: read a fixed ~10KB file in the repo (triggers
              `externalize`)
      turn 2: read the same file again (triggers `snip`)
      turn 3: "Please remember I prefer Python from now on"
              (mixed Chinese/English text triggers
              `auto_learn` cue + `ProjectMemory` write +
              `memory_select` on the next provider call)
6. After the loop exits, write four artifacts:
      - `metrics.json` -- `LoopResult.metrics` serialized
      - `transcript.txt` -- `Transcript.to_jsonable` rendered
        human-readable
      - `trace.stderr` -- already written live by StderrTracer
      - `summary.md` -- generated from metrics + tracer events,
        one row per channel showing trigger count and first
        fire site, plus a tokens-per-turn line
7. Add `examples/_artifacts/*/` to `.gitignore` so artifacts
   do NOT enter version control. The `.gitignore` change MUST
   land in the same M3 commit; otherwise RUNBOOK Phase 1
   pre-flight will reject the next initiative because of a
   dirty working tree.

Test file: `tests/test_visibility_full_demo.py` reuses the
`_ExplodingProvider` + `monkeypatch` pattern from
`tests/test_openai_chat_demo.py`. About 6 cases covering:

   (a) missing `--confirm-api-call` -> exit code 2 AND no
       provider constructed
   (b) missing API key -> exit code 3
   (c) with `--confirm-api-call` and mocked `OpenAIProvider`,
       all four artifact files exist and are non-empty
   (d) trace.stderr contains at least one `[trace] [budget]`
       line and one `[trace] [externalize]` line
   (e) summary.md is well-formed markdown with one row per
       channel
   (f) `.gitignore` contains `examples/_artifacts/`

After M3 commits, append one row to README.md's "Examples"
section describing `visibility_full_demo`. Phase 2C Tier A
auto-apply may also catch this, but call it out explicitly in
the M3 prompt to avoid relying on the auto-apply.

Scope is **tight**. Doing more than this is failure. §2.5 below lists
what you must NOT do even if it seems natural.

## §2.5 Out of scope (do NOT do these, even if tempting)

- **Do not implement features beyond the demo + guard.** The script is
  a demonstration of M1's tracer and M2's preset; do NOT extend
  `cli.py`, `openai_cli.py`, or any production module beyond what is
  strictly needed to call the existing public API from the demo.
- **Do not change `_AGGRESSIVE_THRESHOLDS` values** introduced in M2.
  Import them as-is.
- **Do not modify M1's `trace.py`/`Tracer` Protocol or M2's
  `SnipTool` constructor signature** — both surfaces are frozen per
  M1/M2 HANDOFF Section 4.
- **Do not refactor unrelated modules.** Touch only files in the
  Expected-files list (§2).
- **Do not modify** `automation/scripts/run_all_milestones.sh`,
  `automation/RUNBOOK.md`, or `automation/templates/*.md` — those are
  the harness, not the project.
- **Do not commit any file under `examples/_artifacts/`.** The
  artifact directory MUST be added to `.gitignore` in the same M3
  commit; if you ran the demo locally and produced artifacts, verify
  `git status` is clean of `examples/_artifacts/` paths before
  committing.
- **Do not introduce new dependencies** in `pyproject.toml`.
- **Do not delete or rewrite M1 or M2's HANDOFF subsections / PROGRESS
  blocks.** Append-only contract enforced by exit-gate check 6.
- **Do not skip the test-only path.** The `_ExplodingProvider`
  key-less test MUST run in CI without an API key; if your demo
  imports trigger a real provider construction at import time, the
  test will fail.

If you find an obvious bug or improvement outside scope, **write it in
your HANDOFF.md Section 5 as a deferred item and move on**. The review
session is the right place to surface these.

## §3 Mandatory reading (in order, before any code change)

1. **`python-replica/CLAUDE.md`** — architecture, per-file summaries,
   project-wide execution rules (TDD, immutability, file-size limits).
2. **`python-replica/initiatives/current/PLAN.md`** — this initiative's
   full brief, including the milestone tables.
3. **`python-replica/initiatives/current/config.yaml`** — machine-readable
   milestone table; confirms `exit_gate` + `phase_ids` for `M3`.
4. **`python-replica/initiatives/current/HANDOFF.md`** — current state.
   Section 2 "Completed milestones" tells you what shipped in M1 + M2.
   Section 4 "Important constraints" lists invariants you MUST respect
   (M1's secret-leak, M1's constructor-default, M2's `_AGGRESSIVE_THRESHOLDS`
   key set, M2's SnipTool default).
   Section 5 "Next milestone guidance" is written FOR you.
5. **`python-replica/initiatives/current/PROGRESS.md`** — terse fact log
   of M1 and M2's commits, test deltas, files touched.
6. **`python-replica/initiatives/current/logs/M2.log`** — skim for
   anomalies (warnings, retries, KeyboardInterrupts) that didn't make
   it into HANDOFF Section 2 design-decisions.
7. **The Expected-files list from §2** — read the source files this
   milestone will touch, plus their existing tests, before writing any
   code. In particular, study `tests/test_openai_chat_demo.py` for the
   `_ExplodingProvider` + `monkeypatch` pattern you will reuse, and
   `examples/openai_chat_demo.py` for the `--confirm-api-call` gating
   pattern.
8. Run `git -C python-replica log --oneline -10` and
   `git -C python-replica show HEAD~1` to see what M2 changed.
   `HEAD~1` is M2's commit because each milestone commits exactly once
   before the loop advances; if you suspect that invariant has been
   violated, use `git -C python-replica log
   2d414d91da65cc5998563e9c63b2d2be7028315d..HEAD --oneline | grep -F
   "[obs-thr/" | head -2` to list every milestone commit and
   `git -C python-replica show <sha>` to inspect SHAs. The `-C
   python-replica` is required because the autonomous loop launches
   `claude --print` from `/Users/leng/my-cc-py`, not from inside the
   repo. The `grep -F` is required because the literal `[` would
   otherwise be parsed as an unclosed character class.

**Before invoking any Edit or Write tool**, print a **5-bullet summary**
to stdout (plain text, not a tool call, not a TaskCreate todo, not a
file write) of what you learned from PLAN / HANDOFF / PROGRESS / M1's
and M2's commits / the existing demo script + test. If those sources
disagree on a fact, treat HANDOFF as advisory and `git diff` + test
output as source of truth. This summary lives only in the captured
session log under `initiatives/current/logs/` (not in any committed
file) — its purpose is to force genuine reading rather than skimming.
If you cannot produce a substantive 5-bullet summary, you have not
read enough; go back to §3.

## §4 Implementation requirements

Follow execution rules documented in `python-replica/CLAUDE.md`. In
particular:

- **TDD**: write tests first (RED — must actually run and fail before
  implementation), implement (GREEN), refactor. Every new code path
  needs a test before the implementation lands.
- **Immutability**: dataclasses prefer `frozen=True` where appropriate;
  never mutate inputs; return new objects.
- **File limits**: functions ≤50 lines, files ≤800 lines. The script
  itself is allowed up to ~200 LoC per PLAN's design sketch; split
  helpers if it grows past that.
- **Determinism in tests**: `MockProvider`, `tmp_path`, `monkeypatch`,
  `capsys`/`capfd` for stderr capture, `_ExplodingProvider` to prove
  the safe-by-default path never constructs a real provider. No
  network. No real API key.
- **No swallowed exceptions**.
- **No `git add -A`** — stage explicit paths only.
- **Match style** of existing examples (compare against
  `examples/openai_chat_demo.py` and
  `examples/aggressive_thresholds_demo.py` from M2).

Additional M3-specific requirements:

- **Exit codes 2 vs 3 are distinct and tested separately.** Code 2 =
  missing `--confirm-api-call`. Code 3 = `--confirm-api-call` set but
  no API key in env. Do NOT collapse them into a single non-zero exit.
- **`.gitignore` and the script MUST land in the SAME commit.** If you
  run the demo locally and it writes to `examples/_artifacts/`, the
  Phase 1 pre-flight for the NEXT initiative will refuse a dirty tree.
  Stage `.gitignore` alongside the script in `git add`.
- **The README "Examples" section row** must be a single line in the
  existing table format; do NOT restructure the Examples section.
- **`summary.md` is well-formed markdown** — one row per channel
  showing trigger count and first fire site, plus a tokens-per-turn
  line. Test (e) verifies this; design the row format so the test can
  reasonably assert it (e.g. one `## Channel: <name>` heading per
  channel, OR one row in a single table — pick one and document the
  choice in HANDOFF).
- **The demo uses the M2 preset values for budget tuning** — import
  `_AGGRESSIVE_THRESHOLDS` from `cli.py` (or a shared module if M2
  introduced one) rather than hard-coding numbers. If M2 left
  `_AGGRESSIVE_THRESHOLDS` module-private, accept that and document
  the dependency in HANDOFF Section 5 (deferred items).

After every meaningful diff, run:

```
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

All three must be green before you commit.

## §5 Exit ritual (MANDATORY)

After your milestone work passes the exit gate in §2, before stopping,
perform these steps **in order**. The shell script independently
verifies the **5 side-effects** listed at the end of this section.
Step 1 below ("Confirm exit gate met") is **honor-system** — the script
cannot inspect whether you actually quoted the right command output, so
the 5 mechanical checks are produced by steps 2-4 (commit, PROGRESS
append, HANDOFF rewrite) plus the standing pytest-green requirement
from §4. Skipping step 1 makes a green run meaningless even though it
would still pass the gate; do not skip it.

### 1. Confirm exit gate met (objectively)

Quote the concrete check from §2 verbatim and show the command output
that proves it. Example:

```
$ python examples/visibility_full_demo.py
... explanatory message ...
$ echo $?
2

$ python examples/visibility_full_demo.py --confirm-api-call
... explanatory message about missing API key ...
$ echo $?
3

$ pytest tests/test_visibility_full_demo.py -v --tb=short
... 6 passed in <time>

$ pytest --tb=no -q
... 550 passed
$ mypy src/
Success: no issues found
$ ruff check .
All checks passed!
```

If you have access to a real API key (you generally will NOT in the
autonomous loop), you may additionally run with `--confirm-api-call`
and verify the four artifacts; this is OPTIONAL for the gate — the
key-less and missing-flag exit paths are sufficient.

"Implementation feels complete" is NOT acceptable. The gate must be
objectively verified by a command's output.

### 2. Commit with explicit paths

Never `git add -A`. The commit subject MUST start with `[obs-thr/M3]`:

```
git -C python-replica add examples/visibility_full_demo.py \
                          tests/test_visibility_full_demo.py \
                          .gitignore \
                          README.md \
                          initiatives/current/PLAN.md \
                          initiatives/current/HANDOFF.md \
                          initiatives/current/PROGRESS.md
git -C python-replica commit -m "[obs-thr/M3] <one-line summary>"
```

The shell loop's first exit-gate check is
`git log -1 | grep -qF "[obs-thr/M3]"`. Without this commit the loop
halts and the review session will NOT run.

### 3. Append a block to `initiatives/current/PROGRESS.md`

Use the terse format in `automation/templates/progress_entry.md`. One
block per milestone. PROGRESS.md is the **fact log for the final
review** — narrative belongs in HANDOFF, not here.

Block shape:

```
## M3 — done 2026-05-22

- commit: <sha> [obs-thr/M3] <subject>
- tests: <M2's count> -> <after> (+N)
- mypy: clean | ruff: clean
- files changed: `<file1>`, `<file2>`, ...
- exit gate: <quote from §2> -> PASS (<one-line evidence>)
- notes: <optional, ≤1 line>
```

Replace `2026-05-22` with `$(date -u +%Y-%m-%d)` at the moment of
commit if the loop spans midnight. The `## M1 — done ...` and
`## M2 — done ...` blocks above yours MUST remain verbatim —
append-only contract enforced by exit-gate check 6.

### 4. Rewrite `initiatives/current/HANDOFF.md`

Use the 5-section structure in `automation/templates/handoff_milestone.md`.
The structure is non-negotiable.

**Section 2 "Completed milestones"**: PRESERVE M1's `### M1` and M2's
`### M2` subsections verbatim. APPEND your own `### M3` subsection
below them.

**Section 4 "Important constraints"**: PRESERVE every prior constraint
entry verbatim. APPEND any new invariants (likely none for M3 since it
is the final milestone — the constraints carried forward will become
the archive's frozen surface).

**Section 5 "Next milestone guidance"**: write a short note that THIS
IS THE LAST MILESTONE — no next milestone exists. List any deferred
items you noticed for a future initiative (e.g. JSON-lines tracer
back-end, `/trace` slash command, semantic memory selector). The
review session will surface these for the user.

### 5. (Last milestone only) Mark PLAN.md STATUS as `complete`

Edit `initiatives/current/PLAN.md` to add or update a top-level
STATUS marker. If PLAN.md does not yet contain a STATUS line, insert
this directly after the provenance block (the `> Bootstrapped on ...`
line) and before the first `# Goal` heading:

```
> STATUS: complete (M1 + M2 + M3 shipped 2026-05-22)
```

Replace `2026-05-22` with `$(date -u +%Y-%m-%d)` at commit time if
the loop has spanned midnight. The review session reads this marker
during Phase 2B Step 1 to confirm the initiative is closed.

---

Do not skip any step. After your commit, the shell script independently
verifies:

1. The commit subject contains `[obs-thr/M3]`.
2. `initiatives/current/HANDOFF.md` was modified in your commit.
3. `initiatives/current/PROGRESS.md` contains a heading matching the
   regex `^## M3 — done YYYY-MM-DD` (anchored — a stray `M3` substring
   in a notes line does NOT satisfy this).
4. `pytest --tb=no -q` is still green.
5. `initiatives/current/HANDOFF.md` contains the required 5-section
   structure (verbatim headers: `## 1. Current initiative`, `## 2.
   Completed milestones`, `## 3. Current repo state`, `## 4. Important
   constraints`, `## 5. Next milestone guidance`). A free-form HANDOFF
   fails this check even if the file was modified.
6. **Append-only contract.** M1's `[obs-thr/M1]` and M2's `[obs-thr/M2]`
   commits are in `baseline_commit..HEAD`, so:
   - `initiatives/current/PROGRESS.md` still contains both the
     `^## M1 — done YYYY-MM-DD` and `^## M2 — done YYYY-MM-DD` blocks;
   - `initiatives/current/HANDOFF.md` still contains both the
     `^### M1$` and `^### M2$` subsections under "## 2. Completed
     milestones".
   Rewriting either file from scratch and only including your own `M3`
   block is a HARD FAILURE here — M1 and M2 are the sources of truth
   on themselves; you APPEND only.

Any of those six failing halts the loop. The final review session will
also audit whether you stayed in scope and whether your HANDOFF accurately
describes your diff.
