# obs-thr-harden — M3: demo-fences-zero-overhead-and-doc-sync

You are an autonomous agent executing milestone **M3** of the
**obs-thr-harden** initiative. This is the **last milestone** in this
initiative. This session was launched by
`automation/scripts/run_all_milestones.sh`. There is no user available to
ask — every decision must be made and documented now.

When you are done, the shell loop will spawn a final interactive
`claude --remote-control` review session if (and only if) you have
honored the exit ritual in §5 below, including the last-milestone PLAN
STATUS update.

## §1 Baseline (verify before any code change)

> M3 starts from M2's commit.

- Last commit: `<HEAD as of M3 start — the [obs-thr-hd/M2] commit>` —
  read from `git -C python-replica log --oneline -1`
- pytest: >= 577 passing (M2 left it at 577+)
- mypy: clean
- ruff: clean

Verify with:

```
git -C python-replica log --oneline -3
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

If pytest is below 577 or mypy/ruff are not clean, **stop and report**.

## §2 Scope (what THIS milestone implements)

- **Phase IDs**: C1, C2, C3
- **Exit gate (must be objectively verifiable)**:
  `pytest --tb=no -q` is green with total >= 587 (after M2, 577, +10).
  `examples/visibility_full_demo.py` must:
  (a) `_new_run_dir` handles same-second timestamp collisions by
      appending a `-2` / `-3` ... `-9` suffix; if all 9 suffixes
      are taken in one second, exit non-zero with a clear message.
  (b) `_parse_trace_events` tolerates the repr-quoted values that
      M1 introduced (whitespace-containing strings, dict / list
      reprs).
  `tests/test_visibility_full_demo.py` adds:
    - a directory-collision case (two demo runs in the same second)
    - a parser-tolerates-quoted-values case
  `tests/test_trace.py` adds a `NullTracer` performance assertion:
    timeit 100_000 calls of `tracer.emit("compact", count=1,
    tokens=42)`; total elapsed must be < 20ms (mean < 200ns/call).
    Skip under coverage instrumentation (detect via
    `os.environ.get("COVERAGE_RUN")` or `sys.gettrace()`) so the
    gate doesn't false-fail under `pytest --cov`.
  `tests/test_cli.py` adds a `--help` snapshot test pinning the
  key phrases for --verbose, --aggressive-thresholds, and
  --max-context-tokens (including the M2-added precedence line
  `preset value applies`).
  `README.md` "Examples" section must explicitly reference
  `aggressive_thresholds_demo.py` (currently absent) and
  `visibility_full_demo.py`.
  `python-replica/CLAUDE.md` "Implementation Roadmap" gets one new
  entry for this hardening initiative listing the SHA range and
  pytest delta.
  `mypy src` and `ruff check .` are clean.

- **Expected files to touch**:
  - `examples/visibility_full_demo.py` (extend `_new_run_dir` and
    `_parse_trace_events` only — do NOT create new example files)
  - `tests/test_visibility_full_demo.py` (collision + parser cases)
  - `tests/test_trace.py` (NullTracer perf assert)
  - `tests/test_cli.py` (--help snapshot)
  - `README.md` (Examples section)
  - `python-replica/CLAUDE.md` (Implementation Roadmap addition)

### Implementation notes (from PLAN.md)

Touch list (ceiling): 0 src + 3 test + 1 example + 2 docs. This is the
lowest-risk milestone — zero source-code change.

Do NOT touch `trace.py` (M1 modified it), do NOT touch
`cli.py` / `openai_cli.py` (M2 modified them), do NOT introduce new
dependencies (the perf assert uses the stdlib `timeit`).

**`_new_run_dir` collision logic**:
```python
base = examples/_artifacts / f"visibility-demo-{timestamp}"
if not base.exists():
    base.mkdir(parents=True)
    return base
for suffix in range(2, 10):
    candidate = base.with_name(base.name + f"-{suffix}")
    if not candidate.exists():
        candidate.mkdir(parents=True)
        return candidate
raise SystemExit(
    f"too many demo runs in the same second; clean "
    f"{base.parent} or wait a second"
)
```

**`_parse_trace_events` quoted-value tolerance**:
The current implementation uses `token.partition("=")` plus a line-
level `split()`, which assumes every value is whitespace-free. Change
the scan so that, after `=`, if the next character is `'` or `"`, read
until the matching closing quote (handles `repr({...})`-shaped
values); otherwise split on the next whitespace as before. Preserve
backward compatibility for unquoted values.

**`NullTracer` perf assertion**:
```python
import timeit
from simple_coding_agent.trace import NullTracer
t = NullTracer()
elapsed = timeit.timeit(
    lambda: t.emit("compact", count=1, tokens=42),
    number=100_000,
)
assert elapsed < 0.020, f"{elapsed=}s exceeds 20ms budget"
```
Guard with a coverage-mode skip at the top of the test
(`pytest.skip(...)` if `os.environ.get("COVERAGE_RUN")` set or
`sys.gettrace() is not None`).

**`--help` snapshot test shape**:
Capture help text via `cli.main(["--help"])` + `SystemExit` +
`capsys`. Assert it contains:
  - `"--verbose"`
  - either `"Stream [trace]"` or `"verbose trace"`
  - `"--aggressive-thresholds"`
  - `"preset"`
  - `"preset value applies"` (the M2-added wording)
Pin key phrases, not the whole block, so future innocent help-text
rewording doesn't break the snapshot.

**`README.md` "Examples" section**:
- Confirm `visibility_full_demo.py` is described (the previous M3
  inbox of the prior initiative requested this; verify and fix if
  missing).
- Add a one-line link for `aggressive_thresholds_demo.py`.
- Add `stress_demo.py` and `microcompact_demo.py` links if they are
  not already there.

**`python-replica/CLAUDE.md` "Implementation Roadmap" addition**:
At the end of the Implementation Roadmap section, append:

> - **observable-thresholds-harden initiative — M1–M3**
>   (`<SHA1>`–`<SHA3>`, 2026-05-23). M1 hardens `trace.py` against
>   closed streams and unsafe value serialisation; M2 fixes the
>   `_AGGRESSIVE_THRESHOLDS` bug where `context_tokens` /
>   `reserved_output_tokens` were always shadowed by argparse
>   defaults, and adds the full 8-field precedence matrix; M3 hardens
>   `visibility_full_demo` against directory collision and parser
>   fragility, asserts `NullTracer` zero overhead via timeit, and
>   syncs README + CLAUDE.md. pytest 557 → ~587 (+30).

Fill `<SHA1>` / `<SHA3>` with the actual M1 / M3 commit SHAs (look
them up from `git -C python-replica log --oneline -10`).

**Test cases added (~10)**: directory collision 1-2 + parser tolerance
2 + perf assert 1 + --help snapshot 2-3 + README reference check 1 +
regression coverage 1-2.

**Risk assessment**: zero src changes. Surface is examples / docs /
tests only — the lowest-risk milestone. The perf assertion is
sensitive to interpreter version (Python 3.14 noop `emit` is
~50-100ns/call on the harness CI); the 200ns budget leaves 2-3x
headroom and the test skips under coverage.

pytest baseline: 577 -> >= 587 (+10 minimum).

Scope is **tight**. Doing more than this is failure. §2.5 below lists
what you must NOT do even if it seems natural.

## §2.5 Out of scope (do NOT do these, even if tempting)

- **Do not touch `src/simple_coding_agent/trace.py`** — M1 finalised
  it. M1's HANDOFF Section 4 invariants carry forward.
- **Do not touch `src/simple_coding_agent/cli.py` /
  `openai_cli.py` / `compact.py`** — M2 finalised them. If the
  `--help` snapshot test fails, the snapshot is wrong (M2 already
  pinned the wording); investigate before changing the help text.
- **Do not create new example files.** `aggressive_thresholds_demo.py`,
  `stress_demo.py`, `microcompact_demo.py`, and `visibility_full_demo.py`
  retain their existing shapes. ONLY `visibility_full_demo.py` may be
  edited, and ONLY `_new_run_dir` and `_parse_trace_events`.
- **Do not change the 9-channel list** (`compact`, `reactive`,
  `microcompact`, `snip`, `externalize`, `memory_select`, `claude_md`,
  `auto_learn`, `budget`).
- **Do not add or remove keys from `_AGGRESSIVE_THRESHOLDS`** —
  initiative-wide invariant.
- **Do not change `NullTracer.emit` body** — it MUST remain literally
  `pass`. The whole point of M3's perf assertion is to PROVE this.
- **Do not introduce new dependencies** in `pyproject.toml`. Use
  stdlib `timeit` for the perf assertion.
- **Do not modify** `automation/scripts/run_all_milestones.sh`,
  `automation/RUNBOOK.md`, or `automation/templates/*.md`.
- **Do not change `metrics.json` field set** or `summary.md` table
  columns in `visibility_full_demo.py`.
- **Do not introduce new tracer implementations** (CompositeTracer,
  JSONLinesTracer, OTelTracer, etc.) — out-of-scope at the initiative
  level.

If you find an obvious bug or improvement outside scope, **write it in
your HANDOFF.md Section 5 ("deferred items") and move on**.

## §3 Mandatory reading (in order, before any code change)

1. **`python-replica/CLAUDE.md`** — architecture, per-file summaries.
   You'll be adding a new bullet to "Implementation Roadmap".
2. **`python-replica/initiatives/current/PLAN.md`** — this initiative's
   full brief. Confirm STATUS is currently in-progress; M3 will mark
   it complete in exit ritual step 5.
3. **`python-replica/initiatives/current/config.yaml`** — confirms
   `exit_gate` + `phase_ids` for `M3`.
4. **`python-replica/initiatives/current/HANDOFF.md`** — Section 2
   `### M1` and `### M2` subsections tell you what shipped. Section 4
   lists invariants. Section 5 is written for you.
5. **`python-replica/initiatives/current/PROGRESS.md`** — terse fact
   log of M1 and M2.
6. **`python-replica/examples/visibility_full_demo.py`** — read the
   whole file (~346 lines). You'll edit only `_new_run_dir` and
   `_parse_trace_events`; everything else is untouchable.
7. **`python-replica/src/simple_coding_agent/trace.py`** — read M1's
   final version (no edits!). The perf assertion confirms its
   `NullTracer.emit` is still trivially `pass`.
8. **`python-replica/README.md`** — read the current "Examples"
   section so you know which entries are missing.
9. **Run** `git -C python-replica log --oneline -10` and look at the
   M1 / M2 commits to confirm the SHA range you'll cite in the CLAUDE.md
   bullet.
10. **Skim** `initiatives/current/logs/M1.log` and
    `initiatives/current/logs/M2.log` for anomalies.

**Before invoking any Edit or Write tool**, print a **5-bullet summary**
to stdout of what you learned from PLAN / HANDOFF / PROGRESS /
M1+M2 commits / the current `visibility_full_demo.py` body. If those
sources disagree on a fact, treat HANDOFF as advisory and `git diff` +
test output as source of truth. This summary lives only in the
captured session log — its purpose is to force genuine reading. If you
cannot produce a substantive 5-bullet summary, you have not read
enough; go back to §3.

## §4 Implementation requirements

Follow execution rules documented in `python-replica/CLAUDE.md`. In
particular:

- **TDD**: write tests first (RED — must actually run and fail before
  implementation), implement (GREEN), refactor.
- **Immutability**: dataclasses prefer `frozen=True`; never mutate
  inputs; return new objects.
- **File limits**: functions ≤50 lines, files ≤800 lines.
- **Determinism in tests**: `MockProvider`, `tmp_path`, `monkeypatch`.
  No network. No real API key.
- **No swallowed exceptions**.
- **No `git add -A`** — stage explicit paths only.
- **Match style** of existing modules.

After every meaningful diff, run:

```
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

All three must be green before you commit.

## §5 Exit ritual (MANDATORY — last milestone)

After your milestone work passes the exit gate in §2, before stopping,
perform these steps **in order**.

### 1. Confirm exit gate met (objectively)

Quote the concrete checks from §2 verbatim and show the command output
that proves them. Example:

```
$ cd python-replica && pytest --tb=no -q | tail -3
587 passed in 4.55s

$ cd python-replica && pytest tests/test_visibility_full_demo.py -v -k "collision or quoted" | head -15
PASSED  tests/test_visibility_full_demo.py::test_new_run_dir_appends_suffix_on_collision
PASSED  tests/test_visibility_full_demo.py::test_parse_trace_events_tolerates_quoted_values

$ cd python-replica && pytest tests/test_trace.py::test_null_tracer_zero_overhead -v
PASSED

$ cd python-replica && pytest tests/test_cli.py -v -k help_snapshot
PASSED

$ grep -c "aggressive_thresholds_demo.py" python-replica/README.md
1
```

"Implementation feels complete" is NOT acceptable.

### 2. Commit with explicit paths

Never `git add -A`. The commit subject MUST start with
`[obs-thr-hd/M3]`:

```
git -C python-replica add examples/visibility_full_demo.py tests/test_visibility_full_demo.py tests/test_trace.py tests/test_cli.py README.md CLAUDE.md initiatives/current/PROGRESS.md initiatives/current/HANDOFF.md initiatives/current/PLAN.md
git -C python-replica commit -m "[obs-thr-hd/M3] demo collision fences + NullTracer perf assert + doc sync"
```

Note: `initiatives/current/PLAN.md` is in the file list because step 5
below marks the initiative complete.

### 3. Append a block to `initiatives/current/PROGRESS.md`

Use the terse format. APPEND only; do NOT delete or modify M1's or
M2's blocks.

```
## M3 — done 2026-05-23

- commit: <sha> [obs-thr-hd/M3] <subject>
- tests: <M2-ending count> -> <after> (+<delta>)
- mypy: clean | ruff: clean
- files changed: `examples/visibility_full_demo.py`,
  `tests/test_visibility_full_demo.py`, `tests/test_trace.py`,
  `tests/test_cli.py`, `README.md`, `CLAUDE.md`
- exit gate: <one-line paraphrase of §2 gate> -> PASS (<evidence>)
- notes: <optional, ≤1 line>
```

### 4. Rewrite `initiatives/current/HANDOFF.md`

Use the 5-section structure in `automation/templates/handoff_milestone.md`.
The structure is non-negotiable.

**Section 1 "Current initiative"**:
- `current milestone`: just-completed `M3` — demo-fences-zero-overhead-and-doc-sync
- `next milestone`: none — initiative complete
- `all milestones`: `M1 [done], M2 [done], M3 [done]`

**Section 2 "Completed milestones"**: APPEND your own `### M3`
subsection. The `### M1` and `### M2` subsections from M1 and M2 MUST
remain verbatim — exit-gate check 6 enforces this.

**Section 4 "Important constraints"**: any final invariants the
hypothetical follow-up initiative should respect. At minimum:
- "The pinned `--help` wording `preset value applies` (M2) is now
  load-bearing — both `cli.py` and the `tests/test_cli.py` snapshot
  test depend on it."
- "`visibility_full_demo._parse_trace_events` now accepts both
  unquoted and `'`/`"`-quoted values. Future changes to the trace
  line shape must keep one of these two forms parseable."

**Section 5 "Next milestone guidance"**: `_(initiative complete —
follow-up initiatives bootstrap from a fresh INBOX.md)_`.

### 5. Mark `initiatives/current/PLAN.md` STATUS as complete

This is the last-milestone-only step. Open `initiatives/current/PLAN.md`
and either:
(a) Update an existing `STATUS:` line (e.g., `> STATUS: complete (M1
    + M2 + M3 shipped 2026-05-23)`), OR
(b) If no STATUS line exists, append one immediately after the
    provenance block (line 2 area), in the format:
    `> STATUS: complete (M1 + M2 + M3 shipped 2026-05-23).`

Then commit (this PLAN.md modification belongs in your single M3
commit, included in step 2 above).

---

Do not skip any step. After your commit, the shell script independently
verifies:

1. The commit subject contains `[obs-thr-hd/M3]`.
2. `initiatives/current/HANDOFF.md` was modified in your commit.
3. `initiatives/current/PROGRESS.md` contains a heading matching the
   regex `^## M3 — done YYYY-MM-DD`.
4. `pytest --tb=no -q` is still green.
5. `initiatives/current/HANDOFF.md` contains the required 5-section
   structure.
6. **Append-only contract.** `initiatives/current/PROGRESS.md` still
   contains the `^## M1 — done YYYY-MM-DD` AND `^## M2 — done
   YYYY-MM-DD` blocks; `HANDOFF.md` still contains the `^### M1$` AND
   `^### M2$` subsections under Section 2. Rewriting either file from
   scratch with only your `M3` block is a HARD FAILURE.

After your commit passes those six checks, the shell script will spawn
a final `claude --remote-control` review session that audits the whole
initiative against `automation/templates/review.md`. The review session
will produce `REVIEW.md`, may apply Tier A / B doc edits (per RUNBOOK
"Doc-update tiers"), and will then archive `initiatives/current/` into
`initiatives/_archive/2026-05-obs-thr-harden/` with a final
`[obs-thr-hd/wrap]` commit.
