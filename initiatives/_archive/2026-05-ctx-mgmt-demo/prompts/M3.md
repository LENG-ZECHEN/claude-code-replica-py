# ctx-mgmt-demo — M3: write-3-notebooks-and-readme

You are an autonomous agent executing milestone **M3** of the
**ctx-mgmt-demo** initiative. This session was launched by
`automation/scripts/run_all_milestones.sh`. There is no user available to
ask — every decision must be made and documented now.

When you are done, the shell loop will start the next milestone (none —
M3 is the last) and the review session will run.

## §1 Baseline (verify before any code change)

- Initiative baseline: `9ba662bf65e45d08949d4524203773a63bf36902`
- HEAD must be M2's `[ctx-demo/M2]` commit
- pytest at initiative baseline: 816 passing
- pytest at M1: 816 + 3-ish (M1 added ≥ 3 tests)
- pytest at M2: same as M1 (M2 is side-effect, no new tests)
- mypy: clean | ruff: clean

Verify with:

```
git -C python-replica log --oneline -5
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

If HEAD is not M2's commit, or pytest is not green, **stop and report**.

## §2 Scope (what THIS milestone implements)

- **Phase IDs**: [F3]
- **Exit gate (must be objectively verifiable)**:
  `python-replica/demo/` contains:
  - `README.md` (links the 3 notebooks; references
    `examples/stress_demo.py` for reactive compact with rationale;
    documents the swappable-model fallback list)
  - `01_tool_result_management.md`
  - `02_full_compact.md`
  - `03_microcompact.md`

  Each notebook embeds **at least 5 lines** of captured output from
  its corresponding `_artifacts/` folder, includes `file:line`
  source references for each demoed mechanism, shows the exact
  command line that produced the artifacts, and names the model
  that actually produced them (read from `stats_output.txt`
  header).

  **No `src/` changes, no new tests, no additional real-API spend.**
- **Expected files to touch / create**:
  - `python-replica/demo/README.md` (new)
  - `python-replica/demo/01_tool_result_management.md` (new)
  - `python-replica/demo/02_full_compact.md` (new)
  - `python-replica/demo/03_microcompact.md` (new)
  - **No `src/`, no `tests/`, no `pyproject.toml`, no `.env`.**

### Implementation notes from PLAN

**Pure docs milestone.** M2's artifacts already exist; M3 reads
them and writes markdown.

**Per-notebook structure:**

```
# Title + one-paragraph goal

## Setup
(env, exact command line used in M2, model name read from the
artifact's stats_output.txt header)

## Step-by-step
(numbered, with embedded ``` blocks: $ command, then the relevant
captured output. Don't paste the entire artifact — pick the
demonstrative lines.)

## What to look for
(which [trace] channel lines, which /stats counters, and what
they prove)

## Source mapping
(file:L references into src/simple_coding_agent/... for each
demoed mechanism)
```

**Embed style:** fenced blocks, language-tagged.

- Use ` ```text ` for trace stderr
- Use ` ```json ` for metrics
- Use ` ```console ` for transcript excerpts

**README.md must:**

- One-line summary of each notebook.
- Note that `_artifacts/` is **one canonical run**; re-runs vary in
  exact text but counter-level assertions hold.
- Point reactive-compact readers to `examples/stress_demo.py` with
  the rationale: reactive compact is provider-independent error
  recovery — the mock demo is the right surface for that mechanism.
- List env vars consumed (`DASHSCOPE_API_KEY`, `OPENAI_BASE_URL`,
  `SIMPLE_AGENT_MODEL`) and point at the existing
  `python-replica/.env` (NOT `env.sample`).
- Document the swappable-model fallback: list the alternatives
  (`qwen3-coder-plus-2025-09-23`, `glm-5`, `deepseek-v3.2`,
  `qwen-plus-latest`, `qwen-long-latest`) and tell re-runners to
  switch the `.env`'s `SIMPLE_AGENT_MODEL` if their default's quota
  is dead.

**Source mapping per notebook** (use `file:line` so editors can
click through):

- `01`: `snip.py:<L of SnipTool.snip>`, `tool_result_store.py:<L
  of process_results_batch>`, `trace.py:<channel name>`
- `02`: `compact.py:<L of ContextCompactor.should_compact>`,
  `compact.py:<L of ContextCompactor.compact>`, `loop.py:<L of
  _maybe_compact>`
- `03`: `compact.py:<L of MicroCompactor.should_microcompact>`,
  `compact.py:<L of MicroCompactor.microcompact>`, `loop.py:<L of
  _maybe_microcompact>`, plus `cli.py` / `openai_cli.py` for
  `--microcompact-minutes` (cite M1's actual line)

Find line numbers by running `grep -n` on the live source — do NOT
paste from memory.

Scope is **tight**. Doing more than this is failure. §2.5 below
lists what you must NOT do even if it seems natural.

## §2.5 Out of scope (do NOT do these, even if tempting)

- **Do not re-run any real-API capture.** M2's artifacts are the
  source of truth. If a counter looks wrong, write it in HANDOFF
  Section 5 and proceed with what's there. Do not "fix" by
  re-capturing.
- **Do not modify any `src/` file.** M3 is pure docs.
- **Do not modify any `tests/` file.** No new tests.
- **Do not modify `CLAUDE.md` per-file summaries.** The review
  session handles that per RUNBOOK Doc-update tiers.
- **Do not modify the top-level `README.md`** (the project README).
  The new `demo/README.md` is a per-folder index, not the project
  pitch.
- **Do not modify** `automation/scripts/run_all_milestones.sh`,
  `automation/RUNBOOK.md`, or `automation/templates/*.md`.
- **Do not rewrite the captured artifacts.** Read, quote, and
  reference them. They are immutable evidence.
- **Do not paste large artifact dumps** into the notebooks (≥ 5
  lines per scenario is the floor, not the ceiling — but use
  judgment; a full 200-line trace dump dilutes the explanation).

If you find an obvious bug or improvement outside scope, write it
in HANDOFF.md Section 5 and move on.

## §3 Mandatory reading (in order, before any code change)

1. **`python-replica/CLAUDE.md`** — architecture, per-file summaries
   (you need them for the Source mapping sections).
2. **`python-replica/initiatives/current/PLAN.md`** — full brief.
3. **`python-replica/initiatives/current/config.yaml`** — confirms
   M3's `exit_gate`.
4. **`python-replica/initiatives/current/HANDOFF.md`** — Section 2's
   `### M1` and `### M2` subsections tell you what was built and
   which model was used per scenario. Section 4 lists
   M3-relevant invariants. Section 5 was written FOR you.
5. **`python-replica/initiatives/current/PROGRESS.md`** — M1, M2
   fact entries.
6. **All four artifacts of each scenario**, in this order:
   - `demo/_artifacts/01_tool_result_management/{stats_output.txt,metrics.json,trace.stderr,transcript.txt}`
   - `demo/_artifacts/02_full_compact/{...same...}`
   - `demo/_artifacts/03_microcompact/{...same...}`

   For each, read `stats_output.txt` first (it carries the model
   name in the header), then `metrics.json` (to know counter
   values), then skim `trace.stderr` and `transcript.txt` to pick
   the most demonstrative lines for the notebook.
7. **`examples/visibility_full_demo.py`** — read its docstring
   section so README's "complement to" wording is accurate.
8. **`examples/stress_demo.py`** — read its docstring so the
   reactive-compact pointer in README is accurate.
9. **`git -C python-replica log --oneline -10`** — confirm HEAD is
   M2's `[ctx-demo/M2]` commit.

**Before invoking any Edit or Write tool**, print a **5-bullet
summary** of what you learned from the artifacts and what each
notebook will demonstrate. Specifically address: what model produced
each scenario, what counters each notebook will cite, what the
notable trace lines are.

## §4 Implementation requirements

This is a docs-only milestone, so most code-quality rules in
CLAUDE.md don't apply directly. Still:

- **Files ≤ 800 lines.** Each notebook should be focused and
  scannable; 100-300 lines is plausible.
- **No swallowed accuracy.** If `stats_output.txt` says
  `# model: qwen-plus-latest` for scenario 02, the notebook's Setup
  must say `qwen-plus-latest` for scenario 02. Do not paraphrase
  or guess.
- **All cited line numbers must be current.** Use `grep -n` against
  HEAD; do NOT trust line numbers in PLAN's notes (those were
  illustrative).
- **No `git add -A`** — stage explicit paths.

After your writes, verify the codebase still passes:

```
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

(M3 doesn't change src/ so these should be unchanged from M2's
green state, but verify anyway.)

## §5 Exit ritual (MANDATORY)

After your milestone work passes the exit gate in §2, before
stopping, perform these steps **in order**.

### 1. Confirm exit gate met (objectively)

Quote the concrete check from §2 verbatim and show command output
that proves it. Example:

```
$ ls demo/
README.md  01_tool_result_management.md  02_full_compact.md
03_microcompact.md  _artifacts  _scripts

$ for f in demo/{README,01_tool_result_management,02_full_compact,03_microcompact}.md; do
    echo "=== $f ($(wc -l < $f) lines) ==="
    grep -c '^```' "$f" || true   # count fenced blocks
  done

$ grep -F '# model:' demo/_artifacts/*/stats_output.txt
demo/_artifacts/01_*/stats_output.txt:# model: qwen3.6-plus
...

# Verify each notebook references the correct model:
$ grep -E 'qwen|glm|deepseek' demo/01_tool_result_management.md
...

$ pytest --tb=no -q
... 819 passed in 14s
```

"Notebooks feel complete" is NOT acceptable. The gate must be
objectively verified by command output, including the line counts
and the file existence.

### 2. Append a block to `initiatives/current/PROGRESS.md`

**Write this BEFORE committing.**

Block shape:

```
## M3 — done YYYY-MM-DD

- commit: [ctx-demo/M3] (see git log)
- tests: <unchanged from M2> (+0)
- mypy: clean | ruff: clean
- files changed: `demo/README.md`, `demo/01_tool_result_management.md`, `demo/02_full_compact.md`, `demo/03_microcompact.md`
- exit gate: 4 demo .md files exist with ≥5 embedded lines per scenario, source mappings, model names from stats headers -> PASS
- notes: <optional, ≤1 line — e.g., "all 3 scenarios used qwen3.6-plus; no quota swap needed">
```

### 3. Rewrite `initiatives/current/HANDOFF.md`

Use the 5-section structure in
`automation/templates/handoff_milestone.md`.

**Section 1**: mark this as the LAST milestone — `next milestone:
none — all milestones complete`. Update the milestone status line
to `M1 [done], M2 [done], M3 [done]`.

**Section 2 "Completed milestones"**: PRESERVE `### M1` and
`### M2` verbatim. APPEND `### M3` with files written, behavior
implemented (3 notebooks + README), design decisions (any
embedding choices, source-mapping conventions), known limitations.

**Section 4 "Important constraints"**: add a note for the review
session: "All 3 milestones complete. Review handles project-doc
updates per RUNBOOK Doc-update tiers."

**Section 5 "Next milestone guidance"**: write FOR the review
session. Include:

- next scope: review and wrap-up per RUNBOOK Phase 2B/2C.
- relevant files for review audit: `initiatives/current/*` plus
  the `demo/` tree.
- expected outcome: `REVIEW.md` + archive move + Tier A/B doc
  edits.
- risks: any anomalies during M1/M2/M3 worth flagging (e.g., model
  swap mid-capture, guard test relaxation in M1).

### 4. Commit + last-milestone PLAN status

Stage the 4 notebooks plus PROGRESS.md and HANDOFF.md:

```
git -C python-replica add \
    demo/README.md \
    demo/01_tool_result_management.md \
    demo/02_full_compact.md \
    demo/03_microcompact.md \
    initiatives/current/PROGRESS.md \
    initiatives/current/HANDOFF.md \
    initiatives/current/PLAN.md
git -C python-replica commit -m "[ctx-demo/M3] notebooks + README; initiative complete"
```

**Last milestone extra step — mark `initiatives/current/PLAN.md`
STATUS as complete:** Edit `PLAN.md` to add a new `>` blockquote
line immediately after the existing `> Bootstrapped on 2026-05-25.
...` provenance line:

```
> STATUS: complete (M1–M3 shipped on YYYY-MM-DD)
```

Do not alter the YAML section. Stage `PLAN.md` in the same commit
above.

**Do NOT `git commit --amend`** to embed the SHA into PROGRESS.md.

---

Do not skip any step. After your commit, the shell script
independently verifies:

1. The commit subject contains `[ctx-demo/M3]`.
2. `initiatives/current/HANDOFF.md` was modified in your commit.
3. `initiatives/current/PROGRESS.md` contains `^## M3 — done YYYY-MM-DD`.
4. `pytest --tb=no -q` is still green.
5. `initiatives/current/HANDOFF.md` contains the required 5-section
   structure.
6. Append-only contract: `### M1` and `### M2` subsections in
   HANDOFF Section 2 preserved verbatim; `## M1 — done` and
   `## M2 — done` blocks in PROGRESS.md preserved verbatim.

After wrap-up, the review session will audit all 3 milestones and
generate `initiatives/current/REVIEW.md`, then archive the initiative.
