# ctx-mgmt-pdf-align — M3: autocompact-recent-files-attachment

You are an autonomous agent executing milestone **M3** of the
**ctx-mgmt-pdf-align** initiative. This session was launched by
`automation/scripts/run_all_milestones.sh`. There is no user available to
ask — every decision must be made and documented now.

When you are done, the shell loop will start the next milestone if (and
only if) you have honored the exit ritual in §5 below.

## §1 Baseline (verify before any code change)

- Last commit: `8f1d98f` (initiative baseline; M2's commit is your real
  starting point — re-verify by reading HANDOFF.md Section 3, which M2
  will have rewritten)
- pytest: 615 passing (baseline; after M1+M2 expect 637+; HANDOFF.md
  Section 3 has the post-M2 count)
- mypy: clean
- ruff: clean

Verify with:

```
git -C python-replica log --oneline -3
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

If the numbers do not match, **stop and report**.

## §2 Scope (what THIS milestone implements)

- **Phase IDs**: C1, C2
- **Exit gate (must be objectively verifiable)**:
  pytest --tb=no -q passes with count grown by >=8 from M2-post baseline
  AND
  AgentLoop carries a `_recent_file_snapshots: deque[FileSnapshot]`
  capped at N (default 5) populated inside `_execute_one()` when a
  `read_file` call succeeds (FileSnapshot = frozen dataclass with
  `path: str`, `content: str`, `captured_at: str`) AND
  `_force_compact()` reads the current snapshot deque and passes it
  into `ContextCompactor.compact(snapshots=...)`, which stores it on
  the returned `CompactSummary.recent_file_snapshots: tuple` AND
  `ContextBuilder.build()` reads `compact_summary.recent_file_snapshots`
  and emits one `MessageType.ATTACHMENT` message per snapshot
  immediately after `COMPACT_BOUNDARY` and before the keep_recent
  messages; each attachment serializes as a user-role message whose
  content is `<recent-files>\n<file path="...">CONTENT</file>\n
  </recent-files>`, `is_meta=True`, and is NOT counted toward
  keep_recent budget trimming.
- **Expected files to touch**:
  - `src/simple_coding_agent/loop.py` — add
    `_recent_file_snapshots: deque[FileSnapshot]` state +
    capture hook in `_execute_one()` for successful read_file +
    pass-through into `_force_compact()`
  - `src/simple_coding_agent/compact.py` — `CompactSummary` gains a
    new immutable `recent_file_snapshots` field; `ContextCompactor.compact()`
    accepts and stores it
  - `src/simple_coding_agent/context.py` — `ContextBuilder.build()`
    reads snapshots off the CompactSummary and emits ATTACHMENT
    messages; verify ATTACHMENT passes API serialization filter (NOT
    filtered out)
  - `src/simple_coding_agent/models.py` — `FileSnapshot` frozen
    dataclass (or place in compact.py — your choice, document in
    HANDOFF). Confirm `MessageType.ATTACHMENT` already exists.
  - `tests/test_compact.py` — snapshot field on CompactSummary, compact
    accepts snapshots
  - `tests/test_loop.py` — _execute_one capture hook, deque cap, compact
    integration
  - `tests/test_context.py` — ATTACHMENT pass-through, ordering after
    COMPACT_BOUNDARY, budget-trim does not pop ATTACHMENT preferentially

PDF reference: §4 autoCompact post-restoration "recent files re-inject".
User clarified explicitly: do NOT have the model re-read — snapshot
file contents BEFORE compaction, then re-attach them in the rebuilt
context as an attachment.

`MessageType.ATTACHMENT` already exists in `models.py:51` but is
unused. Confirm during M3 that `context._normalize_messages()` already
passes ATTACHMENT through to API serialization; if it filters them out
(like COMPACT_BOUNDARY), change the filter to allow ATTACHMENT through.

Recent-files tracking lives on AgentLoop (not Transcript) because the
snapshot must be the live content at the moment `read_file` returned,
not whatever the (possibly snipped / microcompacted) tool_result
currently shows. Hook the capture into `_execute_one` after the success
branch of the `read_file` tool dispatch.

Snapshot deque survives across compactions — after compact runs,
snapshots stay attached to AgentLoop so the next compact can re-attach
the same / updated set. Append eviction is newest-wins
(`deque(maxlen=N)`).

`CompactSummary` becomes a frozen dataclass with a new
`recent_file_snapshots: tuple[FileSnapshot, ...] = ()` field (immutable
default).

Scope is **tight**. Doing more than this is failure. §2.5 below lists
what you must NOT do even if it seems natural.

## §2.5 Out of scope (do NOT do these, even if tempting)

- **Do not implement any other milestone** in this initiative. Stop at
  `M3`. M4 will add the model-driven `snip_history` tool — leave that
  alone.
- **Do not refactor unrelated modules.** Touch only files in the
  Expected-files list (§2), plus tests for those files.
- **Do not modify** the harness (`automation/`).
- **Do not modify** `CLAUDE.md` / `README.md` — Tier A in the review
  session handles the new public symbols.
- **Do not remove** the `compact_threshold` legacy second-trigger path
  (HANDOFF invariant from M1).
- **Do not remove** `context._remove_orphan_tool_results()` (HANDOFF
  invariant from M2).
- **Do not change** what triggers compaction — the formula and
  thresholds shipped in M1 stay. M3 only changes what `compact()`
  produces (CompactSummary content) and what `build()` consumes.
- **Do not modify** existing snapshot tools (`write_file`, etc.) — only
  `read_file` results are snapshot-eligible.
- **Do not snapshot** the same path more than once in the deque (use
  newest-wins eviction — appending a new snapshot for an existing path
  removes the older entry).
- **Do not introduce new dependencies** in `pyproject.toml`.
- **Do not touch** `src/simple_coding_agent/tool_result_store.py`.

If you find an obvious bug or improvement outside scope, **write it in
your HANDOFF.md Section 5 and move on**.

## §3 Mandatory reading (in order, before any code change)

1. **`python-replica/CLAUDE.md`** — architecture, per-file summaries.
2. **`python-replica/initiatives/current/PLAN.md`** — full brief.
3. **`python-replica/initiatives/current/config.yaml`** — confirms
   `exit_gate` + `phase_ids` for `M3`.
4. **`python-replica/initiatives/current/HANDOFF.md`** — Section 2
   "Completed milestones" → `### M1`, `### M2` tell you what shipped.
   Section 4 "Important constraints" lists invariants to respect.
   Section 5 "Next milestone guidance" written FOR you.
5. **`python-replica/initiatives/current/PROGRESS.md`** — terse fact log
   for M1 and M2.
6. **The Expected-files list from §2** — read each source file BEFORE
   editing:
   - `src/simple_coding_agent/loop.py` — find `_execute_one()` (around
     line 626-654). The capture hook goes into the SUCCESS branch
     after `tool_result_store.process_result()` returns. Also find
     `_force_compact()` (524-530).
   - `src/simple_coding_agent/compact.py` — `CompactSummary` dataclass
     and `ContextCompactor.compact()` (404-506). Add the
     `recent_file_snapshots` field as a kwarg with default empty tuple.
   - `src/simple_coding_agent/context.py` — `ContextBuilder.build()`
     (203-243). The ATTACHMENT emission happens after the
     COMPACT_BOUNDARY emission and before the keep_recent slice. Check
     `_normalize_messages()` (and any API-serialization site) to
     ensure ATTACHMENT is NOT filtered out alongside COMPACT_BOUNDARY
     and (M2's) SNIP_BOUNDARY.
   - `src/simple_coding_agent/models.py` — confirm `MessageType.ATTACHMENT`
     exists (line 51). The `Message.attachment(path, content)` factory
     may need to be added — follow the existing
     `Message.compact_boundary()` pattern.
   - `tests/test_compact.py` and `tests/test_loop.py` — existing cases.
     Anything that asserts CompactSummary fields needs updating to be
     forward-compatible with the new field.
7. **Since M3 is not M1**: run `git -C python-replica log --oneline -10`
   and `git -C python-replica show HEAD~1` (= M2's commit, assuming the
   single-commit-per-milestone invariant held). Use the
   `8f1d98f01f764a32df538b6319115d392696ab4e..HEAD --oneline | grep -F
   "[ctx-pdf/"` fallback if HEAD~1 isn't M2's commit.
8. **Since M3 is > M2**: skim
   `initiatives/current/logs/M2.log` for any anomalies (warnings,
   retries, KeyboardInterrupts) that didn't make it into HANDOFF
   Section 2 design-decisions.

**Before invoking any Edit or Write tool**, print a **5-bullet summary**
of what you learned. Force genuine reading, not skimming.

## §4 Implementation requirements

Follow execution rules in `python-replica/CLAUDE.md`. Specifically:

- **TDD**: tests first.
- **Immutability**: `FileSnapshot` MUST be `frozen=True`. `CompactSummary`'s
  new field MUST be an immutable tuple (not list). The deque itself on
  AgentLoop IS mutable (it's a deque, that's its purpose), but each
  snapshot inside it is immutable.
- **File limits**: ≤50 lines per function, ≤800 per file.
- **Determinism in tests**: `MockProvider`, `tmp_path`, `monkeypatch`.
  Capture timestamps via `time_machine` or `freezegun` IF the project
  already uses them; otherwise use a `now: Callable[[], datetime]`
  injection like the codebase already does in other places.
- **No swallowed exceptions**.
- **No `git add -A`**.
- **Newest-wins per-path eviction in the deque**: writing a fresh
  snapshot for path `p` when an entry for `p` already exists MUST
  remove the old entry first so the deque never has duplicate paths.
  Cover this with a test.
- **Compact-time snapshot consistency**: the snapshot deque is read
  once at the start of `_force_compact()` and passed in; subsequent
  read_file calls during the same compact run do NOT affect the
  current compact's snapshots. Cover this with a test.

After every meaningful diff, run:

```
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

All three must be green before you commit.

## §5 Exit ritual (MANDATORY)

### 1. Confirm exit gate met (objectively)

Quote each AND-clause from §2's exit gate verbatim and show command
output that proves it.

### 2. Commit with explicit paths

Never `git add -A`. The commit subject MUST start with `[ctx-pdf/M3]`.

### 3. Append a block to `initiatives/current/PROGRESS.md`

```
## M3 — done 2026-05-23

- commit: <sha> [ctx-pdf/M3] <subject>
- tests: <m2-after> -> <m3-after> (+N)
- mypy: clean | ruff: clean
- files changed: `<file1>`, `<file2>`, ...
- exit gate: <quote from §2> -> PASS (<one-line evidence>)
- notes: <optional, ≤1 line>
```

### 4. Rewrite `initiatives/current/HANDOFF.md`

APPEND a `### M3` subsection to Section 2. Preserve M1 and M2 verbatim.

**Section 4 "Important constraints"**: M3 freezes that
`CompactSummary.recent_file_snapshots` is part of the CompactSummary
contract — M4 must not break it. Also: ATTACHMENT messages MUST stay
filtered-IN (i.e., pass through API serialization, unlike
COMPACT_BOUNDARY and SNIP_BOUNDARY which are filtered out).

**Section 5 "Next milestone guidance"**: write for M4 — model-driven
`snip_history` tool + `<msg uuid="...">` serialization + 10k-token
nudge. Likely-touched files: new `snip_tool_model.py`,
`tool_registry_factory.py`, `loop.py`, `context.py`. Note any surprises
from M3 about how `build()` interacts with attachment placement that
M4 should be aware of.

---

Do not skip any step. The 6-check exit-gate verification runs after
commit. Check 6 (append-only contract) now requires M1's AND M2's
PROGRESS blocks + HANDOFF subsections to still exist verbatim.
