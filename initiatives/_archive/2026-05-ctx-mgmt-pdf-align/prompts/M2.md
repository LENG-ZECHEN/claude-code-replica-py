# ctx-mgmt-pdf-align — M2: engine-snip-orphan-and-ancient-pairs

You are an autonomous agent executing milestone **M2** of the
**ctx-mgmt-pdf-align** initiative. This session was launched by
`automation/scripts/run_all_milestones.sh`. There is no user available to
ask — every decision must be made and documented now.

When you are done, the shell loop will start the next milestone if (and
only if) you have honored the exit ritual in §5 below.

## §1 Baseline (verify before any code change)

- Last commit: `8f1d98f` (initiative baseline; M1's commit comes after
  and is your real starting point — re-verify by reading HANDOFF.md
  Section 3, which M1 will have rewritten with its post-commit numbers)
- pytest: 615 passing (baseline; after M1 expect 625+; HANDOFF.md
  Section 3 has the post-M1 count)
- mypy: clean
- ruff: clean

Verify with:

```
git -C python-replica log --oneline -3
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

If the numbers do not match, **stop and report**. Do not start work
against a different baseline.

## §2 Scope (what THIS milestone implements)

- **Phase IDs**: B1, B2, B3
- **Exit gate (must be objectively verifiable)**:
  pytest --tb=no -q passes with count grown by >=12 from M1-post baseline
  AND
  SnipTool.snip() deletes (not folds) tool_use blocks whose paired
  tool_result is missing AND tool_result blocks whose paired tool_use
  is missing (taking over orphan handling from
  `context._remove_orphan_tool_results` for the snip phase) AND
  SnipTool.snip() deletes paired (tool_use, tool_result) blocks when
  their tool_result content equals `CLEARED_TOOL_RESULT_CONTENT` and
  the estimated tokens of all such cleared placeholders >= configurable
  threshold (`ancient_cleared_threshold_tokens`, default 10_000) AND
  every snip() invocation that actually deleted inserts exactly one
  `SNIP_BOUNDARY` marker at the position of the earliest deletion
  (new `MessageType.SNIP_BOUNDARY`, `Message.snip_boundary()`
  classmethod, `is_meta=True`, filtered out of
  `context._normalize_messages()` like `COMPACT_BOUNDARY` is).
- **Expected files to touch**:
  - `src/simple_coding_agent/snip.py` — extend SnipTool.snip() and
    should_snip() with orphan-delete and ancient-cleared-delete phases;
    add SNIP_BOUNDARY insertion
  - `src/simple_coding_agent/models.py` — add `MessageType.SNIP_BOUNDARY`
    and `Message.snip_boundary()` classmethod
  - `src/simple_coding_agent/context.py` — extend the COMPACT_BOUNDARY
    filter in `_normalize_messages()` (and any other API-serialization
    site) to also filter SNIP_BOUNDARY
  - `tests/test_snip.py` — +>=12 new cases for orphan delete, ancient
    pair delete, threshold boundary, SNIP_BOUNDARY insertion + filter

PDF reference: §3 snip "真删除 — 不留占位 + snip_boundary marker"
(engine-side, not the model-driven variant in M4).

Snip remains hard-coded and engine-driven at the current call site
(`loop.py:215 _maybe_snip()`) — the user has confirmed this position
stays. What changes: snip's responsibility expands from "fold redundant
fresh tool results" to also "garbage-collect orphans and ancient
cleared pairs that microcompact left behind".

Token threshold computation: iterate messages, sum
`ContextBudget.estimate_tokens(json.dumps(m))` for every message whose
content list contains a ToolResult with content ==
`CLEARED_TOOL_RESULT_CONTENT`, trigger pair-wise deletion when sum >=
threshold. Delete oldest pairs first until sum drops below threshold.
Removing a tool_use that empties its parent assistant message also
removes that message.

Existing P8 fold-only behaviour for fresh (non-cleared, non-orphan)
results MUST remain intact — all current `tests/test_snip.py` cases must
keep passing without modification.

`SnipTool.should_snip()` gains a new True branch for "cleared tokens >=
threshold" alongside the existing path-count >=3 and pair-count >=10
branches.

Scope is **tight**. Doing more than this is failure. §2.5 below lists
what you must NOT do even if it seems natural.

## §2.5 Out of scope (do NOT do these, even if tempting)

- **Do not implement any other milestone** in this initiative. Stop at
  `M2`. M3 will touch `loop.py` for recent-file snapshots; M4 adds a
  new tool. Leave them alone.
- **Do not refactor unrelated modules.** Touch only files in the
  Expected-files list (§2), plus tests for those files.
- **Do not modify** the harness (`automation/`).
- **Do not modify** `CLAUDE.md` / `README.md` unless this milestone
  exposes a new public symbol — Tier A in the review session handles
  the SNIP_BOUNDARY entry automatically.
- **Do not remove** `context._remove_orphan_tool_results()`. It stays
  in place as defence-in-depth even after snip absorbs primary orphan
  handling. (HANDOFF Section 4 invariant.)
- **Do not change** the engine snip call site (`loop.py:215
  _maybe_snip()`). Position stays.
- **Do not introduce model-callable behaviour.** The model-driven
  `snip_history` tool is M4's job. M2 stays hardcoded.
- **Do not remove or relax** the `compact_threshold` second-trigger
  path that M1 preserved — invariant from HANDOFF Section 4.
- **Do not introduce new dependencies** in `pyproject.toml`.
- **Do not touch** `src/simple_coding_agent/tool_result_store.py`.

If you find an obvious bug or improvement outside scope, **write it in
your HANDOFF.md Section 5 and move on**.

## §3 Mandatory reading (in order, before any code change)

1. **`python-replica/CLAUDE.md`** — architecture, per-file summaries,
   project-wide execution rules.
2. **`python-replica/initiatives/current/PLAN.md`** — this initiative's
   full brief. Section "Background / motivation" explains why snip
   becomes a 4-phase pipeline (fold + orphan-delete + ancient-pair-delete
   + boundary).
3. **`python-replica/initiatives/current/config.yaml`** — confirms
   `exit_gate` + `phase_ids` for `M2`.
4. **`python-replica/initiatives/current/HANDOFF.md`** — current state
   AFTER M1. Section 2 "Completed milestones" → "### M1" tells you what
   M1 actually shipped (may differ from PLAN). Section 4 "Important
   constraints" lists invariants you MUST respect — particularly any
   M1 added about preserving `compact_threshold` legacy path. Section 5
   "Next milestone guidance" is written FOR you.
5. **`python-replica/initiatives/current/PROGRESS.md`** — terse fact log
   of M1 (commit, test delta, files touched).
6. **The Expected-files list from §2** — read each source file BEFORE
   editing it:
   - `src/simple_coding_agent/snip.py` — note `SnipTool` (lines 66-221),
     `SNIPPED_CONTENT`, `KEEP_LATEST_PER_PATH`, `KEEP_LATEST_GLOBAL`,
     `_PATH_THRESHOLD = 3`, `_TOTAL_PAIR_THRESHOLD = 10`. The existing
     `_positions_to_snip()` is the model for your new phase logic.
   - `src/simple_coding_agent/models.py` — note `MessageType` enum
     (lines 41-52, includes `COMPACT_BOUNDARY = "compact_boundary"`)
     and `Message.compact_boundary()` (lines 141-156) — mirror that
     pattern for SNIP_BOUNDARY.
   - `src/simple_coding_agent/context.py` — note where COMPACT_BOUNDARY
     is filtered in `_normalize_messages()` (around line 89). The same
     filter site needs SNIP_BOUNDARY added. Also check
     `_remove_orphan_tool_results()` (134-164) — confirm M2 leaves it
     untouched (HANDOFF invariant).
   - `src/simple_coding_agent/compact.py` — note `CLEARED_TOOL_RESULT_CONTENT`
     (line 44) — this is the string your ancient-pair detector matches.
   - `tests/test_snip.py` — note the existing 13 cases. They must keep
     passing without modification.
7. **Since M2 is not M1**: run `git -C python-replica log --oneline -10`
   and `git -C python-replica show HEAD~1` to see what M1 actually
   changed. (`HEAD~1` is M1's commit because each milestone commits
   exactly once before the loop advances; if you suspect that invariant
   has been violated, use `git -C python-replica log
   8f1d98f01f764a32df538b6319115d392696ab4e..HEAD --oneline | grep -F
   "[ctx-pdf/" | head -1` instead and `git -C python-replica show <sha>`
   to inspect that SHA. The `-C python-replica` is required because the
   autonomous loop launches `claude --print` from `/Users/leng/my-cc-py`,
   not from inside the repo. The `grep -F` is required because the
   literal `[` would otherwise be parsed as an unclosed character
   class. The `8f1d98f...HEAD` range matches what
   `run_all_milestones.sh` uses, so the fallback stays consistent.)

**Before invoking any Edit or Write tool**, print a **5-bullet summary**
of what you learned from PLAN / HANDOFF / PROGRESS / M1's commit / the
existing snip.py. If sources disagree, treat HANDOFF as advisory and
`git diff` + test output as source of truth.

## §4 Implementation requirements

Follow execution rules documented in `python-replica/CLAUDE.md`. In
particular:

- **TDD**: write tests first (RED), implement (GREEN), refactor. Every
  new code path needs a failing test before the implementation lands.
- **Immutability**: `SnipTool.snip()` already returns a new list — keep
  that contract. The new `MessageType.SNIP_BOUNDARY` and
  `Message.snip_boundary()` mirror the existing COMPACT_BOUNDARY
  pattern (immutable factory).
- **File limits**: functions ≤50 lines, files ≤800 lines. `snip.py` is
  ~221 lines today; the new phase logic should stay well under 500.
  If a single function grows past 50 lines, extract sub-functions.
- **Determinism in tests**: `MockProvider`, `tmp_path`, `monkeypatch`.
  No network. No real API key.
- **No swallowed exceptions**.
- **No `git add -A`** — stage explicit paths only.
- **Idempotency**: applying snip a second time to the same already-snipped
  transcript must be a noop (no double SNIP_BOUNDARY insertion). Cover
  this with a test.
- **Pair-deletion safety**: removing a tool_use that empties its parent
  assistant message also removes that message. Cover the edge case where
  the assistant message has BOTH text content AND tool_use blocks — only
  drop the tool_use, keep the text.

After every meaningful diff, run:

```
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

All three must be green before you commit.

## §5 Exit ritual (MANDATORY)

After your milestone work passes the exit gate in §2, before stopping,
perform these steps **in order**.

### 1. Confirm exit gate met (objectively)

Quote each AND-clause from §2's exit gate verbatim and show the
command output that proves it.

"Implementation feels complete" is NOT acceptable.

### 2. Commit with explicit paths

Never `git add -A`. The commit subject MUST start with `[ctx-pdf/M2]`:

```
git -C python-replica add <list each modified/new file>
git -C python-replica commit -m "[ctx-pdf/M2] <one-line summary>"
```

**Do NOT `git commit --amend` to embed this commit's own SHA into
PROGRESS.md / HANDOFF.md** (see M1 prompt for the canonical
two-commit pattern if you need the SHA).

### 3. Append a block to `initiatives/current/PROGRESS.md`

Block shape:

```
## M2 — done 2026-05-23

- commit: <sha> [ctx-pdf/M2] <subject>
- tests: <m1-after> -> <m2-after> (+N)
- mypy: clean | ruff: clean
- files changed: `<file1>`, `<file2>`, ...
- exit gate: <quote from §2> -> PASS (<one-line evidence>)
- notes: <optional, ≤1 line>
```

### 4. Rewrite `initiatives/current/HANDOFF.md`

Use the 5-section structure. APPEND a `### M2` subsection to Section 2 —
do NOT delete or rewrite M1's `### M1` subsection. Preserve everything
M1 wrote in Section 4 invariants and append your own.

**Section 4 "Important constraints"**: M2 freezes that
`MessageType.SNIP_BOUNDARY` is filtered alongside COMPACT_BOUNDARY in
every API-serialization site; M3 and M4 must not introduce new
serialization paths that skip the filter. Also record:
`_remove_orphan_tool_results()` remains live as backstop — do not remove
in M3 or M4.

**Section 5 "Next milestone guidance"**: write for M3 — recent-files
attachment on autoCompact. Likely-touched files: `loop.py` (capture
hook), `compact.py` (CompactSummary new field), `context.py` (ATTACHMENT
filter pass-through), `models.py` (verify ATTACHMENT type already
exists). Note any surprises about how the snip phase interacts with
ContextBuilder's orphan pass that M3 should be aware of.

---

Do not skip any step. The 6-check exit-gate verification (see M1 prompt
for the full enumeration) runs after your commit. Check 6 (append-only
contract) requires both M1's PROGRESS block and `### M1` HANDOFF
subsection to still exist verbatim after your write.
