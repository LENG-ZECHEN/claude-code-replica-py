# ctx-mgmt-pdf-align — M4: model-driven-snip-tool

You are an autonomous agent executing milestone **M4** of the
**ctx-mgmt-pdf-align** initiative. This is the **LAST milestone** of
the initiative. After your exit ritual succeeds, the script will
launch the review + wrap-up session. This session was launched by
`automation/scripts/run_all_milestones.sh`. There is no user available to
ask — every decision must be made and documented now.

## §1 Baseline (verify before any code change)

- Last commit: `8f1d98f` (initiative baseline; M3's commit is your real
  starting point — re-verify by reading HANDOFF.md Section 3, which M3
  will have rewritten)
- pytest: 615 passing (baseline; after M1+M2+M3 expect 645+; HANDOFF.md
  Section 3 has the post-M3 count)
- mypy: clean
- ruff: clean

Verify with:

```
git -C python-replica log --oneline -3
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

If the numbers do not match, **stop and report**.

## §2 Scope (what THIS milestone implements)

- **Phase IDs**: D1, D2, D3
- **Exit gate (must be objectively verifiable)**:
  pytest --tb=no -q passes with count grown by >=15 from M3-post baseline
  AND
  A new `snip_history` tool is registered by
  `tool_registry_factory.build_default_registry()` with
  `input_schema = {"type": "object", "properties": {
    "message_uuids": {"type": "array", "items": {"type": "string"}}
  }, "required": ["message_uuids"]}` AND
  Invoking `snip_history` with a list of message uuids removes those
  messages from the AgentLoop's live Transcript (via
  `Transcript.replace_all(filtered)`) and returns a string
  `"Snipped <N> messages"` AND
  `context._normalize_messages()` wraps each user-role tool_result
  message body with `<msg uuid="<uuid>">...</msg>` so the model sees
  and can target message uuids AND
  AgentLoop tracks `_tokens_since_last_snip` updated after every
  `_handle_tool_calls()` return; when growth >= configurable
  `snip_nudge_growth_tokens` (default 10_000), the next
  `ContextBuilder.build()` call prepends an `is_meta=True`
  system-reminder user message describing the `snip_history` tool and
  listing the uuids of currently-snippable messages AND
  `_tokens_since_last_snip` resets on (a) any snip — engine or model —
  and (b) any full compact AND
  Once `reactive_compact_attempted` flips True during a loop, the nudge
  is suppressed for the lifetime of that AgentLoop instance.
- **Expected files to touch**:
  - `src/simple_coding_agent/snip_tool_model.py` — NEW. Exports
    `register_snip_history_tool(registry, transcript)` helper.
  - `src/simple_coding_agent/tool_registry_factory.py` — call the new
    helper inside `build_default_registry()`.
  - `src/simple_coding_agent/loop.py` — `_tokens_since_last_snip`
    state; nudge-arm logic after `_handle_tool_calls()`;
    `_snip_nudge_suppressed` flag set after reactive compact.
  - `src/simple_coding_agent/context.py` — `<msg uuid="...">` wrap in
    `_normalize_messages()`; `snip_nudge` kwarg on
    `ContextBuilder.build()`; nudge prepend logic.
  - `tests/test_snip_tool_model.py` — NEW. Cover tool registration,
    schema, valid uuid deletion, refuse-recent, refuse-non-tool_result,
    refuse-future, transcript closure semantics, return string format.
  - `tests/test_loop.py` — token-growth tracking, nudge arming, nudge
    suppression after reactive, reset after snip / compact.
  - `tests/test_context.py` — uuid wrap serialization, nudge prepend
    ordering, nudge body lists candidate uuids only.

PDF reference: §3 snip model-driven SnipTool + shouldNudgeForSnips
("every ~10k tokens").

User clarified: input schema uses `message_uuids: list[str]` (not
tool_use_ids), and the growth check runs after each tool-call iteration
(not just at turn boundaries).

Implementation location: new file
`src/simple_coding_agent/snip_tool_model.py` exporting a
`register_snip_history_tool(registry, transcript)` helper that
`tool_registry_factory.build_default_registry()` calls. The registered
tool fn captures the live Transcript by closure; pairing with the
AgentLoop's transcript happens because both the registry and the
AgentLoop are built from the same Transcript instance in
`_build_repl_loop`.

Tool description must explicitly tell the model:
- "Each tool_result you have seen is wrapped in
  `<msg uuid='...'>...</msg>`; pass any of those uuids."
- "You can only snip past tool_result messages; you cannot snip your
  own most recent response or the user's current turn."

Tool fn validates: each uuid exists in transcript, the matching message
is a tool_result-bearing user message (`is_meta=True` AND content list
has only ToolResult blocks), refuses to snip the most recent N (default
5) tool_result messages, refuses to snip any message later than the
latest user-text message. Refusals return `"snip refused: <reason>"`
and bump `is_error=True` so the model sees the failure.

Nudge injection: `ContextBuilder.build()` gains a new optional kwarg
`snip_nudge: SnipNudge | None = None`; when present, prepends one
`is_meta=True` message before the first kept message. AgentLoop computes
the `SnipNudge` when `_tokens_since_last_snip >= snip_nudge_growth_tokens`
AND `not _snip_nudge_suppressed`. The SnipNudge body lists candidate
uuids; if no candidates exist, no nudge is built (silent skip).

Scope is **tight**. Doing more than this is failure. §2.5 below lists
what you must NOT do even if it seems natural.

## §2.5 Out of scope (do NOT do these, even if tempting)

- **Do not implement any other milestone** — M4 is the last.
- **Do not refactor unrelated modules.** Touch only files in the
  Expected-files list (§2), plus tests for those files.
- **Do not modify** the harness (`automation/`).
- **Do not modify** `CLAUDE.md` / `README.md` — Tier A in the review
  session handles the new public symbols (`snip_history` tool, etc.).
- **Do not remove** the `compact_threshold` legacy second-trigger path
  (M1 invariant).
- **Do not remove** `context._remove_orphan_tool_results()` (M2
  invariant).
- **Do not change** ATTACHMENT filtering behaviour from M3 (M3
  invariant: ATTACHMENT passes through API serialization).
- **Do not** change the engine snip call site (`loop.py:215`) — M2's
  engine snip and M4's model snip COEXIST. Both are needed; neither
  replaces the other.
- **Do not** put the `snip_history` tool's implementation in
  `snip.py` — that file is for the engine-side SnipTool. The new
  file `snip_tool_model.py` is required to keep the two surfaces
  separate.
- **Do not** use any provider-specific metadata field (like an OpenAI
  `metadata` key) to expose uuids — `OpenAI Chat Completions` strips
  those. The `<msg uuid="...">` content-wrap is the only sanctioned
  surface.
- **Do not** make the nudge cadence configurable per provider — one
  global `snip_nudge_growth_tokens` threshold (default 10_000) on
  AgentLoop, period.
- **Do not introduce new dependencies** in `pyproject.toml`.
- **Do not touch** `src/simple_coding_agent/tool_result_store.py`.

If you find an obvious bug or improvement outside scope, **write it in
your HANDOFF.md Section 5 and move on**.

## §3 Mandatory reading (in order, before any code change)

1. **`python-replica/CLAUDE.md`** — architecture, per-file summaries.
2. **`python-replica/initiatives/current/PLAN.md`** — full brief.
   Re-read the "Risks / known unknowns" entry about OpenAI Chat
   Completions stripping per-message metadata — that's why the uuid
   surface MUST be content-wrapped.
3. **`python-replica/initiatives/current/config.yaml`** — confirms
   `exit_gate` + `phase_ids` for `M4`.
4. **`python-replica/initiatives/current/HANDOFF.md`** — Section 2
   "Completed milestones" → `### M1`, `### M2`, `### M3` tell you what
   shipped. Section 4 lists ALL invariants from prior milestones. The
   ATTACHMENT-filter-pass-through invariant from M3 is especially
   relevant — M4's nudge messages must be ATTACHMENT-shaped or
   user-role; ensure they don't accidentally get filtered out the way
   COMPACT_BOUNDARY does.
5. **`python-replica/initiatives/current/PROGRESS.md`** — fact log for
   M1, M2, M3.
6. **The Expected-files list from §2** — read each source file BEFORE
   editing:
   - `src/simple_coding_agent/tool_registry_factory.py` — note
     `build_default_registry(workspace)` (lines 37-174), how
     `registry.register(Tool(...))` is called for each of the 5
     existing tools, the lambda-binding pattern for capturing
     workspace.
   - `src/simple_coding_agent/tools.py` — `Tool` dataclass (lines
     56-68), `ToolRegistry` (75-106), `ToolExecutor.execute()`
     (113-139).
   - `src/simple_coding_agent/loop.py` — `_handle_tool_calls()` (around
     600-624), `_execute_one()` (626-654), `reactive_compact_attempted`
     guard (around 240).
   - `src/simple_coding_agent/context.py` — `_normalize_messages()`
     (where tool_result messages are serialized — your uuid wrap goes
     here), `ContextBuilder.build()` (203-243, where your nudge
     prepend integrates).
   - `src/simple_coding_agent/transcript.py` — `Transcript.replace_all()`
     and `all_messages()`.
   - `src/simple_coding_agent/models.py` — `Message.uuid` field (each
     message has a stable uuid from `Message.__init__`).
7. **Since M4 is not M1**: run `git -C python-replica log --oneline -10`
   and `git -C python-replica show HEAD~1` (= M3's commit). Use the
   `8f1d98f01f764a32df538b6319115d392696ab4e..HEAD --oneline | grep -F
   "[ctx-pdf/"` fallback if HEAD~1 isn't M3's commit.
8. **Since M4 is > M2**: skim
   `initiatives/current/logs/M3.log` and `M2.log` for any anomalies
   that didn't make it into HANDOFF Section 2 design-decisions.

**Before invoking any Edit or Write tool**, print a **5-bullet summary**.

## §4 Implementation requirements

Follow execution rules in `python-replica/CLAUDE.md`. Specifically:

- **TDD**: tests first. The new file `snip_tool_model.py` and its
  test `tests/test_snip_tool_model.py` MUST both land in this commit;
  the test file written first.
- **Immutability**: the `SnipNudge` dataclass MUST be `frozen=True`.
- **File limits**: ≤50 lines per function, ≤800 per file.
- **Determinism in tests**: `MockProvider`, `tmp_path`, `monkeypatch`.
  No network. No real API key. The `snip_history` tool's transcript
  closure must be testable without spinning up an AgentLoop — extract
  the validation logic into a pure function callable from tests.
- **No swallowed exceptions**.
- **No `git add -A`**.
- **Closure ownership**: `register_snip_history_tool(registry,
  transcript)` captures `transcript` by reference. Both `_build_repl_loop`
  in `cli.py` and `_run_openai_repl` in `openai_cli.py` MUST be updated
  so the registry's transcript is the SAME instance the AgentLoop
  holds. If they currently aren't, change the wiring order. Cover with
  an integration test that calls `snip_history` and asserts the
  AgentLoop's `_transcript.all_messages()` reflects the deletion.
- **Tool description text** must be ≥150 characters and explicitly
  document the `<msg uuid="...">` wrap convention, the refuse-recent
  rule, and the refuse-non-tool_result rule.
- **Suppression-after-reactive**: when `reactive_compact_attempted`
  flips True in `loop.py`, set `_snip_nudge_suppressed = True` in the
  same code path. Cover with a test that reactive compact fires once
  and the subsequent build() has no nudge prepended even if the token
  growth exceeded threshold.

After every meaningful diff, run:

```
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

All three must be green before you commit.

## §5 Exit ritual (MANDATORY) — last milestone

### 1. Confirm exit gate met (objectively)

Quote each AND-clause from §2's exit gate verbatim and show command
output that proves it. M4 has 7 AND-clauses — show evidence for each.

### 2. Commit with explicit paths

Never `git add -A`. The commit subject MUST start with `[ctx-pdf/M4]`.

### 3. Append a block to `initiatives/current/PROGRESS.md`

```
## M4 — done 2026-05-23

- commit: <sha> [ctx-pdf/M4] <subject>
- tests: <m3-after> -> <m4-after> (+N)
- mypy: clean | ruff: clean
- files changed: `<file1>`, `<file2>`, ...
- exit gate: <quote from §2> -> PASS (<one-line evidence>)
- notes: <optional, ≤1 line>
```

### 4. Rewrite `initiatives/current/HANDOFF.md`

APPEND a `### M4` subsection to Section 2. Preserve M1, M2, M3 verbatim.

**Section 4 "Important constraints"**: M4 is the last milestone — the
invariants you record here matter for the review session and future
initiatives.

**Section 5 "Next milestone guidance"**: M4 is the last, so write this
section as: "Initiative complete. Next agent is the review session.
Audit focus areas: (a) does the snip_history tool actually work
end-to-end with OpenAIProvider via openai_cli; (b) does the
`<msg uuid="...">` wrap survive OpenAI's serialization without
truncation; (c) do all 4 milestones' invariants in Section 4 still
hold."

### 5. Mark PLAN.md STATUS as complete (LAST MILESTONE ONLY)

This step is M4-only. Add a single line to
`initiatives/current/PLAN.md`'s provenance block (just below the existing
`> Bootstrapped on ...` line):

```
> STATUS: complete (M1–M4 shipped on YYYY-MM-DD)
```

Use the actual completion date (YYYY-MM-DD). Insert directly after the
existing `> Bootstrapped on ...` line. This is what the review session
checks to know the initiative finished cleanly.

---

Do not skip any step. The 6-check exit-gate verification runs after
commit. Check 6 (append-only contract) requires M1, M2, AND M3's
PROGRESS blocks + HANDOFF subsections to still exist verbatim.

After your exit ritual, the script automatically launches the review +
wrap-up session that audits everything, writes
`initiatives/current/REVIEW.md`, applies Tier A / B doc edits, and
archives the whole `initiatives/current/` into
`initiatives/_archive/2026-05-ctx-mgmt-pdf-align/`. You do not start
that session — it starts itself.
