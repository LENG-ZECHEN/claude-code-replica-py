# session-memory-dream — M4: SM-compact observability + dual-arm latency benchmark

You are an autonomous agent executing milestone **M4** of the
**session-memory-dream** initiative. This session was launched by
`automation/scripts/run_all_milestones.sh`. There is no user available to
ask — every decision must be made and documented now.

When you are done, the shell loop will start the next milestone if (and
only if) you have honored the exit ritual in §5 below.

## §1 Baseline (verify before any code change)

- Last commit: `094cf90d09fef37b3f8357b4e2b8de0434834dfd`
- pytest: 912 passing (+1 xpassed)
- mypy: clean (mypy src → no issues in 30 source files)
- ruff: clean (ruff check . → All checks passed!)

Verify with:

```
git -C python-replica log --oneline -3
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

If the numbers do not match, **stop and report**. Do not start work
against a different baseline. M1, M2, and M3 will already have committed
ahead of this baseline by the time M4 runs — that is expected (see §3
step 7); the baseline above is the *initiative* baseline, and the live
`pytest`/`HEAD` numbers will be HIGHER. What must match is HANDOFF.md
Section 3 (the state M3 left), not the line above. Re-read HANDOFF
Section 3 for the real pre-M4 numbers and use those as your `<before>`
in PROGRESS / HANDOFF.

## §2 Scope (what THIS milestone implements)

- **Phase IDs**: [S4]
- **Exit gate (must be objectively verifiable)**:

  MetricsCollector gains `sm_compact_reuses` and `sm_compact_misses`
  (record_* methods + format_stats lines) surfaced via the REPL /stats
  command. The reuse/miss signal is emitted on the EXISTING 'compact'
  trace channel as a `reused=<bool>` field (NO new channel — the 11-name
  vocab is frozen and test-pinned; assert the StderrTracer line).
  New benchmarks/bench_sm_compact_latency.py runs headless and writes
  benchmarks/_results/04_sm_compact_latency.{json,md} with TWO clearly
  labeled arms: (a) DETERMINISTIC — RuleBasedSummarizer real recompute
  vs O(0) SessionMemorySummarizer reuse, no API, fully reproducible
  (the defensible floor); (b) REAL-API (gated behind --confirm-api-call
  + key, like bench_openai_cost.py) — measured LLMSummarizer wall-clock
  vs ~0 reuse on DashScope qwen-plus-latest. JSON includes raw per-run
  perf_counter timings (median + p90 over R≥50) and a `latency_source`
  field disclosing where each number came from. tests/test_bench_sm_compact.py
  asserts measured_reuse_ms < full_arm_ms with a tiny injected delay so
  CI stays fast. pytest grows by ≥ 7.

- **Expected files to touch**:

  - MODIFIED: `src/simple_coding_agent/metrics.py` — add `sm_compact_reuses`
    and `sm_compact_misses` int fields, `record_sm_compact_reuse()` /
    `record_sm_compact_miss()` methods, and two lines in `format_stats()`
    (extend the existing block — see metrics.py:125-147; mirror the
    `record_full_compact` / `record_todo_write` shape exactly).
  - MODIFIED: `src/simple_coding_agent/loop.py` — emit `reused=<bool>` on
    the EXISTING `compact` trace channel and bump the new metric at the
    compaction fire site (`_force_compact`, loop.py:690-701). Note:
    `_force_compact` does NOT currently emit a `compact` trace line at
    all — M3 wired `SessionMemorySummarizer` here; M4 ADDS the
    `self._tracer.emit("compact", reused=<bool>, ...)` call beside the
    existing `self._metrics.record_full_compact()` (loop.py:698), choosing
    `reused=True` when the M3 SM warm-reuse path was taken (0 summarization
    provider calls) and `reused=False` when it fell back to full
    Rule/LLM compaction. Use whatever M3 left as the in-loop signal for
    "warm reuse happened" — read M3's diff (§3 step 7) to find it; do NOT
    invent a parallel detection path.
  - MODIFIED: `src/simple_coding_agent/cli.py` — `/stats` already prints
    `MetricsCollector.format_stats()`, so the new counters surface
    automatically once they are in `format_stats()`. Touch cli.py ONLY if
    `/stats` needs an explicit new line; prefer letting `format_stats()`
    carry them (no separate cli.py edit unless a test demands it).
  - NEW: `benchmarks/bench_sm_compact_latency.py` — the dual-arm
    benchmark (headless `main()` returning an exit code, same shape as
    `bench_compression_ratio.py`).
  - NEW: `benchmarks/_results/04_sm_compact_latency.json` and
    `benchmarks/_results/04_sm_compact_latency.md` — committed artifacts
    from running the DETERMINISTIC arm (the real-API arm is opt-in and
    does NOT run in the loop; commit the deterministic-floor results).
  - NEW: `tests/test_bench_sm_compact.py` — asserts
    `measured_reuse_ms < full_arm_ms` with a tiny injected delay (no real
    API, no real sleep beyond microseconds; keep CI fast).
  - MODIFIED: `tests/test_metrics_collector.py` — extend with cases for
    the two new counters + `format_stats()` lines (mirror the existing
    `test_metrics_counts_full_compact_invocations` at line 161 and the
    `format_stats` coverage pattern).

  **Honesty rules (this benchmark EXISTS to replace the never-existed
  "98.7%" claim — do not reintroduce a fabricated percentage):**
  - Every number discloses its source. Deterministic arm = reproducible
    floor; real-API arm = realistic headline; both labeled, never
    conflated. The JSON `latency_source` field is REQUIRED on every arm
    and must say exactly where the number came from (e.g.,
    `"deterministic: RuleBasedSummarizer recompute vs SessionMemorySummarizer reuse, perf_counter, no network"`
    vs `"live API: DashScope qwen-plus-latest LLMSummarizer wall-clock, drifts run-to-run"`).
  - Report it as "full summarize measured Xs (DashScope) → SM reuse
    ~0ms (median of N)", with the real arm flagged "live API, drifts
    run-to-run" exactly like bench3 (`bench_openai_cost.py`). NEVER
    present a simulated/deterministic number as if it were the real-API
    number, or vice versa.
  - **Template to follow** (deterministic, no-net, writes `_results/NN_*`):
    `benchmarks/bench_compression_ratio.py` — match its `main()` / `_run()`
    / `_render_markdown()` structure, its `_RESULTS_DIR` / `_JSON_PATH` /
    `_MD_PATH` module constants, and its `json.dumps(..., indent=2,
    sort_keys=True)` write convention.
  - **Real-API precedent** (DashScope, `--confirm-api-call` + key gate):
    `benchmarks/bench_openai_cost.py` — copy its gating EXACTLY:
    `--confirm-api-call` action flag (bench_openai_cost.py:476); refuse
    without it → **exit code 2** (line 498-503); refuse without
    `OPENAI_API_KEY` or `DASHSCOPE_API_KEY` → **exit code 3** (line
    504-510); `_DASHSCOPE_BASE_URL = "https://dashscope.aliyuncs.com/compatible-mode/v1"`
    (line 114) as the default base; default model in the qwen family
    (the gate requires `qwen-plus-latest` per §2). The deterministic arm
    runs unconditionally; the real-API arm runs ONLY when both
    `--confirm-api-call` and a key are present.

  **Source mapping (read BEFORE implementing — these tie every claim to
  TS source):**
  - `claude-code-source-code/src/services/compact/sessionMemoryCompact.ts:498`
    — "SM-compact has no compact-API-call" — the confirmed basis for the
    time saving. The benchmark MEASURES this saving honestly; it does not
    assert a magic percentage.
  - `claude-code-source-code/src/services/compact/sessionMemoryCompact.ts:58-60`
    — `DEFAULT_SM_COMPACT_CONFIG` (minTokens=10_000,
    minTextBlockMessages=5, maxTokens=40_000) — context for what scale
    the SM path operates at; seed the benchmark transcript large enough
    that the full arm does real work.
  - `claude-code-source-code/src/services/compact/compact.ts:1136`
    streamCompactSummary — the LLM call the SM warm path SKIPS (this is
    the latency the real-API arm measures).

  **The 11-channel trace vocabulary is FROZEN and test-pinned** — see
  `trace.py` per-file summary in CLAUDE.md and the pinning tests in
  `tests/test_trace.py` (`test_stderr_tracer_accepts_locked_channels`
  line 145, `test_nine_channel_format_unchanged_for_scalar_values` line
  469). Reuse the `compact` channel; do NOT add a 12th channel. Adding a
  channel will fail those tests. `reused` is a new *field* on an existing
  channel, which is allowed (fields are free-form; channels are frozen).
  When you assert the StderrTracer line in your test, match the locked
  format `[trace] [compact] ... reused=...` with keys sorted
  alphabetically (per trace.py's deterministic-key contract).

Scope is **tight**. Doing more than this is failure. §2.5 below lists
what you must NOT do even if it seems natural.

## §2.5 Out of scope (do NOT do these, even if tempting)

- **Do not implement any other milestone** in this initiative. Even if
  you "have time" or "see the obvious next step", stop at `M4`. In
  particular: M5 (`consolidation_lock`), M6 (`dream.py`), and M7 (dream
  CLI) are NOT yours. Do not create `consolidation_lock.py`, `dream.py`,
  or a `dream` CLI subcommand.
- **Do not refactor unrelated modules.** Touch only files in the
  Expected-files list (§2), plus tests for those files.
- **Do not add a new trace channel.** The 11-name vocabulary is frozen
  and test-pinned. Emit `reused=<bool>` as a FIELD on the existing
  `compact` channel only.
- **Do not reintroduce a fabricated percentage** anywhere (code,
  docstring, JSON, markdown). The whole point of this milestone is to
  replace the never-existed "98.7%" with disclosed, reproducible numbers.
  No "~99%", no rounded headline %, no hardcoded ratio that is not
  computed from measured `perf_counter` timings.
- **Do not call a real API in the test suite or in the loop-committed
  benchmark run.** `tests/test_bench_sm_compact.py` must be deterministic
  (MockProvider / injected tiny delay, no network, no real key). The
  committed `_results/04_*.{json,md}` come from the DETERMINISTIC arm
  only. The real-API arm is opt-in (`--confirm-api-call`) and never runs
  unattended.
- **Do not modify** `automation/scripts/run_all_milestones.sh`,
  `automation/RUNBOOK.md`, or `automation/templates/*.md` — those are
  the harness, not the project.
- **Do not modify** `CLAUDE.md` per-file summaries or `README.md` unless
  this milestone explicitly adds a new public symbol/CLI flag. The new
  `MetricsCollector` counters and the new benchmark file ARE new public
  symbols, but the end-of-initiative review session handles project-doc
  updates (per RUNBOOK Doc-update tiers); doing it here causes merge
  conflicts and wastes the review's audit. Record what changed in
  HANDOFF Section 2 instead.
- **Do not change public API signatures** of existing modules unless §2
  requires it. Adding new fields/methods to `MetricsCollector` is
  additive and allowed; do not change existing counter names or the
  `format_stats()` lines other milestones depend on (only APPEND lines).
- **Do not introduce new dependencies** in `pyproject.toml`. `perf_counter`
  is in the stdlib (`time`); statistics (median / p90) can be computed
  with `statistics.median` + a small percentile helper or `statistics.quantiles`
  — no numpy.

If you find an obvious bug or improvement outside scope, **write it in
your HANDOFF.md Section 5 ("risks" or as a deferred item) and move on**.
Do not fix it.

## §3 Mandatory reading (in order, before any code change)

1. **`python-replica/CLAUDE.md`** — architecture, per-file summaries,
   project-wide execution rules (TDD, immutability, file-size limits).
   Pay attention to the `metrics.py`, `trace.py`, and `loop.py`
   per-file summaries and the frozen-trace-vocabulary note.
2. **`python-replica/initiatives/current/PLAN.md`** — this initiative's
   full brief, including the milestone tables and the benchmark-honesty
   risk callout.
3. **`python-replica/initiatives/current/config.yaml`** — machine-readable
   milestone table; confirms `exit_gate` + `phase_ids` for `M4`.
4. **`python-replica/initiatives/current/HANDOFF.md`** — current state.
   Section 2 "Completed milestones" tells you what M1/M2/M3 shipped
   already (especially M3's SM-compact warm-reuse wiring in
   `_force_compact`). Section 3 has the REAL pre-M4 pytest count (use it
   as your `<before>`). Section 4 "Important constraints" lists invariants
   you MUST respect (the frozen trace vocab and the
   backward-compatible-`session_store` rules are already in there). Section
   5 "Next milestone guidance" is written FOR you.
5. **`python-replica/initiatives/current/PROGRESS.md`** — terse fact log
   of M1/M2/M3 (commit, test delta, files touched).
6. **The Expected-files list from §2** — read the source files this
   milestone will touch, plus their existing tests, before writing any
   code. Specifically:
   - `src/simple_coding_agent/metrics.py` (the whole file — it is small;
     mirror the `record_full_compact` + `format_stats` shape).
   - `src/simple_coding_agent/loop.py` `_force_compact` (lines 690-701)
     and the existing trace emit sites (lines 287, 456, 671, 868, 874)
     for the `self._tracer.emit(...)` calling convention.
   - `src/simple_coding_agent/trace.py` (the StderrTracer line format +
     `_render_value`) so your `reused=<bool>` field renders predictably.
   - `benchmarks/bench_compression_ratio.py` (deterministic template) and
     `benchmarks/bench_openai_cost.py` (real-API gating + DashScope base
     URL + exit codes 2/3).
   - `tests/test_metrics_collector.py` (the counter-test pattern) and
     `tests/test_trace.py` (the channel-pinning + no-raw-content tests).
   - The M3 source files that wired SM into compaction
     (`compact.py::SessionMemorySummarizer`, the M3 changes in `loop.py`)
     so you emit `reused` from the SAME signal M3 already computes.
7. **`M4` is not M1**: run `git -C python-replica log --oneline -10` and
   `git -C python-replica show HEAD~1` to see what the immediately prior
   milestone (M3) changed — M3's `SessionMemorySummarizer` wiring in
   `_force_compact` is the source of your `reused=<bool>` signal, so read
   that diff carefully. (`HEAD~1` is M3's commit because each milestone
   commits exactly once before the loop advances; if you suspect that
   invariant has been violated, use `git -C python-replica log
   094cf90d09fef37b3f8357b4e2b8de0434834dfd..HEAD --oneline | grep -F
   "[sm-dream/" | head -1` instead and `git -C python-replica show <sha>`
   to inspect that SHA. The `-C python-replica` is required because the
   autonomous loop launches `claude --print` from `/Users/leng/my-cc-py`,
   not from inside the repo. The `grep -F` is required because the literal
   `[` in the marker would otherwise be parsed as an unclosed character
   class (e.g. `[sm-dream/` triggers `grep: invalid character range`);
   `-F` forces fixed-string matching. The
   `094cf90d09fef37b3f8357b4e2b8de0434834dfd..HEAD` range matches what
   `run_all_milestones.sh` uses, so the fallback stays consistent with
   the resumability check.)
8. **`M4` > M2**: skim `initiatives/current/logs/M3.log` (and `M2.log`,
   `M1.log` if present) for any anomalies (warnings, retries,
   KeyboardInterrupts) that didn't make it into HANDOFF Section 2
   design-decisions — in particular any note about how M3 surfaced the
   warm-reuse-vs-fallback decision, since that is exactly the signal M4
   reads.

**Before invoking any Edit or Write tool**, print a **5-bullet summary**
to stdout (plain text, not a tool call, not a TaskCreate todo, not a
file write) of what you learned from PLAN / HANDOFF / PROGRESS / the
previous commit. If those sources disagree on a fact, treat HANDOFF as
advisory and `git diff` + test output as source of truth. This summary
lives only in the captured session log under `initiatives/current/logs/`
(not in any committed file) — its purpose is to force genuine reading
rather than skimming. If you cannot produce a substantive 5-bullet
summary, you have not read enough; go back to §3.

## §4 Implementation requirements

Follow execution rules documented in `python-replica/CLAUDE.md`. In
particular:

- **TDD**: write tests first (RED — must actually run and fail before
  implementation), implement (GREEN), refactor. Every new code path
  needs a test before the implementation lands. The two new
  `MetricsCollector` counters, the `reused=<bool>` trace field, and the
  `measured_reuse_ms < full_arm_ms` benchmark assertion each need a
  failing test before the code that satisfies it.
- **Immutability**: dataclasses prefer `frozen=True` where appropriate;
  never mutate inputs; return new objects. (`MetricsCollector` is the one
  intentionally-mutable counter dataclass — follow its existing
  write-once-per-event style; do not freeze it.)
- **File limits**: functions ≤50 lines, files ≤800 lines. The benchmark
  follows the small-helper decomposition of `bench_compression_ratio.py`
  (`_run`, `_render_markdown`, `main`) — split the deterministic arm and
  the real-API arm into separate functions.
- **Determinism in tests**: `MockProvider`, `tmp_path`, `monkeypatch`.
  No network. No real API key. The benchmark test injects a tiny delay
  (e.g., a fake summarizer that sleeps microseconds, or monkeypatched
  `perf_counter`) so `full_arm_ms > reuse_ms` is guaranteed without a
  real LLM call.
- **No swallowed exceptions**.
- **No `git add -A`** — stage explicit paths only.
- **Match style** of existing modules (see CLAUDE.md per-file summaries):
  `metrics.py` for counter shape, `bench_compression_ratio.py` for the
  benchmark scaffold, `bench_openai_cost.py` for the real-API gate.

After every meaningful diff, run:

```
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

All three must be green before you commit. Also run the new benchmark's
deterministic arm once to regenerate the committed artifacts:

```
cd python-replica && python -m benchmarks.bench_sm_compact_latency
```

(no `--confirm-api-call`, so the real-API arm stays dormant; only the
deterministic floor is written to `_results/04_*`).

## §5 Exit ritual (MANDATORY)

After your milestone work passes the exit gate in §2, before stopping,
perform these steps **in order**. The shell script independently
verifies the **6 side-effects** listed at the end of this section.
Step 1 below ("Confirm exit gate met") is **honor-system** — the script
cannot inspect whether you actually quoted the right command output, so
the mechanical checks are produced by steps 2-4 (commit, PROGRESS
append, HANDOFF rewrite) plus the standing pytest-green requirement
from §4. Skipping step 1 makes a green run meaningless even though it
would still pass the gate; do not skip it.

**Ordering is load-bearing**: PROGRESS.md (step 2) and HANDOFF.md (step 3)
MUST be written BEFORE the git commit (step 4), and all three land in
ONE `[sm-dream/M4]` commit. Reversing this fails exit-gate checks 1 & 2.

### 1. Confirm exit gate met (objectively)

Quote the concrete check from §2 verbatim and show the command output
that proves it. Example:

```
$ pytest tests/test_bench_sm_compact.py tests/test_metrics_collector.py --tb=no -q
.............  N passed in 0.5s

$ python -m benchmarks.bench_sm_compact_latency
[bench4] deterministic: full=X.XXXms -> reuse=0.00Xms (median of 50)
[bench4] artifacts: 04_sm_compact_latency.json, 04_sm_compact_latency.md

$ python -c "from simple_coding_agent.metrics import MetricsCollector; m=MetricsCollector(); m.record_sm_compact_reuse(); print('sm_compact_reuses' in m.format_stats())"
True
```

"Implementation feels complete" is NOT acceptable. The gate must be
objectively verified by a command's output. Confirm explicitly: pytest
grew by ≥ 7; `04_sm_compact_latency.{json,md}` exist with a
`latency_source` field on each arm; the deterministic arm shows
`measured_reuse_ms < full_arm_ms`; and a StderrTracer test asserts the
`reused=<bool>` field on the `compact` channel.

### 2. Append a block to `initiatives/current/PROGRESS.md`

**Write this BEFORE committing** so it can be staged in the same commit
as your code. PROGRESS.md is the **fact log for the final review** —
narrative belongs in HANDOFF, not here.

Block shape (use terse format from `automation/templates/progress_entry.md`):

```
## M4 — done 2026-06-15

- commit: [sm-dream/M4] (see git log)
- tests: <before> -> <after> (+N)
- mypy: clean | ruff: clean
- files changed: `<file1>`, `<file2>`, ...
- exit gate: <quote from §2> -> PASS (<one-line evidence>)
- notes: <optional, ≤1 line>
```

Use the REAL `<before>` from HANDOFF Section 3 (M3's post-count), not the
912 initiative baseline. Leave the SHA as `(see git log)` — you do not
know it yet and must NOT amend to fill it in (see note in step 4 below).

### 3. Rewrite `initiatives/current/HANDOFF.md`

Use the 5-section structure in `automation/templates/handoff_milestone.md`.
The structure is non-negotiable. The required verbatim headers are
`## 1. Current initiative`, `## 2. Completed milestones`, `## 3. Current
repo state`, `## 4. Important constraints`, `## 5. Next milestone
guidance`.

**Section 2 "Completed milestones"**: APPEND a new `### M4` subsection
under "## 2. Completed milestones". Do NOT delete or rewrite the
`### M1`, `### M2`, or `### M3` blocks — each milestone is the source of
truth on itself. (Exit-gate check 6 walks `094cf90d..HEAD`, finds every
prior `[sm-dream/M{i}]` commit, and verifies its `### M{i}` subsection
still exists. Rewriting from scratch with only `### M4` is a HARD
FAILURE.)

**Section 4 "Important constraints"**: APPEND any new invariant M4 froze.
At minimum, note that the `MetricsCollector.sm_compact_reuses` /
`sm_compact_misses` counter names and the `reused=<bool>` field on the
`compact` trace channel are now part of the observable surface — later
milestones must not rename them. Keep the prior constraints (frozen
trace vocab, backward-compatible session_store) verbatim.

**Section 5 "Next milestone guidance"**: write as if onboarding the next
agent (M5 — `consolidation_lock` + dream gate cascade) in person.
Include:
- next scope (paraphrase from PLAN + anything you learned in M4 that
  sharpens it — note that M5 is the start of the D-line and depends on
  M1's ForkedAgentRunner only indirectly; M5 itself is pure lock + gate
  infrastructure).
- relevant files (M5 will create `src/simple_coding_agent/consolidation_lock.py`
  and `tests/test_consolidation_lock.py`; point at the session-dir
  resolution helpers `resolve_sessions_dir` / `SIMPLE_AGENT_SESSIONS_DIR`
  in `session_store.py` that M5's `list_sessions_touched_since` reuses).
- expected tests (`tests/test_consolidation_lock.py`, ≥ 8 cases, all
  timestamps injected via `os.utime` + `monkeypatch`).
- risks (surprises you ran into in M4 that M5 should watch for).

### 4. Commit everything in ONE commit with the required prefix

Never `git add -A`. Stage your implementation files **plus**
`initiatives/current/PROGRESS.md` and `initiatives/current/HANDOFF.md`
together. The commit subject MUST start with `[sm-dream/M4]`:

```
git -C python-replica add \
    src/simple_coding_agent/metrics.py \
    src/simple_coding_agent/loop.py \
    benchmarks/bench_sm_compact_latency.py \
    benchmarks/_results/04_sm_compact_latency.json \
    benchmarks/_results/04_sm_compact_latency.md \
    tests/test_bench_sm_compact.py \
    tests/test_metrics_collector.py \
    initiatives/current/PROGRESS.md \
    initiatives/current/HANDOFF.md
git -C python-replica commit -m "[sm-dream/M4] <one-line summary>"
```

(Add `src/simple_coding_agent/cli.py` to the stage list only if you
actually edited it.) The shell loop's check 1 is
`git log -1 | grep -qF "[sm-dream/M4]"`, and check 2 requires HANDOFF.md
to appear in **that same commit**. Both checks fail if PROGRESS/HANDOFF
land in a separate commit that lacks the prefix. **Every commit you make
during the exit ritual must carry the `[sm-dream/M4]` prefix.**

**Do NOT `git commit --amend`** to embed this commit's own SHA into
PROGRESS.md. Instead, leave the SHA as `(see git log)` in the PROGRESS
block (step 2 above). If traceability demands the exact SHA, add a
*second* `[sm-dream/M4]` commit that fills it in — the script counts ALL
matching commits, so a follow-up does not break the exit gate. See
`obs-thr-harden` M3 → `4582997` for the canonical two-commit SHA-fill
pattern.

---

Do not skip any step. After your commit, the shell script independently
verifies:

1. The commit subject contains `[sm-dream/M4]`.
2. `initiatives/current/HANDOFF.md` was modified in your commit.
3. `initiatives/current/PROGRESS.md` contains a heading matching the
   regex `^## M4 — done YYYY-MM-DD` (anchored — a stray `M4` substring in
   a notes line does NOT satisfy this).
4. `pytest --tb=no -q` is still green.
5. `initiatives/current/HANDOFF.md` contains the required 5-section
   structure (verbatim headers: `## 1. Current initiative`, `## 2.
   Completed milestones`, `## 3. Current repo state`, `## 4. Important
   constraints`, `## 5. Next milestone guidance`). A free-form HANDOFF
   fails this check even if the file was modified.
6. **Append-only contract.** For every prior milestone `M{i}` your
   initiative has already shipped (every `[sm-dream/M{i}]` commit in
   `094cf90d..HEAD`):
   - `initiatives/current/PROGRESS.md` still contains the
     `^## M{i} — done YYYY-MM-DD` block from that milestone;
   - `initiatives/current/HANDOFF.md` still contains the `^### M{i}$`
     subsection under "## 2. Completed milestones".
   Rewriting either file from scratch and only including your own `M4`
   block is a HARD FAILURE here — prior milestones are the source of
   truth on themselves; you APPEND only.

Any of those six failing halts the loop. The final review session will
also audit whether you stayed in scope and whether your HANDOFF accurately
describes your diff.
