# session-memory-dream — M6: DreamConsolidator engine (4-stage forked agent + deterministic fallback)

You are an autonomous agent executing milestone **M6** of the
**session-memory-dream** initiative. This session was launched by
`automation/scripts/run_all_milestones.sh`. There is no user available to
ask — every decision must be made and documented now.

When you are done, the shell loop will start the next milestone if (and
only if) you have honored the exit ritual in §5 below.

## §1 Baseline (verify before any code change)

- Last commit: `094cf90d09fef37b3f8357b4e2b8de0434834dfd`
- pytest: 912 passing (+1 xpassed)
- mypy: clean (mypy src → no issues in 30 source files)
- ruff: clean (ruff check . → All checks passed!)

Verify with:

```
git -C python-replica log --oneline -3
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

If the numbers do not match, **stop and report**. Do not start work
against a different baseline.

Note: the baseline above is the *initiative* baseline (the commit
`run_all_milestones.sh` resumes from). M1–M5 have shipped ahead of you,
so the live `pytest` count and `last commit` you observe will be HIGHER
than 912 / `094cf90`. That is expected — confirm pytest/mypy/ruff are
GREEN and that the immediately prior commit is the `[sm-dream/M5]`
commit (see §3 item 7), then proceed. Only "not green" or "no
`[sm-dream/M5]` commit" is a stop-and-report condition.

## §2 Scope (what THIS milestone implements)

- **Phase IDs**: ['D2']
- **Exit gate (must be objectively verifiable)**:

  New src/simple_coding_agent/dream.py::DreamConsolidator. LLM mode runs
  ForkedAgentRunner (M1) with the ported 4-stage consolidation prompt
  (Orient / Gather / Consolidate / Prune+Index) and a memory-dir-scoped
  can_use_tool gate (read-only list/read/search + writes confined to
  memory_dir; max_turns ≈ 20). Deterministic fallback (MockProvider /
  no provider) does Jaccard dedup of near-identical entries (keep newest,
  conservative high threshold) + mtime-based prune. Returns frozen
  `DreamResult(merged, pruned, runs, written_paths)`. All writes go
  through ProjectMemory.save/delete (secret + path-traversal guards
  intact). Idempotent: a second dream over an already-consolidated store
  is a no-op (merged=0, pruned=0). Gating reuses M5's consolidation_lock.
  tests/test_dream_consolidator.py passes (≥ 10 cases incl. the
  deterministic dedup/prune/idempotency paths under MockProvider).
  pytest grows by ≥ 10.

- **Expected files to touch**:

  - NEW: `src/simple_coding_agent/dream.py` (`DreamConsolidator` + frozen `DreamResult`)
  - NEW TESTS: `tests/test_dream_consolidator.py`
  - READ-ONLY references / DEPENDENCIES (do NOT modify these — read them so you wire correctly):
    - `src/simple_coding_agent/forked_agent.py` (M1 — the LLM path: `ForkedAgentRunner.run(task_prompt, context_messages=())` + per-call `can_use_tool` gate + configurable `max_turns`)
    - `src/simple_coding_agent/consolidation_lock.py` (M5 — the gate cascade: `should_dream(...)` / `try_acquire_consolidation_lock()` / `rollback_consolidation_lock()`)
    - `src/simple_coding_agent/memory.py` (`ProjectMemory.save` / the delete path; `MemorySelector` Jaccard scoring for dedup; `MemoryEntry`)
    - `src/simple_coding_agent/memdir.py` (`scan_memory_files` / manifest helpers; `MAX_ENTRYPOINT_LINES` / `MAX_ENTRYPOINT_BYTES` prune targets)

### Implementation notes from PLAN

Source mapping (read these BEFORE implementing — every claim below ties
to a specific line range in `claude-code-source-code/`):
  - `autoDream/consolidationPrompt.ts:10` `buildConsolidationPrompt` —
    PORT the 4 phases AND the anti-turn-waste directives verbatim in
    spirit: "grep narrowly, don't read whole files", "Look only for
    things you already suspect matter", and feed the session-id list
    into the prompt so the agent doesn't scan to find scope.
  - `extractMemories/extractMemories.ts:171` `createAutoMemCanUseTool` —
    the memory-dir gate to mirror (read-only ops + writes within dir).
  - `memdir.ts MAX_ENTRYPOINT_LINES=200 / MAX_ENTRYPOINT_BYTES=25_000`
    (the MEMORY.md prune targets).

Design intent (from PLAN):

Dream is the periodic-CONSOLIDATION counterpart to per-turn
`extract_memories` (capture). **DEFAULT engine = LLM** — the forked
agent runs the ported prompt and does the merge ITSELF; the harness does
NOT merge in LLM mode. The deterministic Jaccard/mtime path is the
no-API/test fallback used when there is no real provider (MockProvider /
`None`). This mirrors the existing LLMSummarizer/RuleBasedSummarizer
split (provider present → LLM, else deterministic) — locked decision #2
in PLAN.

**DEPENDS ON**: M1 (`ForkedAgentRunner`) for the LLM path, M5
(`consolidation_lock`) for gating. Reuse `MemorySelector` for the dedup
scoring rather than re-implementing Jaccard.

Concrete shape this milestone must land:

- `DreamConsolidator(memory_dir, provider=None, sessions_dir=None,
  max_turns=20)` (match the actual constructor patterns you see in
  `extract_memories.py` / `forked_agent.py` after reading them — do NOT
  invent a signature that fights the existing runner). When a real
  provider is present, `.consolidate(...)` (or the equivalent run entry
  you choose) constructs a `ForkedAgentRunner` with:
    - the 4-stage consolidation prompt as `task_prompt` (ported per
      `consolidationPrompt.ts:10`, with the session-id list fed in so the
      agent doesn't waste turns finding scope),
    - `context_messages=()` — dream reads from disk via tools, it passes
      NO conversation (PLAN M1 notes: "Dream passes none; it reads from
      disk via tools"),
    - a memory-dir-scoped `can_use_tool(name, input) -> (allow, reason)`
      gate mirroring `createAutoMemCanUseTool` (allow read-only
      list/read/search; allow writes ONLY when the target path resolves
      inside `memory_dir`; deny everything else with a reason),
    - `max_turns=20`.
- Deterministic fallback (no provider / MockProvider): scan entries via
  `memdir.scan_memory_files` / `ProjectMemory`, score near-identical
  pairs with `MemorySelector`'s Jaccard, and at a **conservative HIGH
  threshold** merge duplicates **keeping the NEWEST** (by mtime) and
  deleting the older; then mtime-based prune of stale entries. The HIGH
  threshold is the safety knob — pick a value that only collapses
  genuinely near-identical entries (document the chosen constant + the
  trade-off in the `dream.py` module header, same way `todo.py` documents
  `TODO_REMINDER_TURNS`).
- Returns a frozen `DreamResult(merged: int, pruned: int, runs: int,
  written_paths: tuple[str, ...])` (or the field names the exit gate
  lists — `merged`, `pruned`, `runs`, `written_paths`). `frozen=True`,
  immutable, no mutation of inputs.
- **All writes go through `ProjectMemory.save` / the `ProjectMemory`
  delete path** — never write `.md` files or `os.remove` directly. This
  keeps M1's `_SAFE_ENTRY_ID_PATTERN` + `is_relative_to()`
  path-traversal guard and the secret-detection rejection in force. A
  test MUST prove a path-traversal / secret body cannot escape via the
  dream write surface.
- **Idempotency is a hard exit-gate requirement**: a second
  `.consolidate()` over an already-consolidated store returns
  `merged=0, pruned=0`. Write an explicit test for this (run once, assert
  some merges/prunes; run again on the resulting store, assert 0/0).
- **Gating reuses M5's `consolidation_lock`** — do NOT re-implement the
  cascade. Read `consolidation_lock.py` and call its existing
  `should_dream(...)` / acquire / rollback entry points. On a failure
  partway through, call M5's `rollback_consolidation_lock()` so the time
  gate re-opens (per M5's contract).

Scope is **tight**. Doing more than this is failure. §2.5 below lists
what you must NOT do even if it seems natural.

## §2.5 Out of scope (do NOT do these, even if tempting)

- **Do not implement any other milestone** in this initiative. M6 is the
  dream ENGINE only. The `dream` CLI subcommand, the `--dream-on-exit`
  in-loop trigger, the `dream_runs`/`dream_merged`/`dream_pruned`
  metrics, the ADR, and the doc updates are **M7** — do NOT build them
  here even if "have time" or "see the obvious next step".
- **Do not modify** `consolidation_lock.py` (M5), `forked_agent.py` (M1),
  `memory.py`, or `memdir.py`. They are read-only dependencies. If you
  find you NEED a new public symbol from one of them, that is a scope
  signal — record it in HANDOFF Section 5 and pick a design that works
  with the existing surface instead.
- **Do not refactor unrelated modules.** Touch only `dream.py` and
  `tests/test_dream_consolidator.py`.
- **Do not modify** `automation/scripts/run_all_milestones.sh`,
  `automation/RUNBOOK.md`, or `automation/templates/*.md` — those are
  the harness, not the project.
- **Do not modify** `CLAUDE.md` per-file summaries or `README.md`. M6
  adds a new public symbol (`DreamConsolidator`), but the review session
  at the end of the initiative handles project-doc updates (per RUNBOOK
  Doc-update tiers); doing it here causes merge conflicts and wastes the
  review's audit. (M7's exit gate explicitly owns the CLAUDE.md /
  Current-Limitations updates.)
- **Do not change public API signatures** of existing modules.
- **Do not introduce new dependencies** in `pyproject.toml`. Jaccard
  scoring is already in `MemorySelector`; the LLM loop is already in
  `ForkedAgentRunner`.
- **Do not write `.md` memory files or call `os.remove` directly.** Every
  write/delete goes through `ProjectMemory`.
- **Do not let any test hit the network or require an API key.** LLM-mode
  tests use `MockProvider` scripted responses; the deterministic-fallback
  tests use no provider. Use `tmp_path` for the memory dir and
  `monkeypatch` / `os.utime` for mtimes.

If you find an obvious bug or improvement outside scope, **write it in
your HANDOFF.md Section 5 ("risks" or as a deferred item) and move on**.
Do not fix it.

## §3 Mandatory reading (in order, before any code change)

1. **`python-replica/CLAUDE.md`** — architecture, per-file summaries,
   project-wide execution rules (TDD, immutability, file-size limits).
2. **`python-replica/initiatives/current/PLAN.md`** — this initiative's
   full brief, including the milestone tables and the locked decisions.
3. **`python-replica/initiatives/current/config.yaml`** — machine-readable
   milestone table; confirms `exit_gate` + `phase_ids` for `M6`.
4. **`python-replica/initiatives/current/HANDOFF.md`** — current state.
   Section 2 "Completed milestones" tells you what shipped already (M1–M5).
   Section 4 "Important constraints" lists invariants you MUST respect.
   Section 5 "Next milestone guidance" is written FOR you.
5. **`python-replica/initiatives/current/PROGRESS.md`** — terse fact log
   of each completed milestone (commit, test delta, files touched).
6. **The Expected-files list from §2** — read the dependency source files
   this milestone wires against, plus their existing tests, before
   writing any code:
   - `src/simple_coding_agent/forked_agent.py` + `tests/test_forked_agent.py`
     (M1 — the runner you call in LLM mode: confirm the `run()` signature,
     the `can_use_tool` callback shape, and how `max_turns` is set).
   - `src/simple_coding_agent/consolidation_lock.py` +
     `tests/test_consolidation_lock.py` (M5 — the gate cascade you reuse:
     confirm `should_dream` / acquire / rollback names and the
     `sessions_dir` resolution).
   - `src/simple_coding_agent/extract_memories.py` (the capture
     counterpart — `ExtractMemoriesRunner` is the closest precedent for a
     forked memory loop; mirror its structure and its
     `_get_existing_manifest` / `build_extract_prompt` patterns).
   - `src/simple_coding_agent/memory.py` (`ProjectMemory.save` + delete,
     `MemorySelector.select_top_n` / its Jaccard scorer, `MemoryEntry`).
   - `src/simple_coding_agent/memdir.py` (`scan_memory_files`,
     `format_memory_manifest`, `MAX_ENTRYPOINT_LINES` /
     `MAX_ENTRYPOINT_BYTES`).
   - The TS source files from the §2 source mapping:
     `claude-code-source-code/src/services/autoDream/consolidationPrompt.ts`
     (port the 4-stage prompt), and
     `claude-code-source-code/src/services/extractMemories/extractMemories.ts:171`
     (the `createAutoMemCanUseTool` gate to mirror). `claude-code-source-code/`
     is READ-ONLY reference — never modify it; use it to confirm the
     source-mapping comments you put in `dream.py`.
7. Run `git -C python-replica log --oneline -10` and `git -C
   python-replica show HEAD~1` to see what the immediately prior
   milestone (M5) changed. `HEAD~1` is M5's commit because each milestone
   commits exactly once before the loop advances; if you suspect that
   invariant has been violated, use `git -C python-replica log
   094cf90d09fef37b3f8357b4e2b8de0434834dfd..HEAD --oneline | grep -F
   "[sm-dream/" | head -1` instead and `git -C python-replica show <sha>`
   to inspect that SHA. The `-C python-replica` is required because the
   autonomous loop launches `claude --print` from `/Users/leng/my-cc-py`,
   not from inside the repo. The `grep -F` is required because the literal
   `[` in the marker would otherwise be parsed as an unclosed character
   class (e.g. `[sm-dream/` triggers `grep: invalid character range`);
   `-F` forces fixed-string matching. The
   `094cf90d09fef37b3f8357b4e2b8de0434834dfd..HEAD` range matches what
   `run_all_milestones.sh` uses, so the fallback stays consistent with the
   resumability check and is robust against any prefix-collision edge case
   Phase 1 pre-flight may have missed. **Reading M5's commit is
   non-optional for M6**: it shows you the exact `consolidation_lock.py`
   API (function names, `sessions_dir` resolution, the
   `mtime == lastConsolidatedAt` contract) that your gating MUST call.
8. Skim `initiatives/current/logs/M5.log` (and earlier `M{N}.log` files if
   present) for any anomalies (warnings, retries, KeyboardInterrupts) that
   didn't make it into HANDOFF Section 2 design-decisions — especially any
   note that the M5 lock API diverged from the names in PLAN's source
   mapping.

**Before invoking any Edit or Write tool**, print a **5-bullet summary**
to stdout (plain text, not a tool call, not a TaskCreate todo, not a
file write) of what you learned from PLAN / HANDOFF / PROGRESS / the M5
commit / the M1 `ForkedAgentRunner`. If those sources disagree on a fact,
treat HANDOFF as advisory and `git diff` + test output as source of
truth. This summary lives only in the captured session log under
`initiatives/current/logs/` (not in any committed file) — its purpose is
to force genuine reading rather than skimming. If you cannot produce a
substantive 5-bullet summary, you have not read enough; go back to §3.

## §4 Implementation requirements

Follow execution rules documented in `python-replica/CLAUDE.md`. In
particular:

- **TDD**: write tests first (RED — must actually run and fail before
  implementation), implement (GREEN), refactor. Every new code path
  needs a test before the implementation lands. Write the deterministic
  dedup / prune / idempotency tests FIRST (they need no provider and pin
  the contract), then the LLM-mode test (MockProvider scripting the
  forked agent's tool calls), then the path-traversal/secret-guard test.
- **Immutability**: `DreamResult` is `frozen=True`; never mutate inputs;
  return new objects. The dedup/prune logic must not mutate the scanned
  entry list in place — build new collections.
- **File limits**: functions ≤50 lines, files ≤800 lines. The 4-stage
  prompt text is large — if `dream.py` approaches the limit, keep the
  prompt as a module-level constant and keep the consolidate loop thin
  (mirror how `extract_memories.py` keeps `build_extract_prompt`
  separate from the runner).
- **Determinism in tests**: `MockProvider`, `tmp_path`, `monkeypatch`,
  `os.utime`. No network. No real API key. No real `time.sleep`.
- **No swallowed exceptions** (the `obs-thr-harden` precedent allows
  swallowing only `(OSError, ValueError)` at the trace-emit boundary —
  do not extend that elsewhere; a failed dream must surface, then
  rollback the lock).
- **No `git add -A`** — stage explicit paths only.
- **Match style** of existing modules — `extract_memories.py` is your
  closest structural template (frozen result dataclass, `__all__` export
  list, source-mapping comments at the cited TS line ranges, a thin
  whitelisted/gated tool-execution loop).

After every meaningful diff, run:

```
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

All three must be green before you commit.

## §5 Exit ritual (MANDATORY)

After your milestone work passes the exit gate in §2, before stopping,
perform these steps **in order**. The shell script independently
verifies the **6 side-effects** listed at the end of this section.
Step 1 below ("Confirm exit gate met") is **honor-system** — the script
cannot inspect whether you actually quoted the right command output, so
the mechanical checks are produced by steps 2-4 (commit, PROGRESS
append, HANDOFF rewrite) plus the standing pytest-green requirement
from §4. Skipping step 1 makes a green run meaningless even though it
would still pass the gate; do not skip it.

### 1. Confirm exit gate met (objectively)

Quote the concrete check from §2 verbatim and show the command output
that proves it. Example:

```
$ pytest tests/test_dream_consolidator.py --tb=no -q
..........  10 passed in 0.5s

$ python -c "from simple_coding_agent.dream import DreamConsolidator, DreamResult; print(DreamResult.__dataclass_params__.frozen)"
True
```

The idempotency clause and the deterministic-fallback clause are part of
the gate — show the test names that prove "second dream → merged=0,
pruned=0" and "writes go through ProjectMemory (path-traversal/secret
rejected)". "Implementation feels complete" is NOT acceptable. The gate
must be objectively verified by a command's output.

### 2. Append a block to `initiatives/current/PROGRESS.md`

**Write this BEFORE committing** so it can be staged in the same commit
as your code. PROGRESS.md is the **fact log for the final review** —
narrative belongs in HANDOFF, not here.

Block shape (use terse format from `automation/templates/progress_entry.md`):

```
## M6 — done YYYY-MM-DD

- commit: [sm-dream/M6] (see git log)
- tests: <before> -> <after> (+N)
- mypy: clean | ruff: clean
- files changed: `src/simple_coding_agent/dream.py`, `tests/test_dream_consolidator.py`
- exit gate: <quote from §2> -> PASS (<one-line evidence>)
- notes: <optional, ≤1 line>
```

Leave the SHA as `(see git log)` — you do not know it yet and must NOT
amend to fill it in (see note in step 4 below).

### 3. Rewrite `initiatives/current/HANDOFF.md`

Use the 5-section structure in `automation/templates/handoff_milestone.md`.
The structure is non-negotiable.

**Section 2 "Completed milestones"**: APPEND your own `### M6` subsection
after the existing `### M1` … `### M5` subsections. Do NOT delete or
rewrite prior milestone blocks — each milestone is the source of truth
on itself.

**Section 4 "Important constraints"**: if your work froze any file,
behavior, or interface that M7 must not change, add it here. At minimum
record: the `DreamResult` field set / frozen contract, the
"all writes through ProjectMemory" invariant, and the
`DreamConsolidator` constructor signature (M7 wires the CLI against it,
so its shape is now public API). This is how invariants propagate across
the initiative.

**Section 5 "Next milestone guidance"**: write as if onboarding the M7
agent in person. Include:
- next scope: M7 — `simple-agent memory dream` CLI subcommand (dry-run
  default / `--apply` / `--force`), the opt-in `--dream-on-exit` in-loop
  trigger, `dream_runs`/`dream_merged`/`dream_pruned` metrics in `/stats`,
  an ADR under `docs/DECISIONS`, and the CLAUDE.md / Current-Limitations
  (no-cron divergence) doc updates — paraphrased from PLAN, sharpened
  with anything you learned about the `DreamConsolidator` surface.
- relevant files: `memory_cli.py` (extend add/list/delete/search/show
  routing), `metrics.py` (new counters), `cli.py` /
  `cli._build_repl_loop` (the `--dream-on-exit` trigger + provider
  injection pattern), and your new `dream.py`.
- expected tests: `tests/test_memory_cli_dream.py` (dry-run vs --apply
  vs --force, exit codes, provider injection).
- risks: surprises you ran into (e.g., how the M1 runner's `can_use_tool`
  signature actually looks vs PLAN's description; whether the M5 lock
  names matched PLAN; any threshold tuning that was finicky).

### 4. Commit everything in ONE commit with the required prefix

Never `git add -A`. Stage your implementation files **plus**
`initiatives/current/PROGRESS.md` and `initiatives/current/HANDOFF.md`
together. The commit subject MUST start with `[sm-dream/M6]`:

```
git -C python-replica add src/simple_coding_agent/dream.py \
    tests/test_dream_consolidator.py \
    initiatives/current/PROGRESS.md \
    initiatives/current/HANDOFF.md
git -C python-replica commit -m "[sm-dream/M6] <one-line summary>"
```

The shell loop's check 1 is `git log -1 | grep -qF "[sm-dream/M6]"`,
and check 2 requires HANDOFF.md to appear in **that same commit**. Both
checks fail if PROGRESS/HANDOFF land in a separate commit that lacks the
prefix. **Every commit you make during the exit ritual must carry the
`[sm-dream/M6]` prefix.**

**Do NOT `git commit --amend`** to embed this commit's own SHA into
PROGRESS.md. Instead, leave the SHA as `(see git log)` in the PROGRESS
block (step 2 above). If traceability demands the exact SHA, add a
*second* `[sm-dream/M6]` commit that fills it in — the script counts ALL
matching commits, so a follow-up does not break the exit gate. See
`obs-thr-harden` M3 → `4582997` for the canonical two-commit SHA-fill
pattern.

---

Do not skip any step. After your commit, the shell script independently
verifies:

1. The commit subject contains `[sm-dream/M6]`.
2. `initiatives/current/HANDOFF.md` was modified in your commit.
3. `initiatives/current/PROGRESS.md` contains a heading matching the
   regex `^## M6 — done YYYY-MM-DD` (anchored — a stray `M6` substring in
   a notes line does NOT satisfy this).
4. `pytest --tb=no -q` is still green.
5. `initiatives/current/HANDOFF.md` contains the required 5-section
   structure (verbatim headers: `## 1. Current initiative`, `## 2.
   Completed milestones`, `## 3. Current repo state`, `## 4. Important
   constraints`, `## 5. Next milestone guidance`). A free-form HANDOFF
   fails this check even if the file was modified.
6. **Append-only contract.** For every prior milestone `M{i}` your
   initiative has already shipped (every `[sm-dream/M{i}]` commit in
   `094cf90d09fef37b3f8357b4e2b8de0434834dfd..HEAD` — i.e. M1–M5):
   - `initiatives/current/PROGRESS.md` still contains the
     `^## M{i} — done YYYY-MM-DD` block from that milestone;
   - `initiatives/current/HANDOFF.md` still contains the `^### M{i}$`
     subsection under "## 2. Completed milestones".
   Rewriting either file from scratch and only including your own `M6`
   block is a HARD FAILURE here — prior milestones are the source of
   truth on themselves; you APPEND only.

Any of those six failing halts the loop. The final review session will
also audit whether you stayed in scope and whether your HANDOFF accurately
describes your diff.
