# session-memory-dream — M7: dream CLI subcommand + optional post-session trigger + metrics + docs

You are an autonomous agent executing milestone **M7** of the
**session-memory-dream** initiative. This session was launched by
`automation/scripts/run_all_milestones.sh`. There is no user available to
ask — every decision must be made and documented now.

When you are done, the shell loop will start the next milestone if (and
only if) you have honored the exit ritual in §5 below.

**M7 is the LAST milestone** of this initiative. The §5 exit ritual has
an extra mandatory step (mark `initiatives/current/PLAN.md` STATUS =
complete). Do not skip it.

## §1 Baseline (verify before any code change)

- Last commit: `094cf90d09fef37b3f8357b4e2b8de0434834dfd`
- pytest: 912 passing (+1 xpassed)
- mypy: clean (mypy src → no issues in 30 source files)
- ruff: clean (ruff check . → All checks passed!)

Verify with:

```
git -C python-replica log --oneline -3
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

If the numbers do not match, **stop and report**. Do not start work
against a different baseline. NOTE: this is the baseline at *initiative
bootstrap* (2026-06-15). M1–M6 have shipped since, so the live counts on
HEAD are higher — re-verify against HANDOFF.md Section 3 (the post-M6
state), and treat the post-M6 pytest total as your "<before>" number in
the PROGRESS block.

## §2 Scope (what THIS milestone implements)

- **Phase IDs**: [D3, D4]
- **Exit gate (must be objectively verifiable)**:

  memory_cli gains a `dream` subcommand: DEFAULT dry-run (prints planned
  merged/pruned counts, writes NOTHING), `--apply` actually consolidates,
  `--force` bypasses the M5 gate cascade for demo. Resolves memory_dir
  from SIMPLE_AGENT_MEMORY_DIR or <cwd>/.simple-agent/memory/; exit 0 on
  success, exit 2 on bad dir; MockProvider default, `--provider openai`
  constructs an OpenAIProvider (mocked in tests). Optional in-loop
  trigger behind `--dream-on-exit` (default OFF, mirrors
  extract_memories_enabled) fires one dream at REPL /exit. MetricsCollector
  gains dream_runs / dream_merged / dream_pruned shown in /stats. An ADR
  under docs/DECISIONS, CLAUDE.md per-file summaries, and Current
  Limitations (the no-cron divergence) are updated.
  tests/test_memory_cli_dream.py passes. pytest grows by ≥ 8.

- **Expected files to touch**:

  - MODIFIED: `src/simple_coding_agent/memory_cli.py` — new `dream`
    subcommand: DEFAULT dry-run (prints planned merged/pruned counts,
    writes NOTHING) / `--apply` (actually consolidates) / `--force`
    (bypasses the M5 gate cascade for demo) / `--provider openai`
    (constructs an `OpenAIProvider`; `MockProvider` is the default).
    memory_dir resolved from `SIMPLE_AGENT_MEMORY_DIR` or
    `<cwd>/.simple-agent/memory/`; exit 0 on success, exit 2 on bad dir.
  - MODIFIED: `src/simple_coding_agent/loop.py` — optional post-session
    `/exit` trigger behind `--dream-on-exit` (a new `dream_on_exit: bool
    = False` AgentLoop kwarg, default OFF, mirroring
    `extract_memories_enabled`); fires exactly one dream at REPL `/exit`.
  - MODIFIED: `src/simple_coding_agent/cli.py` — `--dream-on-exit` flag
    threading into the REPL-spawned `AgentLoop` (openai_cli.py inherits
    via the shared `_drive_repl_session` / `_build_repl_loop` path — do
    NOT duplicate the flag wiring there unless a test proves it is
    needed).
  - MODIFIED: `src/simple_coding_agent/metrics.py` — `dream_runs`,
    `dream_merged`, `dream_pruned` counters (+ `record_*` methods +
    `format_stats` lines) surfaced via the REPL `/stats` command.
  - NEW: `docs/DECISIONS/0005-<slug>.md` — ADR for the no-cron
    divergence + dry-run-default safety posture (next free number is
    0005; 0001–0004 exist).
  - MODIFIED: `CLAUDE.md` — per-file summaries for the touched modules +
    a new "Current Limitations" bullet documenting the no-cron
    divergence (see §2.5 exception below).
  - NEW: `tests/test_memory_cli_dream.py` — the milestone's test file
    (≥ 8 cases; pytest grows by ≥ 8).

### Implementation notes from PLAN

Source mapping (read these BEFORE implementing — every claim below ties
to a specific line range; consult the TS reference under
`claude-code-source-code/` read-only to confirm):

  - `query/stopHooks.ts:155` `executeAutoDream` (fire-and-forget at turn
    end) — the replica analog is the opt-in `/exit` trigger PLUS the CLI
    batch command. The replica's no-async stand-in for "fire-and-forget
    at turn end" is a synchronous, post-session, opt-in dream.
  - `utils/backgroundHousekeeping.ts:37` `initAutoDream` (the startup
    arm) — the replica has no startup background housekeeping thread;
    "scheduled dream" is instead the externally-invoked CLI subcommand
    (see the divergence note below).

DIVERGENCE TO DOCUMENT PROMINENTLY (ADR + CLAUDE.md Current
Limitations): the replica has no cron and no asyncio/background thread,
so "scheduled dream" = `simple-agent memory dream` invoked by an
EXTERNAL cron / RUNBOOK; the in-loop trigger is opt-in, post-session
ONLY (fired at REPL `/exit`), NOT per-turn like the TS stop hook.
Document this the same way the loop already documents the synchronous
sideQuery recall divergence (see the `recall_hooks.py` summary in
CLAUDE.md and the existing "no asyncio loop" wording from M3).

SAFETY POSTURE (this is the whole reason the default is dry-run): dream
is DESTRUCTIVE — it merges, prunes, and deletes memory files via
`ProjectMemory.save`/`delete` (M6's `DreamConsolidator`). The DEFAULT
dry-run path protects against memory data loss: it reports the planned
`merged`/`pruned` counts and writes NOTHING. `--apply` is REQUIRED to
write. The in-loop `--dream-on-exit` trigger is OFF by default. Do not
invert these defaults.

`memory_cli` precedent to extend (read it before writing the
subcommand):
  - existing `add` / `list` / `delete` / `search` / `show` arg routing
    and subcommand dispatch shape — match it exactly for `dream`.
  - `SIMPLE_AGENT_MEMORY_DIR` resolution + `<cwd>/.simple-agent/memory/`
    fallback (the SAME helper the other subcommands use — reuse, do not
    re-derive).
  - the exit-code-2 convention for bad dir / guard violations (P9 M1
    memory_cli established it; secret-body + path-traversal guards
    already surface as exit 2).
  - `cli._build_repl_loop`'s provider-injection (Mock vs OpenAI)
    pattern — mirror it for the `--provider openai` arg so the dream
    subcommand constructs the same provider the REPL would, and so tests
    can inject a mocked provider with no network / no API key.

ENGINE REUSE (do NOT reimplement consolidation here): the `dream`
subcommand and the `/exit` trigger are THIN drivers over M6's
`src/simple_coding_agent/dream.py::DreamConsolidator` (which returns a
frozen `DreamResult(merged, pruned, runs, written_paths)`) gated by
M5's `src/simple_coding_agent/consolidation_lock.py`. M7 wires the
surface; it does not add dedup/prune logic. In dry-run mode you still
need a planned count — read `DreamConsolidator`'s API from the M6 commit
(`git -C python-replica show` on the `[sm-dream/M6]` commit) to see
whether it exposes a plan/preview path or whether dry-run means "run the
deterministic consolidator against a scratch copy and report counts
without persisting". Match whatever M6 actually shipped; do not assume.

GATE INTERACTION: `--force` bypasses the M5 gate cascade
(`should_dream` / time-gate / session-gate / scan-throttle / PID lock).
Without `--force`, the CLI MUST run the M5 gate cascade first and, if a
gate is closed, report that (e.g. "dream skipped: <gate reason>") and
exit 0 (a closed gate is a normal no-op, not an error). Confirm the M5
function names and signatures from the `[sm-dream/M5]` commit before
calling them.

Scope is **tight**. Doing more than this is failure. §2.5 below lists
what you must NOT do even if it seems natural.

## §2.5 Out of scope (do NOT do these, even if tempting)

- **Do not implement any other milestone** in this initiative. M7 is the
  last one; there is no "obvious next step" to start — stop at `M7`.
- **Do not reimplement the dream engine or the gate cascade.** The merge
  / dedup / prune logic lives in M6's `dream.py::DreamConsolidator`; the
  gating lives in M5's `consolidation_lock.py`. M7 is a CLI + loop-hook +
  metrics surface ONLY. If `DreamConsolidator` or `consolidation_lock`
  needs a change to be drivable, prefer a thin additive wrapper in
  `memory_cli.py` over editing those frozen modules; if you genuinely
  must touch them, document why in HANDOFF Section 4 and keep it minimal.
- **Do not refactor unrelated modules.** Touch only files in the
  Expected-files list (§2), plus the named test file.
- **Do not modify** `automation/scripts/run_all_milestones.sh`,
  `automation/RUNBOOK.md`, or `automation/templates/*.md` — those are the
  harness, not the project.
- **Do not change public API signatures** of existing modules unless §2
  requires it (the `AgentLoop` `dream_on_exit` kwarg and the new
  `metrics.py` counters ARE required additions; they must be
  back-compatible — default OFF / zero, mirroring how
  `extract_memories_enabled` and the existing counters were added).
- **Do not introduce new dependencies** in `pyproject.toml`.
- **Do not add a new trace channel.** The 11-name trace vocabulary
  (`trace.py`) is frozen and test-pinned (M4 already reused the existing
  `compact` channel for SM rather than adding one). Dream is surfaced via
  the `metrics.py` counters + the `/stats` command + the CLI output —
  NOT a 12th trace channel. If you want a trace line, reuse an existing
  channel; do not register a new name.

**DOC EXCEPTION (this milestone only).** The standard template rule is
"do not modify CLAUDE.md per-file summaries or README until the final
review." That rule is OVERRIDDEN here: M7 adds new public symbols
(`dream` subcommand, `--dream-on-exit` flag, `dream_runs`/`dream_merged`/
`dream_pruned` counters) AND is itself the last milestone before the
review, so it legitimately updates CLAUDE.md per-file summaries for the
modules it touches and adds the no-cron "Current Limitations" bullet.
This is the explicit exception. Keep the edits scoped to the modules YOU
touched; do not rewrite unrelated per-file summaries.

If you find an obvious bug or improvement outside scope, **write it in
your HANDOFF.md Section 5 ("risks" or as a deferred item) and move on**.
Do not fix it.

## §3 Mandatory reading (in order, before any code change)

1. **`python-replica/CLAUDE.md`** — architecture, per-file summaries,
   project-wide execution rules (TDD, immutability, file-size limits).
   Pay attention to the `memory_cli.py` (P9 M1), `metrics.py` (P9 M3),
   `loop.py`, and `extraction_hooks.py` summaries — they show the
   precedent patterns (subcommand dispatch, counter add, opt-in stop
   hook) you are extending.
2. **`python-replica/initiatives/current/PLAN.md`** — this initiative's
   full brief: Goal (the "auto-dream" mechanism = the 4th memory layer),
   Design sketch (M5–M7 dream sequence), locked decisions (default LLM +
   deterministic fallback; faithful gate cascade; `--force` bypass;
   dry-run safety).
3. **`python-replica/initiatives/current/config.yaml`** — machine-readable
   milestone table; confirms `exit_gate` + `phase_ids` for `M7`.
4. **`python-replica/initiatives/current/HANDOFF.md`** — current state.
   Section 2 "Completed milestones" tells you what M1–M6 shipped
   already (especially the M5 lock API and the M6 `DreamConsolidator` /
   `DreamResult` API you will call). Section 3 "Current repo state" has
   the live post-M6 pytest count — use it, not the §1 bootstrap number.
   Section 4 "Important constraints" lists invariants you MUST respect.
   Section 5 "Next milestone guidance" is written FOR you (M7).
5. **`python-replica/initiatives/current/PROGRESS.md`** — terse fact log
   of M1–M6 (commit, test delta, files touched).
6. **The Expected-files list from §2** — read the source files this
   milestone will touch, plus their existing tests, before writing any
   code. In particular read `memory_cli.py` end-to-end (its subcommand
   dispatch + dir resolution + exit-code-2 convention),
   `src/simple_coding_agent/dream.py` (M6 — the engine you drive),
   `src/simple_coding_agent/consolidation_lock.py` (M5 — the gate you
   call), and `metrics.py` (the counter-add pattern).
7. Run `git -C python-replica log --oneline -10` and
   `git -C python-replica show HEAD~1` to see what the immediately prior
   milestone (M6, `DreamConsolidator`) changed. `HEAD~1` is the prior
   milestone's commit because each milestone commits exactly once before
   the loop advances; if you suspect that invariant has been violated,
   use `git -C python-replica log
   094cf90d09fef37b3f8357b4e2b8de0434834dfd..HEAD --oneline | grep -F
   "[sm-dream/" | head -1` instead and `git -C python-replica show
   <sha>` to inspect that SHA. The `-C python-replica` is required
   because the autonomous loop launches `claude --print` from
   `/Users/leng/my-cc-py`, not from inside the repo. The `grep -F`
   forces fixed-string matching because the literal `[` in
   `[sm-dream/` would otherwise be parsed as an unclosed character class
   (`grep: invalid character range`). The
   `094cf90d09fef37b3f8357b4e2b8de0434834dfd..HEAD` range matches what
   `run_all_milestones.sh` uses, so the fallback stays consistent with
   the resumability check and is robust against any prefix-collision
   edge case Phase 1 pre-flight may have missed.
8. Since M7 > M2, skim `initiatives/current/logs/M6.log` (and, if
   present, `M5.log`) for any anomalies (warnings, retries,
   KeyboardInterrupts) that didn't make it into HANDOFF Section 2
   design-decisions — especially anything about the `DreamConsolidator`
   dry-run/preview surface or the M5 gate signatures, since M7 calls
   both.

**Before invoking any Edit or Write tool**, print a **5-bullet summary**
to stdout (plain text, not a tool call, not a TaskCreate todo, not a
file write) of what you learned from PLAN / HANDOFF / PROGRESS / the M6
commit. If those sources disagree on a fact, treat HANDOFF as advisory
and `git diff` + test output as source of truth. This summary lives only
in the captured session log under `initiatives/current/logs/` (not in
any committed file) — its purpose is to force genuine reading rather than
skimming. If you cannot produce a substantive 5-bullet summary, you have
not read enough; go back to §3.

## §4 Implementation requirements

Follow execution rules documented in `python-replica/CLAUDE.md`. In
particular:

- **TDD**: write tests first (RED — must actually run and fail before
  implementation), implement (GREEN), refactor. Every new code path
  needs a test before the implementation lands. The CORE tests:
  (a) `dream` dry-run reports planned counts and writes NOTHING (assert
  the memory dir is byte-identical before/after); (b) `dream --apply`
  actually invokes `DreamConsolidator` and persists; (c) `dream --force`
  bypasses a CLOSED M5 gate (inject a fresh lock so the time gate would
  otherwise be closed); (d) a closed gate without `--force` is a no-op
  exit 0; (e) bad dir → exit 2; (f) `--provider openai` constructs an
  `OpenAIProvider` (mock the SDK; no network, no key); (g) the metrics
  counters bump and show in `/stats`; (h) `--dream-on-exit` OFF (default)
  fires no dream at `/exit`, ON fires exactly one.
- **Immutability**: dataclasses prefer `frozen=True` where appropriate;
  never mutate inputs; return new objects. `DreamResult` is already
  frozen (M6) — read it, do not re-wrap it mutably.
- **File limits**: functions ≤50 lines, files ≤800 lines. `memory_cli.py`
  and `loop.py` are large — check their current line counts BEFORE
  adding; if a change would push a file over 800 lines, extract a small
  helper (e.g. a `dream_cli.py` helper module the subcommand delegates
  to) rather than bloating the file. Note that extraction in earlier
  milestones (`extraction_hooks.py`, `recall_hooks.py`) was done exactly
  to keep `loop.py` ≤800.
- **Determinism in tests**: `MockProvider`, `tmp_path`, `monkeypatch`.
  No network. No real API key. The `--provider openai` test mocks the
  provider construction; it must never make a live call.
- **No swallowed exceptions**.
- **No `git add -A`** — stage explicit paths only.
- **Match style** of existing modules (see CLAUDE.md per-file summaries).

After every meaningful diff, run:

```
cd python-replica && pytest --tb=no -q
cd python-replica && mypy src/
cd python-replica && ruff check .
```

All three must be green before you commit.

## §5 Exit ritual (MANDATORY)

After your milestone work passes the exit gate in §2, before stopping,
perform these steps **in order**. The shell script independently
verifies the **6 side-effects** listed at the end of this section.
Step 1 below ("Confirm exit gate met") is **honor-system** — the script
cannot inspect whether you actually quoted the right command output, so
the mechanical checks are produced by steps 2-4 (commit, PROGRESS
append, HANDOFF rewrite) plus the standing pytest-green requirement from
§4. Skipping step 1 makes a green run meaningless even though it would
still pass the gate; do not skip it.

### 1. Confirm exit gate met (objectively)

Quote the concrete check from §2 verbatim and show the command output
that proves it. Example:

```
$ pytest tests/test_memory_cli_dream.py --tb=no -q
........  8 passed in 0.5s

$ SIMPLE_AGENT_MEMORY_DIR=/tmp/mem python -m simple_coding_agent.memory_cli dream
dream (dry-run): planned merged=2 pruned=1 — no files written (use --apply)

$ SIMPLE_AGENT_MEMORY_DIR=/tmp/mem python -m simple_coding_agent.memory_cli dream --apply
dream applied: merged=2 pruned=1 written=3
```

"Implementation feels complete" is NOT acceptable. The gate must be
objectively verified by a command's output.

### 2. Append a block to `initiatives/current/PROGRESS.md`

**Write this BEFORE committing** so it can be staged in the same commit
as your code. PROGRESS.md is the **fact log for the final review** —
narrative belongs in HANDOFF, not here.

Block shape (use terse format from `automation/templates/progress_entry.md`):

```
## M7 — done YYYY-MM-DD

- commit: [sm-dream/M7] (see git log)
- tests: <before> -> <after> (+N)
- mypy: clean | ruff: clean
- files changed: `<file1>`, `<file2>`, ...
- exit gate: <quote from §2> -> PASS (<one-line evidence>)
- notes: <optional, ≤1 line>
```

Use the post-M6 pytest total (from HANDOFF Section 3) as `<before>`, not
the §1 bootstrap baseline of 912. Leave the SHA as `(see git log)` — you
do not know it yet and must NOT amend to fill it in (see note in step 4
below).

### 3. Rewrite `initiatives/current/HANDOFF.md`

Use the 5-section structure in `automation/templates/handoff_milestone.md`.
The structure is non-negotiable.

**Section 2 "Completed milestones"**: PRESERVE `### M1`–`### M6`
verbatim and APPEND your own `### M7` subsection. Do NOT delete or
rewrite prior milestone blocks — each milestone is the source of truth
on itself (exit-gate check 6 walks every prior `[sm-dream/M{i}]` commit
and verifies its `### M{i}` subsection still exists).

**Section 4 "Important constraints"**: M7 is the last milestone, so add
the closing note for the review session, e.g. "All 7 milestones complete
(M1 ForkedAgentRunner → M7 dream CLI). No further invariants to
propagate — the review session handles any remaining doc updates." Also
record any constraint M7 itself froze (e.g. "dream is dry-run by
default; `--apply` required to write — do not invert").

**Section 5 "Next milestone guidance"**: there is no next milestone.
Write this section FOR THE REVIEW SESSION instead: note the no-cron
divergence is documented in ADR-0005 + CLAUDE.md Current Limitations;
list any deferred items surfaced during M1–M7 (e.g. the out-of-scope TS
TUI dream surfacing, GrowthBook flags, truly-concurrent thread-backed
extractor); and point at `REVIEW.md`-relevant facts (benchmark honesty,
dual-arm latency from M4).

### Last-milestone extra step — mark PLAN.md STATUS = complete

Because M7 is the final milestone, edit `initiatives/current/PLAN.md` to
record completion. Append a new `>` blockquote line immediately AFTER
the existing `> Bootstrapped on 2026-06-15. Baseline commit: ...` line
(near the top of the prose body, just under the YAML front-matter), with
this exact shape:

```
> STATUS: complete (M1–M7 shipped on YYYY-MM-DD)
```

Do NOT alter the YAML section (the `milestones:` table is parsed by the
harness). Stage `initiatives/current/PLAN.md` in the SAME `[sm-dream/M7]`
commit as everything else (step 4).

### 4. Commit everything in ONE commit with the required prefix

Never `git add -A`. Stage your implementation files **plus**
`initiatives/current/PROGRESS.md`, `initiatives/current/HANDOFF.md`,
**and** `initiatives/current/PLAN.md` (the STATUS line) together. The
commit subject MUST start with `[sm-dream/M7]`:

```
git -C python-replica add src/simple_coding_agent/memory_cli.py \
    src/simple_coding_agent/loop.py \
    src/simple_coding_agent/cli.py \
    src/simple_coding_agent/metrics.py \
    docs/DECISIONS/0005-<slug>.md \
    CLAUDE.md \
    tests/test_memory_cli_dream.py \
    initiatives/current/PROGRESS.md \
    initiatives/current/HANDOFF.md \
    initiatives/current/PLAN.md
git -C python-replica commit -m "[sm-dream/M7] <one-line summary>"
```

The shell loop's check 1 is `git log -1 | grep -qF "[sm-dream/M7]"`,
and check 2 requires HANDOFF.md to appear in **that same commit**. Both
checks fail if PROGRESS/HANDOFF land in a separate commit that lacks the
prefix. **Every commit you make during the exit ritual must carry the
`[sm-dream/M7]` prefix.**

**Do NOT `git commit --amend`** to embed this commit's own SHA into
PROGRESS.md. Instead, leave the SHA as `(see git log)` in the PROGRESS
block (step 2 above). If traceability demands the exact SHA, add a
*second* `[sm-dream/M7]` commit that fills it in — the script counts ALL
matching commits, so a follow-up does not break the exit gate. See
`obs-thr-harden` M3 → `4582997` for the canonical two-commit SHA-fill
pattern.

---

Do not skip any step. After your commit, the shell script independently
verifies:

1. The commit subject contains `[sm-dream/M7]`.
2. `initiatives/current/HANDOFF.md` was modified in your commit.
3. `initiatives/current/PROGRESS.md` contains a heading matching the
   regex `^## M7 — done YYYY-MM-DD` (anchored — a stray `M7` substring
   in a notes line does NOT satisfy this).
4. `pytest --tb=no -q` is still green.
5. `initiatives/current/HANDOFF.md` contains the required 5-section
   structure (verbatim headers: `## 1. Current initiative`, `## 2.
   Completed milestones`, `## 3. Current repo state`, `## 4. Important
   constraints`, `## 5. Next milestone guidance`). A free-form HANDOFF
   fails this check even if the file was modified.
6. **Append-only contract.** For every prior milestone `M{i}` your
   initiative has already shipped (every `[sm-dream/M{i}]` commit in
   `baseline_commit..HEAD` — i.e. M1–M6):
   - `initiatives/current/PROGRESS.md` still contains the
     `^## M{i} — done YYYY-MM-DD` block from that milestone;
   - `initiatives/current/HANDOFF.md` still contains the `^### M{i}$`
     subsection under "## 2. Completed milestones".
   Rewriting either file from scratch and only including your own `M7`
   block is a HARD FAILURE here — prior milestones are the source of
   truth on themselves; you APPEND only.

Any of those six failing halts the loop. The final review session will
also audit whether you stayed in scope and whether your HANDOFF
accurately describes your diff.
